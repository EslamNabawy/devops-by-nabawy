// covers: first-page thumbnails need poppler (absent) — typographic fallback.
// Records cover:null; UI draws typographic cover. Never fails the build.
export async function makeCover() {
  return null;
}
