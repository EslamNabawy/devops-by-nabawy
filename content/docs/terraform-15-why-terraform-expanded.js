window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["terraform-15-why-terraform-expanded"]={"html":"<p>{/* MIGRATED from: D:\\NTI Courses Master Folder\\TerraformMaster Folder\\Terraform\\level1\\04-why-terraform.md (2026-09-06)\nCourse metadata: course=terraform | level=1 | difficulty=Beginner */}</p>\n<h1 id=\"why-terraform-expanded\">Why Terraform? — Expanded</h1>\n<p>Companion to IaC and CloudFormation comparison. One-line: <strong>learn once, manage anything, with the best preview and biggest ecosystem.</strong></p>\n<h2 id=\"1-your-6-points-each-idea-example-objection-answered\">1. Your 6 points (each: idea → example → objection answered)</h2>\n<h3 id=\"1-iac-done-right\">1. IaC done right</h3>\n<p>Idea: code replaces clicks; Git replaces memory.</p>\n<pre><code class=\"language-bash\">git <span class=\"hljs-built_in\">log</span> --oneline -- infra/   <span class=\"hljs-comment\"># who built prod? answered in 5 sec</span>\ngit revert &lt;sha&gt; &amp;&amp; terraform apply  <span class=\"hljs-comment\"># rollback without re-clicking 40 things</span>\n</code></pre>\n<p>Objection \"scripts do that\" → scripts aren't idempotent/previewable; Terraform is.</p>\n<h3 id=\"2-platform-agnostic-with-proof\">2. Platform agnostic (with proof)</h3>\n<pre><code class=\"language-hcl\"><span class=\"hljs-attribute\">resource</span> <span class=\"hljs-string\">\"aws_s3_bucket\"</span> <span class=\"hljs-string\">\"a\"</span> { <span class=\"hljs-attribute\">bucket</span> = <span class=\"hljs-string\">\"x\"</span> }            <span class=\"hljs-comment\"># AWS</span>\nresource <span class=\"hljs-string\">\"azurerm_resource_group\"</span> <span class=\"hljs-string\">\"b\"</span> { <span class=\"hljs-attribute\">name</span> = <span class=\"hljs-string\">\"rg\"</span> }    <span class=\"hljs-comment\"># Azure, same file works</span>\nresource <span class=\"hljs-string\">\"github_repository\"</span> <span class=\"hljs-string\">\"c\"</span> { <span class=\"hljs-attribute\">name</span> = <span class=\"hljs-string\">\"infra\"</span> }      <span class=\"hljs-comment\"># SaaS, same apply</span>\n</code></pre>\n<p>One <code>apply</code> touches 3 APIs. CFN/Bicep can't. Objection \"we're AWS-only\" → still useful for GitHub/Cloudflare/Datadog in same run.</p>\n<h3 id=\"3-plan-before-apply-with-sample\">3. Plan before apply (with sample)</h3>\n<pre><code>Plan: <span class=\"hljs-number\">1</span> <span class=\"hljs-keyword\">to</span> <span class=\"hljs-built_in\">add</span>, <span class=\"hljs-number\">1</span> <span class=\"hljs-keyword\">to</span> <span class=\"hljs-keyword\">change</span>, <span class=\"hljs-number\">0</span> <span class=\"hljs-keyword\">to</span> destroy.\n  + aws_s3_bucket.<span class=\"hljs-keyword\">new</span>\n  ~ aws_instance.web (t3.micro → t3.small)\n</code></pre>\n<p>PR shows this as a comment; reviewer sees cost/blast radius before merge. <code>plan -out=tfplan → apply tfplan</code> guarantees reviewed == executed. Objection \"CFN has change sets\" → slower, console-bound, less CI-native.</p>\n<h3 id=\"4-coding-best-practices-copy-paste-checklist\">4. Coding best practices (copy-paste checklist)</h3>\n<pre><code class=\"language-hcl\">terraform { required_<span class=\"hljs-keyword\">version</span> = <span class=\"hljs-string\">\"&gt;= 1.6, &lt; 2.0\"</span> }  <span class=\"hljs-comment\"># pin</span>\nmodule <span class=\"hljs-string\">\"vpc\"</span> { source = <span class=\"hljs-string\">\"...\"</span>, <span class=\"hljs-keyword\">version</span> = <span class=\"hljs-string\">\"5.1.0\"</span> } <span class=\"hljs-comment\"># versioned reuse</span>\n</code></pre>\n<pre><code class=\"language-bash\">terraform <span class=\"hljs-built_in\">fmt</span> -check &amp;&amp; terraform validate &amp;&amp; tflint &amp;&amp; checkov -d .  <span class=\"hljs-comment\"># gate</span>\n</code></pre>\n<p>Plus: remote state+lock, no secrets (Vault/SSM), small modules, tag all. Same bar as app code.</p>\n<h3 id=\"5-massive-community-numbers-that-matter\">5. Massive community (numbers that matter)</h3>\n<ul>\n<li>3000+ providers, 10k+ modules, every error already answered.</li>\n<li>Example: need EKS? <code>terraform-aws-modules/eks/aws</code> (versioned, tested) vs writing 400 lines CFN.</li>\n<li>Hiring: most platform/DevOps posts name Terraform explicitly.</li>\n</ul>\n<h3 id=\"6-speed-reliable-how-not-hype\">6. Speed + reliable (how, not hype)</h3>\n<ul>\n<li>Graph-parallel: 50 independent resources created concurrently, dependents ordered automatically.</li>\n<li>Idempotent: re-apply = No changes; safe retries after network blips.</li>\n<li>Enterprise: remote runs, locking, policy gates (OPA/Sentinel), drift jobs — 1 file → 500 workspaces same tool.</li>\n</ul>\n<pre><code class=\"language-bash\">terraform apply        <span class=\"hljs-comment\"># 1 added</span>\nterraform apply        <span class=\"hljs-comment\"># No changes (proof)</span>\n</code></pre>\n<h2 id=\"2-when-to-pick-skip-honest\">2. When to pick / skip (honest)</h2>\n<p>Pick: multi-cloud, module reuse, PR-driven GitOps, SaaS+cloud one-apply, job hunting.\nSkip: AWS-only Control Tower org → CFN/CDK; inside-VM config → Ansible; image baking → Packer; containers → K8s; 5-min throwaway → console.</p>\n<h2 id=\"3-vs-alternatives-30-sec-each-example\">3. vs alternatives (30-sec each + example)</h2>\n<ul>\n<li><strong>vs CFN:</strong> portability+plan vs native rollback/StackSets. Full comparison in <a href=\"/courses/terraform/level1/vs-cloudformation/\">Terraform vs CloudFormation</a>.</li>\n<li><strong>vs Pulumi:</strong> HCL + ops jobs vs real code (devs love, fewer postings).</li>\n<li><strong>vs Ansible:</strong> build infra vs setup inside. Stack them:</li>\n</ul>\n<pre><code><span class=\"hljs-title class_\">Terraform</span>: aws_instance.web ─→ <span class=\"hljs-title class_\">Ansible</span>: install nginx ─→ <span class=\"hljs-title class_\">K8s</span>: run app\n</code></pre>\n<ul>\n<li><strong>vs scripts:</strong> <code>aws ec2 run-instances</code> duplicates every run; Terraform converges.</li>\n</ul>\n<h2 id=\"4-mini-labs-prove-each-point-in-15-min\">4. Mini-labs (prove each point in 15 min)</h2>\n<ol>\n<li>Agnostic: <code>local</code> + <code>random</code> providers in one apply (no cloud).</li>\n<li>Plan: change one tag, read <code>~/−</code> diff, apply exactly via <code>-out</code>.</li>\n<li>Community: import one registry module, pin version, <code>init</code> + <code>plan</code>.</li>\n</ol>\n<h2 id=\"5-interview-q-amp-a\">5. Interview Q&amp;A</h2>\n<ul>\n<li><strong>Why Terraform?</strong> Agnostic, plan, ecosystem, GitOps, jobs.</li>\n<li><strong>Biggest cost?</strong> Self-managed state → remote+lock solves.</li>\n<li><strong>TF vs Ansible?</strong> Provision vs configure.</li>\n<li><strong>How rollback?</strong> Revert commit + apply.</li>\n<li><strong>Why declarative?</strong> Safe diffs, retries, reviews.</li>\n</ul>\n<hr />\n<p>Next: State deep-dive.</p>\n","toc":[{"id":"why-terraform-expanded","text":"Why Terraform? — Expanded","level":1},{"id":"1-your-6-points-each-idea-example-objection-answered","text":"1. Your 6 points (each: idea → example → objection answered)","level":2},{"id":"1-iac-done-right","text":"1. IaC done right","level":3},{"id":"2-platform-agnostic-with-proof","text":"2. Platform agnostic (with proof)","level":3},{"id":"3-plan-before-apply-with-sample","text":"3. Plan before apply (with sample)","level":3},{"id":"4-coding-best-practices-copy-paste-checklist","text":"4. Coding best practices (copy-paste checklist)","level":3},{"id":"5-massive-community-numbers-that-matter","text":"5. Massive community (numbers that matter)","level":3},{"id":"6-speed-reliable-how-not-hype","text":"6. Speed + reliable (how, not hype)","level":3},{"id":"2-when-to-pick-skip-honest","text":"2. When to pick / skip (honest)","level":2},{"id":"3-vs-alternatives-30-sec-each-example","text":"3. vs alternatives (30-sec each + example)","level":2},{"id":"4-mini-labs-prove-each-point-in-15-min","text":"4. Mini-labs (prove each point in 15 min)","level":2},{"id":"5-interview-q-amp-a","text":"5. Interview Q&amp;A","level":2}],"words":419,"assets":[]};