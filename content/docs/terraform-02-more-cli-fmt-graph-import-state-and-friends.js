window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["terraform-02-more-cli-fmt-graph-import-state-and-friends"]={"html":"<p>{/* MIGRATED from: D:\\NTI Courses Master Folder\\TerraformMaster Folder\\Terraform\\level1\\24-more-cli-commands.md (2026-09-06)\nCourse metadata: course=terraform | level=1 | difficulty=Beginner→Intermediate */}</p>\n<h1 id=\"more-cli-fmt-graph-import-state-and-friends\">More CLI — fmt / graph / import / state and Friends</h1>\n<p>Day 1 · the inspection and maintenance drawer</p>\n<p>Beyond the big four (see <a href=\"/courses/terraform/level1/cli-core/\">Core CLI</a>), a second tier of commands inspects, visualizes, and repairs. None of them create infrastructure — they explain it, draw it, or fix the ledger.</p>\n<h2 id=\"1-the-roster-one-line-each-then-the-two-that-need-demos\">1. The roster (one line each, then the two that need demos)</h2>\n<div class=\"table-scroll\"><table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Job</th>\n<th>When</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>terraform fmt [options] [DIR]</code></td>\n<td>Canonical style</td>\n<td>Before commit; <code>-check</code> in CI (see <a href=\"/courses/terraform/level1/fmt-destroy/\">fmt &amp; destroy</a>)</td>\n</tr>\n<tr>\n<td><code>terraform validate</code></td>\n<td>Config syntax + consistency, no cloud</td>\n<td>After every edit, before <code>plan</code></td>\n</tr>\n<tr>\n<td><code>terraform graph</code></td>\n<td>DOT-format dependency graph</td>\n<td>Visualizing order, debugging cycles</td>\n</tr>\n<tr>\n<td><code>terraform import ADDR ID</code></td>\n<td>Bind existing object to config</td>\n<td>Adopting click-ops (see <a href=\"/courses/terraform/level1/state-commands/\">State Commands</a>)</td>\n</tr>\n<tr>\n<td><code>terraform state &lt;sub&gt;</code></td>\n<td>Ledger surgery</td>\n<td>Rename/forget/inspect (see <a href=\"/courses/terraform/level1/state-commands/\">State Commands</a>)</td>\n</tr>\n<tr>\n<td><code>terraform output [NAME]</code></td>\n<td>Read exports</td>\n<td>CI, scripts, humans</td>\n</tr>\n<tr>\n<td><code>terraform show [PATH]</code></td>\n<td>Human/JSON view of state or plan</td>\n<td><code>show -json tfplan | jq</code> in CI</td>\n</tr>\n<tr>\n<td><code>terraform refresh</code></td>\n<td>Reconcile state with reality (legacy)</td>\n<td>Prefer <code>plan -refresh-only</code> today</td>\n</tr>\n<tr>\n<td><code>terraform taint ADDR</code></td>\n<td>Mark for re-creation next apply</td>\n<td>Replacing a sick instance (legacy: <code>-replace</code> flag preferred)</td>\n</tr>\n</tbody>\n</table></div>\n<h2 id=\"2-graph-see-the-order-terraform-sees\">2. graph — see the order Terraform sees</h2>\n<pre><code class=\"language-bash\">$ terraform graph | dot -Tpng &gt; graph.png\n<span class=\"hljs-comment\"># needs GraphViz installed (dot). Opens the dependency DAG as an image:</span>\n<span class=\"hljs-comment\"># aws_vpc.main → aws_subnet.public → aws_instance.web (edges = references)</span>\n</code></pre>\n<pre><code class=\"language-bash\">$ terraform graph -<span class=\"hljs-built_in\">type</span>=plan | dot -Tpng &gt; plan-graph.png\n<span class=\"hljs-comment\"># graph of the PENDING changes only — smaller, review-friendly</span>\n</code></pre>\n<p>You never write ordering (see <a href=\"/courses/terraform/level1/how-it-works/\">Core vs Plugins</a>) — the graph derives it from <code>resource.A.x</code> references. When <code>apply</code> seems to do things in a surprising sequence, draw the graph before blaming the provider.</p>\n<h2 id=\"3-the-repair-kit-with-outputs\">3. The repair kit (with outputs)</h2>\n<pre><code class=\"language-bash\">$ terraform validate\nSuccess! The configuration is valid.\n\n$ terraform show -json tfplan | jq <span class=\"hljs-string\">'.output_changes'</span>\n{ <span class=\"hljs-string\">\"vpc_id\"</span>: { <span class=\"hljs-string\">\"actions\"</span>: [<span class=\"hljs-string\">\"no-op\"</span>] } }\n\n$ terraform output -raw vpc_id\nvpc-0ab123456\n</code></pre>\n<p>📝 Modern note: <code>terraform refresh</code> as a standalone command is legacy — current Terraform folds refresh into <code>plan</code>/<code>apply</code> automatically, and <code>terraform plan -refresh-only</code> previews drift explicitly. Old tutorials still show bare <code>refresh</code>; prefer the <code>-refresh-only</code> form.</p>\n<h2 id=\"remember\">Remember</h2>\n<ul>\n<li>✓ Inspect: <code>validate</code> (syntax), <code>show</code> (state/plan), <code>output</code> (exports), <code>graph</code> (order).</li>\n<li>✓ Repair: <code>import</code> (adopt), <code>state mv/rm</code> (reshape), <code>-replace</code> (re-create sick resource).</li>\n<li>⚠ Gotcha: <code>taint</code>/<code>-replace</code> schedules DESTRUCTION + re-creation — on a stateful resource (database) that's data loss. Snapshot first, replace second.</li>\n<li>🎓 Exam takeaway: \"visualize dependencies?\" → <code>graph | dot</code>. \"Check config without cloud?\" → <code>validate</code>. \"Adopt existing?\" → <code>import</code>.</li>\n</ul>\n","toc":[{"id":"more-cli-fmt-graph-import-state-and-friends","text":"More CLI — fmt / graph / import / state and Friends","level":1},{"id":"1-the-roster-one-line-each-then-the-two-that-need-demos","text":"1. The roster (one line each, then the two that need demos)","level":2},{"id":"2-graph-see-the-order-terraform-sees","text":"2. graph — see the order Terraform sees","level":2},{"id":"3-the-repair-kit-with-outputs","text":"3. The repair kit (with outputs)","level":2},{"id":"remember","text":"Remember","level":2}],"words":362,"assets":[]};