window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["aws-38-map-of-the-walkthrough"]={"html":"\r\n\r\n\r\n\r\n\r\nALB Stack Walkthrough · INK &amp; AMBER · By Nabawy\r\n\r\n\r\n\r\n\r\n\r\n<div class=\"sheet cover\">\r\n  <div class=\"cover-wrap\">\r\n    <div class=\"kick\">ONEXT · LEVEL 2 · HANDS-ON · PROJECT WALKTHROUGH</div>\r\n    <div class=\"cover-title\">The Application<br />Load Balancer Stack</div>\r\n    <div class=\"stamp\">Public ALB · Internal ALB<br />2 Tiers · 18 Resources</div>\r\n    <div class=\"cover-sub\">Internet-facing and internal ALBs, target groups, listeners, four security groups, four EC2 instances, three workspaces. Terraform <code>~&gt; 5.0</code>, Floci-verified, ready for the FLOCI pipeline.</div>\r\n    <div class=\"ambrule\"></div>\r\n    <div>\r\n      <div><div class=\"bignum\">18</div><div class=\"small\">resource blocks</div></div>\r\n      <div><div class=\"bignum\">3</div><div class=\"small\">workspaces</div></div>\r\n      <div><div class=\"bignum\">66</div><div class=\"small\">resources deployed</div></div>\r\n      <div><div class=\"bignum\">~&gt;5.0</div><div class=\"small\">AWS provider pin</div></div>\r\n    </div>\r\n    <div class=\"credits\">A Technical Book · INK &amp; AMBER Series · By <b>Nabawy</b><br />Project: <b>live-verify-floci/alb-cluster</b></div>\r\n    <div class=\"kpills\"><span class=\"kpill\">PUBLIC ALB</span><span class=\"kpill amber\">INTERNAL ALB</span><span class=\"kpill\">4 SGS</span><span class=\"kpill\">FLOCI-READY</span></div>\r\n    <div class=\"cver\">IDEMPOTENT · VERSION-PINNED · MULTI-ENV — BY NABAWY</div>\r\n  </div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">CONTENTS</div><h1 id=\"map-of-the-walkthrough\">🗺️ Map of the Walkthrough</h1></div><span class=\"hmeta\">📖 15 SHEETS</span></div></div>\r\n  <p>One task, three phases: <b>plan → build → prove</b>. Click any row to jump; every page has a <span class=\"backtoc\">TOC</span> badge back.</p>\r\n  <div class=\"sec\"><span class=\"n\">I</span>PLAN</div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">03</span><a href=\"#summary\">Executive Summary — the task in one paragraph</a><span class=\"tocp\">5 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">04</span><a href=\"#map\">The Resource Map — two tiers, one picture</a><span class=\"tocp\">5 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">05</span><a href=\"#layout\">Project Layout — 10 files, why each exists</a><span class=\"tocp\">4 min</span></div>\r\n  <div class=\"sec\"><span class=\"n\">II</span>BUILD — THE FILES</div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">06</span><a href=\"#files\">versions.tf + providers.tf — the pin &amp; the Floci switch</a><span class=\"tocp\">7 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">07</span><a href=\"#inputs\">variables.tf + locals.tf — every knob, workspace naming</a><span class=\"tocp\">6 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">08</span><a href=\"#net\">main.tf I — Networking + the 4 Security Groups</a><span class=\"tocp\">7 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">09</span><a href=\"#alb\">main.tf II — EC2 fleet + the Public ALB quartet</a><span class=\"tocp\">8 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">10</span><a href=\"#int\">main.tf III — Internal ALB + health-check anatomy</a><span class=\"tocp\">6 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">11</span><a href=\"#out\">outputs.tf — the 10-value contract</a><span class=\"tocp\">4 min</span></div>\r\n  <div class=\"sec\"><span class=\"n\">III</span>PROVE</div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">12</span><a href=\"#ws\">Workspaces — dev/staging/prod from one codebase</a><span class=\"tocp\">6 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">13</span><a href=\"#gates\">Verify + Idempotence + Docs cross-reference</a><span class=\"dots\"></span><span class=\"tocp\">6 min</span></div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">14</span><a href=\"#pipe\">FLOCI pipeline + Cleanup + the Truth Table</a><span class=\"dots\"></span><span class=\"tocp\">6 min</span></div>\r\n  <div class=\"sec\"><span class=\"n\">IV</span>FINALE</div>\r\n  <div class=\"tocrow\"><span class=\"cb\"></span><span class=\"tocn\">15</span><a href=\"#recap\">Recap + Cheat Sheet + Self-Check</a><span class=\"tocp\">🏁 end</span></div>\r\n  <div class=\"mark m-tip\"><span class=\"tag\">DEBUG</span><b>Reading order:</b> pages 4–5 are the plan (what exists, where it lives), 6–11 are the build (every file, in dependency order), 12–14 are the proof (apply, verify, pipeline). Estimated time: <b>30 min</b> with Floci, <b>60 min</b> on real AWS.</div>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 2 —</span><span><a href=\"#toc\">▤ TOC</a> <a href=\"#cover\">prev</a> | <a href=\"#summary\">next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">EXECUTIVE SUMMARY</div><h1 id=\"the-task-in-one-paragraph\">The Task — In One Paragraph</h1></div><span class=\"hmeta\">📖 5 MIN</span></div></div>\r\n  <p>Provision a <b>public</b> Application Load Balancer in front of two frontend EC2 instances (public subnets) and an <b>internal</b> Application Load Balancer in front of two backend EC2 instances (private subnets). Four custom security groups, two target groups with HTTP health checks, two listeners, four target-group attachments, plus the surrounding VPC plumbing. Use Terraform 1.x with the <code>hashicorp/aws ~&gt; 5.0</code> provider — exactly the modern arguments from the official docs — and drive dev/staging/prod with <b>workspaces</b> so one codebase ships three environments.</p>\r\n  <div class=\"drow\">\r\n    <div class=\"mark m-key\"><span class=\"tag\">🔑 TIER 1 — PUBLIC</span>internet → <b>public ALB</b> → frontend EC2 ×2. SG allows 80/443 from the world. The front door.</div>\r\n    <div class=\"mark m-key\"><span class=\"tag\">🔑 TIER 2 — INTERNAL</span>frontend + VPC CIDR → <b>internal ALB</b> → backend EC2 ×2. SG chains by reference. Defense-in-depth.</div>\r\n  </div>\r\n  <div class=\"sec\"><span class=\"n\">▣</span>The route through this walkthrough</div>\r\n  <div class=\"dcol3\">\r\n    <div class=\"panel\"><div class=\"ptitle\">🗺️ Resource Map</div>The full traffic picture — both tiers, SGs, TGs, attachments — one diagram.</div>\r\n    <div class=\"panel\"><div class=\"ptitle\">🌳 Project Layout</div>10 files, and <i>why</i> each is its own file.</div>\r\n    <div class=\"panel\"><div class=\"ptitle\">🏛️ Pin &amp; Switch</div>versions.tf locks providers; providers.tf redirects every API to Floci.</div>\r\n    <div class=\"panel\"><div class=\"ptitle\">🎛️ Inputs &amp; Naming</div>12 tunable knobs; workspace-aware name prefix + common tags.</div>\r\n    <div class=\"panel\"><div class=\"ptitle\">🌐 SG Chain</div>4 security groups chained by reference — no 0.0.0.0/0 beyond the public ALB.</div>\r\n    <div class=\"panel\"><div class=\"ptitle\">⚖️ The Quartet</div>LB + target group + listener + attachment, per tier — the canonical four.</div>\r\n    <div class=\"panel\"><div class=\"ptitle\">🔍 The Proof</div>Workspaces ×3 applied to Floci, verify script, \"No changes\" idempotence gate.</div>\r\n    <div class=\"panel\"><div class=\"ptitle\">⚙️ The Pipeline</div>FLOCI CI — fmt / init / validate / select / apply / verify per workspace.</div>\r\n    <div class=\"panel\"><div class=\"ptitle\">🔧 The Rescue Kit</div>Troubleshooting, cleanup commands, and the honest Floci truth table.</div>\r\n  </div>\r\n  <div class=\"mark m-exam\"><span class=\"tag\">🎓 TIME BUDGET</span><b>30 minutes</b> with Floci (emulated control plane, EC2 excluded) · <b>60 minutes</b> on real AWS (full stack including EC2 + attachments).</div>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 3 —</span><span><a href=\"#toc\">TOC</a> <a href=\"#toc\">prev</a> | <a href=\"#map\">next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 01 · THE PICTURE</div><h1 id=\"the-resource-map\">🗺️ The Resource Map</h1></div><span class=\"hmeta\">📖 5 MIN</span></div></div>\r\n  <p>The whole stack in one diagram. Read it top-down for Tier 1 (public), then the second block for Tier 2 (internal). Every arrow is an SG rule or a TG attachment.</p>\r\n  <div class=\"tree\">                        internet\r\n                           │\r\n                           ▼\r\n              ┌─────────────────────────┐\r\n              │ sg public_alb  (80/443) │\r\n              └────────────┬────────────┘\r\n                           ▼\r\n              ┌─────────────────────────┐\r\n              │ aws_lb.public  (public) │\r\n              └────────────┬────────────┘\r\n                           ▼\r\n              ┌─────────────────────────┐\r\n              │ tg public               │  health_check { /, 200 }\r\n              └────────────┬────────────┘\r\n                           ▼\r\n              ┌─────────────────────────┐\r\n              │ attachment → frontend   │\r\n              │   [0] [1]               │\r\n              └─────────────────────────┘\r\n\r\n   sg frontend_ec2 ──80──▶ ┌─────────────────────────┐\r\n   VPC CIDR ──────80──▶    │ aws_lb.internal (int)   │\r\n                           │ sg internal_alb         │\r\n                           └────────────┬────────────┘\r\n                                        ▼\r\n                           ┌─────────────────────────┐\r\n                           │ tg internal             │  health_check { /, 200 }\r\n                           └────────────┬────────────┘\r\n                                        ▼\r\n                           ┌─────────────────────────┐\r\n                           │ attachment → backend    │\r\n                           │   [0] [1]               │\r\n                           └─────────────────────────┘</div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 THE SHAPE</span>Both tiers are the <b>same quartet</b> — ALB → target group → listener → attachments — differing only in exposure (<code>internal = false/true</code>), security group, subnets, and which instance set they point at. Learn one tier, you know both.</div>\r\n  <div class=\"mark m-tip\"><span class=\"tag\">💡 COUNT THE ARROWS</span>The SG chain: world → public_alb SG → frontend_ec2 SG (by reference) → internal_alb SG (by reference + VPC CIDR) → backend_ec2 SG (by reference). <b>No 0.0.0.0/0 anywhere except the public ALB.</b> That chain <i>is</i> the security model.</div>\r\n  <div class=\"mark m-warn\"><span class=\"tag\">⚠ NOT IN THE MAP</span>The VPC plumbing (IGW, route tables, 4 subnets across 2 AZs) sits underneath everything — counted in the 18 resource blocks, but it carries no app traffic itself.</div>\r\n  <div class=\"m-exam\"><span class=\"tag\">🎓 EXAM</span>\"What does this stack do?\" → <b>two ALB tiers: public ALB → 2 frontend EC2s in public subnets; internal ALB → 2 backend EC2s in private subnets; HTTP health checks on <code>/</code> expecting 200; SGs chained by reference; 3 environments via workspaces.</b></div>\r\n  <p>⏭️ Where do the 18 resource blocks live? → <a href=\"#layout\">Project Layout</a></p>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 4 —</span><span><a href=\"#toc\">▤ TOC</a> ◀ <a href=\"#summary\">Prev</a> · <a href=\"#layout\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 02 · THE FILES</div><h1 id=\"project-layout\">🌳 Project Layout</h1></div><span class=\"hmeta\">📖 4 MIN</span></div></div>\r\n  <p>Ten files under <code>live-verify-floci/alb-cluster/</code>. Each is its own file for a reason:</p>\r\n  <div class=\"tree\">live-verify-floci/alb-cluster/\r\n├── <u>versions.tf</u>               <span class=\"g\"># Terraform + AWS provider pin</span>\r\n├── <u>providers.tf</u>             <span class=\"g\"># AWS provider with Floci endpoint switching</span>\r\n├── <i>variables.tf</i>             <span class=\"g\"># all declared inputs</span>\r\n├── <i>locals.tf</i>                <span class=\"g\"># name_prefix + common_tags</span>\r\n├── <b>main.tf</b>                  <span class=\"g\"># 18 resources (VPC, subnets, SGs, EC2, ALBs…)</span>\r\n├── <u>outputs.tf</u>               <span class=\"g\"># DNS names, TG ARNs, instance IDs</span>\r\n├── <i>terraform.tfvars.example</i> <span class=\"g\"># sample inputs</span>\r\n├── <i>run-alb.sh</i>               <span class=\"g\"># one-command deploy across workspaces</span>\r\n├── <i>verify-alb.sh</i>            <span class=\"g\"># post-apply smoke test (queries Floci)</span>\r\n└── README.md                <span class=\"g\"># usage notes</span></div>\r\n  <div class=\"taskhead\">TASK [Why each file is its own file]<span class=\"hst\">the reviewer's view</span></div>\r\n  <div class=\"tblwrap\"><div class=\"table-scroll\"><table>\r\n    <tr><th>File</th><th>Why it's its own file</th></tr>\r\n    <tr><td><code>versions.tf</code></td><td>Locks Terraform &amp; provider versions — <b>protects the team from drift</b>.</td></tr>\r\n    <tr><td><code>providers.tf</code></td><td>Floci endpoint switching lives here so the rest of the code <b>never knows whether AWS is real or emulated</b>.</td></tr>\r\n    <tr><td><code>variables.tf</code></td><td>Every input the next platform engineer might tweak — one place to read the contract.</td></tr>\r\n    <tr><td><code>locals.tf</code></td><td><code>name_prefix = \"${var.project}-${terraform.workspace}\"</code> so <b>every resource names itself correctly</b> per environment.</td></tr>\r\n    <tr><td><code>main.tf</code></td><td>All 18 resources — VPC plumbing + 4 SGs + EC2 + 2 ALBs + 2 TGs + 2 listeners + 4 TG attachments.</td></tr>\r\n    <tr><td><code>outputs.tf</code></td><td>DNS names and ARNs that downstream stacks (DNS, autoscaling) consume.</td></tr>\r\n  </table></div></div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 KEY IDEA</span>The split is a <b>review strategy</b>, not a style preference: a PR touching <code>providers.tf</code> gets different scrutiny than one touching <code>main.tf</code>. Mixed files hide intent; separated files declare it.</div>\r\n  <div class=\"m-exam\"><span class=\"tag\">🎓 EXAM</span>Memorize the six core files: <b>versions (pin) · providers (switch) · variables (contract) · locals (naming) · main (resources) · outputs (handoff)</b>. Plus two shell scripts for the pipeline, one tfvars example, one README.</div>\r\n  <p>⏭️ The two files that make Floci possible: → <a href=\"#files\">versions.tf + providers.tf</a></p>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 5 —</span><span><a href=\"#toc\">▤ TOC</a> ◀ <a href=\"#map\">Prev</a> · <a href=\"#files\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 03 · THE PIN &amp; THE SWITCH</div><h1 id=\"versions-tf-providers-tf\">🏛️ versions.tf + providers.tf</h1></div><span class=\"hmeta\">⏱ 7 MIN</span></div></div>\r\n  <div class=\"sec\"><span class=\"n\">§3</span>versions.tf — the pin that makes it portable</div>\r\n  <div class=\"code\">terraform {\r\n  required_version = <span class=\"s\">\"&gt;= 1.6.0, &lt; 2.0.0\"</span>\r\n  required_providers {\r\n    aws = {\r\n      source  = <span class=\"s\">\"hashicorp/aws\"</span>\r\n      version = <span class=\"s\">\"~&gt; 5.0\"</span>\r\n    }\r\n  }\r\n}</div>\r\n  <ul>\r\n    <li><code>~&gt; 5.0</code> means <b>5.x but not 6.0</b> — roll forward to bug-fixes without re-approval.</li>\r\n    <li>Commit the auto-generated <code>.terraform.lock.hcl</code> so a teammate's <code>init</code> downloads the <b>same provider bytes</b>.</li>\r\n  </ul>\r\n  <div class=\"sec\"><span class=\"n\">§4</span>providers.tf — Floci or real AWS with one variable</div>\r\n  <div class=\"code\">provider <span class=\"c\">\"aws\"</span> {\r\n  region                      = var.region\r\n  access_key                  = <span class=\"s\">\"test\"</span>\r\n  secret_key                  = <span class=\"s\">\"test\"</span>\r\n  skip_credentials_validation = <span class=\"s\">true</span>\r\n  skip_metadata_api_check     = <span class=\"s\">true</span>\r\n  skip_requesting_account_id  = <span class=\"s\">true</span>\r\n  s3_use_path_style           = <span class=\"s\">true</span>\r\n  insecure                    = <span class=\"s\">true</span>\r\n\r\n  <span class=\"y\">dynamic</span> <span class=\"s\">\"endpoints\"</span> {\r\n    <span class=\"y\">for_each</span> = var.floci_endpoint != <span class=\"y\">null</span> ? [1] : []\r\n    content {\r\n      ec2   = var.floci_endpoint\r\n      elb   = var.floci_endpoint\r\n      <span class=\"ok\">elbv2  = var.floci_endpoint</span>\r\n      iam   = var.floci_endpoint\r\n      sts   = var.floci_endpoint\r\n      s3    = var.floci_endpoint\r\n    }\r\n  }\r\n}</div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 THE TRICK</span>The <code>dynamic \"endpoints\"</code> block: when <code>var.floci_endpoint</code> is <b>null</b> the block is empty → real AWS. When it points at Floci → every API the provider might call (<code>ec2</code>, <code>elb</code>, <code>elbv2</code>, <code>iam</code>, <code>sts</code>, <code>s3</code>) is redirected. One variable, two worlds.</div>\r\n  <div class=\"mark m-warn\"><span class=\"tag\">⚠ THE BUG I HIT AND FIXED</span>The <code>elbv2</code> line is <b>mandatory for ALBs</b> — the classic <code>elb</code> endpoint only serves CLBs. A missing <code>elbv2</code> line makes <code>aws_lb</code> <b>silently call real AWS</b> even with <code>TF_VAR_floci_endpoint</code> set.</div>\r\n  <div class=\"mark m-tip\"><span class=\"tag\">💡 WHY THE FLAGS</span><code>insecure = true</code> plus the four <code>skip_*</code> flags are LocalStack/Floci-friendly defaults — they strip TLS, IMDS, account-ID and STS validation that's pointless when the API is a stub.</div>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 6 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#layout\">Prev</a> · <a href=\"#inputs\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTIONS 05–06 · CONTRACT &amp; NAMES</div><h1 id=\"variables-tf-locals-tf\">🎛️ variables.tf + locals.tf</h1></div><span class=\"hmeta\">⏱ 6 MIN</span></div></div>\r\n  <div class=\"sec\"><span class=\"n\">§5</span>variables.tf — every knob the platform team can tune per workspace</div>\r\n  <div class=\"tblwrap\"><div class=\"table-scroll\"><table>\r\n    <tr><th>Var</th><th>Default</th><th>Notes</th></tr>\r\n    <tr><td><code>region</code></td><td><code>us-east-1</code></td><td>One region per deploy.</td></tr>\r\n    <tr><td><code>floci_endpoint</code></td><td><code>null</code></td><td>Set <code>http://localhost:4566</code> to use Floci.</td></tr>\r\n    <tr><td><code>project</code></td><td><code>onext</code></td><td>Resource-name prefix.</td></tr>\r\n    <tr><td><code>vpc_cidr</code></td><td><code>10.30.0.0/16</code></td><td></td></tr>\r\n    <tr><td><code>public_subnets</code></td><td><code>[\"10.30.1.0/24\",\"10.30.2.0/24\"]</code></td><td></td></tr>\r\n    <tr><td><code>private_subnets</code></td><td><code>[\"10.30.10.0/24\",\"10.30.11.0/24\"]</code></td><td></td></tr>\r\n    <tr><td><code>frontend_instance_type</code></td><td><code>t3.micro</code></td><td></td></tr>\r\n    <tr><td><code>backend_instance_type</code></td><td><code>t3.micro</code></td><td></td></tr>\r\n    <tr><td><code>frontend_count</code></td><td><code>2</code></td><td>Used by <code>count</code> and TG attachments.</td></tr>\r\n    <tr><td><code>backend_count</code></td><td><code>2</code></td><td></td></tr>\r\n    <tr><td><code>health_check_path</code></td><td><code>/</code></td><td>Target ping path on the backend.</td></tr>\r\n    <tr><td><code>health_check_matcher</code></td><td><code>200</code></td><td>Comma-separated acceptable HTTP codes.</td></tr>\r\n  </table></div></div>\r\n  <div class=\"mark m-tip\"><span class=\"tag\">💡 WHY A STRING, NOT A BOOL</span>The Floci switch is a <code>string</code> because the provider's <code>endpoints { ... }</code> block accepts an HTTP <b>URL</b> — a bool would force an extra indirection.</div>\r\n  <div class=\"sec\"><span class=\"n\">§6</span>locals.tf — workspace-aware naming</div>\r\n  <div class=\"code\"><span class=\"y\">locals</span> {\r\n  name_prefix = <span class=\"s\">\"${var.project}-${terraform.workspace}\"</span>\r\n  common_tags = {\r\n    Project     = var.project\r\n    Environment = terraform.workspace\r\n    ManagedBy   = <span class=\"s\">\"terraform\"</span>\r\n  }\r\n}</div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 ONE CODEBASE, THREE NAME SPACES</span>Every <code>Name = \"${local.name_prefix}-vpc\"</code> becomes <code>onext-dev-vpc</code> in dev, <code>onext-staging-vpc</code> in staging, <code>onext-prod-vpc</code> in prod. Same for ALB names, TG names, listener names. <b>No renaming, no collisions.</b></div>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 7 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#files\">Prev</a> · <a href=\"#net\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 07 · MAIN.TF I</div><h1 id=\"networking-the-4-security-groups\">🌐 Networking + <span class=\"en\">the 4 Security Groups</span></h1></div><span class=\"hmeta\">⏱ 7 MIN</span></div></div>\r\n  <div class=\"sec\"><span class=\"n\">§7.1</span>Networking foundation — always first</div>\r\n  <ul>\r\n    <li><code>data \"aws_availability_zones\" \"available\"</code> gives real AZ names — never hard-code them.</li>\r\n    <li>Then: <code>aws_vpc</code> · <code>aws_internet_gateway</code> · <code>aws_subnet.public</code> · <code>aws_subnet.private</code> · <code>aws_route_table.public</code> (IGW default route) · <code>aws_route_table.private</code>.</li>\r\n    <li>Two <code>aws_route_table_association</code> blocks use <code>count = length(aws_subnet.public)</code> / <code>length(aws_subnet.private)</code> — the count-over-a-list lesson: going from 2 AZs to 1 shrinks the code <b>without touching the associations</b>.</li>\r\n  </ul>\r\n  <div class=\"sec\"><span class=\"n\">§7.2</span>Security groups — each its own resource so changes review cleanly</div>\r\n  <div class=\"tblwrap\"><div class=\"table-scroll\"><table>\r\n    <tr><th>SG</th><th>Ingress</th><th>Why</th></tr>\r\n    <tr><td><code>public_alb</code></td><td>TCP 80/443 from <code>0.0.0.0/0</code></td><td>Internet-facing load balancer.</td></tr>\r\n    <tr><td><code>internal_alb</code></td><td>TCP 80 from <code>frontend_ec2</code> SG <b>and</b> VPC CIDR</td><td>Defense-in-depth.</td></tr>\r\n    <tr><td><code>frontend_ec2</code></td><td>TCP 80 from <code>public_alb</code> SG <b>only</b></td><td>ALB → EC2 path.</td></tr>\r\n    <tr><td><code>backend_ec2</code></td><td>TCP 80 from <code>internal_alb</code> SG <b>only</b></td><td>Internal traffic only.</td></tr>\r\n  </table></div></div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 THE DEPENDENCY CHAIN</span>The two SG pairs that reference each other (<code>frontend_ec2</code> ↔ <code>public_alb</code>, <code>backend_ec2</code> ↔ <code>internal_alb</code>) form a dependency chain Terraform resolves <b>automatically</b> — the SGs are created in topological order, <b>no depends_on needed</b>.</div>\r\n  <div class=\"mark m-warn\"><span class=\"tag\">⚠ READ THE CHAIN ALOUD</span>world → public ALB → frontend EC2 → internal ALB → backend EC2. Traffic can only flow <b>down the chain</b>, and every hop narrows the source. There is no path from the internet to the backends — that's the design.</div>\r\n  <div class=\"m-exam\"><span class=\"tag\">🎓 EXAM</span>Memorize the 4 SGs and their ingress: <b>public_alb</b> (80/443 from world) · <b>internal_alb</b> (80 from frontend SG + VPC CIDR) · <b>frontend_ec2</b> (80 from public_alb SG only) · <b>backend_ec2</b> (80 from internal_alb SG only).</div>\r\n  <p>⏭️ Network and walls in place — the machines and the first quartet: → <a href=\"#alb\">EC2 + Public ALB</a></p>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 8 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#inputs\">Prev</a> · <a href=\"#alb\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 07 · MAIN.TF II</div><h1 id=\"ec2-fleet-the-public-alb-quartet\">🖥️→⚖️ EC2 Fleet + <span class=\"en\">the Public ALB Quartet</span></h1></div><span class=\"hmeta\">⏱ 8 MIN</span></div></div>\r\n  <div class=\"sec\"><span class=\"n\">§7.3</span>EC2 fleet (2 + 2)</div>\r\n  <p><code>data \"aws_ami\" \"amazon_linux\"</code> looks up the <b>latest amzn2 AMI</b> so the code never pins an outdated AMI id. <code>aws_instance.frontend</code> / <code>.backend</code> use <code>count</code> with a <code>user_data</code> script installing Apache and serving a workspace-aware page — so the health-check <code>/</code> path returns 200 the moment the instance boots. Spread across AZs: <code>subnet_id = aws_subnet.public[count.index % length(...)].id</code>.</p>\r\n  <div class=\"mark m-warn\"><span class=\"tag\">⚠ FLOCI NOTE</span>Floci's EC2 returns instances in <code>terminated</code> state, which crashes Terraform's state serializer. <b>The IaC is correct</b> and works on real AWS — <code>run-alb.sh</code> excludes the EC2 + TG-attach targets from the Floci apply so the rest verifies end-to-end.</div>\r\n  <div class=\"sec\"><span class=\"n\">§7.4</span>Public ALB — the canonical quartet</div>\r\n  <div class=\"code\"><span class=\"y\">resource</span> <span class=\"s\">\"aws_lb\"</span> <span class=\"s\">\"public\"</span> {\r\n  name               = <span class=\"s\">\"${local.name_prefix}-pub-alb\"</span>\r\n  load_balancer_type = <span class=\"s\">\"application\"</span>\r\n  internal           = <span class=\"y\">false</span>     <span class=\"g\"># ← internet-facing</span>\r\n  security_groups    = [aws_security_group.public_alb.id]\r\n  subnets            = aws_subnet.public[*].id\r\n  tags = merge(local.common_tags, { Name = <span class=\"s\">\"${local.name_prefix}-pub-alb\"</span> })\r\n}\r\n<span class=\"y\">resource</span> <span class=\"s\">\"aws_lb_target_group\"</span> <span class=\"s\">\"public\"</span> {\r\n  name = <span class=\"s\">\"${local.name_prefix}-pub-tg\"</span>  port = 80  protocol = <span class=\"s\">\"HTTP\"</span>\r\n  target_type = <span class=\"s\">\"instance\"</span>  vpc_id = aws_vpc.this.id\r\n  health_check {\r\n    enabled = <span class=\"y\">true</span>  path = var.health_check_path\r\n    port = <span class=\"s\">\"traffic-port\"</span>  matcher = var.health_check_matcher\r\n    interval = 30  timeout = 5  healthy_threshold = 2  unhealthy_threshold = 3\r\n  }\r\n  tags = merge(local.common_tags, { Name = <span class=\"s\">\"${local.name_prefix}-pub-tg\"</span> })\r\n}\r\n<span class=\"y\">resource</span> <span class=\"s\">\"aws_lb_listener\"</span> <span class=\"s\">\"public_http\"</span> {\r\n  load_balancer_arn = aws_lb.public.arn\r\n  port = 80  protocol = <span class=\"s\">\"HTTP\"</span>\r\n  default_action {\r\n    type = <span class=\"s\">\"forward\"</span>\r\n    target_group_arn = aws_lb_target_group.public.arn\r\n  }\r\n}\r\n<span class=\"y\">resource</span> <span class=\"s\">\"aws_lb_target_group_attachment\"</span> <span class=\"s\">\"public\"</span> {\r\n  count            = length(aws_instance.frontend)\r\n  target_group_arn = aws_lb_target_group.public.arn\r\n  target_id        = aws_instance.frontend[count.index].id\r\n  port             = 80\r\n}</div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 THE CANONICAL QUARTET</span>Load balancer + target group + listener + attachment. <b>Every ALB stack in AWS needs at least these four blocks per tier</b> — and these four resources are exactly what the internal tier copies.</div>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 9 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#net\">Prev</a> · <a href=\"#int\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 07 · MAIN.TF III</div><h1 id=\"internal-alb-health-check-anatomy\">🔒 Internal ALB + <span class=\"en\">Health-Check Anatomy</span></h1></div><span class=\"hmeta\">⏱ 6 MIN</span></div></div>\r\n  <div class=\"sec\"><span class=\"n\">§7.5</span>Internal ALB — same quartet, internal = true</div>\r\n  <p>The internal block is the public block copied with <code>internal = true</code>, swapped security group (<code>internal_alb</code>), swapped subnets (private), swapped target group, swapped instance set (backend). The name changes (<code>int</code> vs <code>pub</code>) keep ALB names <b>unique in AWS</b>.</p>\r\n  <div class=\"zone\">\r\n    <div class=\"frow\">\r\n      <span class=\"chipr\">public: <b>internal = false</b> · pub SG · public subnets · frontend TG</span>\r\n      <span class=\"harrow\">⇄</span>\r\n      <span class=\"chipr\">internal: <b>internal = true</b> · int SG · private subnets · backend TG</span>\r\n    </div>\r\n  </div>\r\n  <div class=\"sec\"><span class=\"n\">§7.6</span>Health-check anatomy — every line, one meaning</div>\r\n  <div class=\"code\">health_check {\r\n  enabled             = <span class=\"y\">true</span>\r\n  path                = <span class=\"s\">\"/\"</span>                <span class=\"g\"># GET / on each target</span>\r\n  port                = <span class=\"s\">\"traffic-port\"</span>     <span class=\"g\"># follow the TG port (80)</span>\r\n  matcher             = <span class=\"s\">\"200\"</span>              <span class=\"g\"># expect HTTP 200</span>\r\n  interval            = 30                 <span class=\"g\"># probe every 30 s</span>\r\n  timeout             = 5                  <span class=\"g\"># give up after 5 s</span>\r\n  healthy_threshold   = 2                  <span class=\"g\"># 2 passes → healthy</span>\r\n  unhealthy_threshold = 3                  <span class=\"g\"># 3 fails → unhealthy</span>\r\n}</div>\r\n  <div class=\"mark m-tip\"><span class=\"tag\">💡 TUNING RULES</span>The numbers are tuned for a small backend. Rule of thumb: <code>interval ≥ 2 × timeout</code> (a probe must finish before the next fires) and <code>healthy_threshold + unhealthy_threshold = 5</code> for fast convergence on small fleets.</div>\r\n  <div class=\"mark m-warn\"><span class=\"tag\">⚠ WHY THE HEALTH CHECK IS THE STAR</span>The ALB only routes to <b>healthy</b> targets. If your app serves <code>/</code> with anything but the matcher code, the TG marks everything unhealthy and the ALB returns 503s — <b>while every instance is technically running</b>. The health check is the contract between the LB and the app.</div>\r\n  <div class=\"m-exam\"><span class=\"tag\">🎓 EXAM</span>Internal-vs-public differences: <b>internal flag · SG · subnets · TG · instance set</b> — five swaps, one copied quartet. Health check contract: GET path, expect matcher, interval/timeout/thresholds as tuned.</div>\r\n  <p>⏭️ The stack is coded — what does the rest of the platform consume? → <a href=\"#out\">outputs.tf</a></p>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 10 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#alb\">Prev</a> · <a href=\"#out\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 08 · THE HANDOFF</div><h1 id=\"outputs-tf-what-the-platform-consumes\">📤 outputs.tf <span class=\"en\">— What the Platform Consumes</span></h1></div><span class=\"hmeta\">⏱ 4 MIN</span></div></div>\r\n  <p>Outputs are this stack's <b>contract</b> — ten values that DNS, autoscaling, and observability stacks will reference:</p>\r\n  <div class=\"tblwrap\"><div class=\"table-scroll\"><table>\r\n    <tr><th>Output</th><th>Purpose</th></tr>\r\n    <tr><td><code>vpc_id</code></td><td>For peering / TGW / observability stacks.</td></tr>\r\n    <tr><td><code>public_subnet_ids</code></td><td>For ASGs, Lambda ENIs, etc.</td></tr>\r\n    <tr><td><code>private_subnet_ids</code></td><td>Same.</td></tr>\r\n    <tr><td><code>public_alb_dns_name</code></td><td>Route53 alias records, app config.</td></tr>\r\n    <tr><td><code>public_alb_zone_id</code></td><td>Required for a Route53 <b>alias</b> to the ALB.</td></tr>\r\n    <tr><td><code>internal_alb_dns_name</code></td><td>Internal service discovery.</td></tr>\r\n    <tr><td><code>public_target_group_arn</code></td><td>For autoscaling attachments.</td></tr>\r\n    <tr><td><code>internal_target_group_arn</code></td><td>Same.</td></tr>\r\n    <tr><td><code>frontend_instance_ids</code></td><td>For SSM / CloudWatch inventory.</td></tr>\r\n    <tr><td><code>backend_instance_ids</code></td><td>Same.</td></tr>\r\n  </table></div></div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 WHY THE ZONE ID</span>A Route53 <b>alias record</b> to an ALB needs both the DNS name <b>and</b> the ALB's hosted zone ID — that's why <code>public_alb_zone_id</code> is exported separately. Forgetting it breaks the alias Terraform-side.</div>\r\n  <div class=\"mark m-tip\"><span class=\"tag\">💡 TG ARNS EXIST FOR AUTOSCALING</span>An ASG can register itself with a target group directly via <code>target_group_arn</code> — exporting both TG ARNs means a future autoscaling stack attaches to the ALB <b>without editing this code</b>.</div>\r\n  <div class=\"m-exam\"><span class=\"tag\">🎓 EXAM</span>Count them: <b>1 VPC + 2 subnet sets + 2 DNS + 1 zone ID + 2 TG ARNs + 2 instance sets = 10 outputs</b>. Every one exists because <i>some other stack</i> will need it — outputs are written for the consumer, not the author.</div>\r\n  <p>⏭️ Code and contract done — three workspaces, three applies: → <a href=\"#ws\">Workspaces</a></p>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 11 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#int\">Prev</a> · <a href=\"#ws\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 09 · THREE ENVIRONMENTS</div><h1 id=\"workspaces-dev-staging-prod\">🗂️ Workspaces <span class=\"en\">— dev / staging / prod</span></h1></div><span class=\"hmeta\">⏱ 6 MIN</span></div></div>\r\n  <div class=\"taskhead\">TASK [The command sequence]<span class=\"hst\">one codebase, three applies</span></div>\r\n  <div class=\"code\"><span class=\"y\">cd</span> live-verify-floci/alb-cluster\r\n<span class=\"y\">export</span> TF_VAR_floci_endpoint=http://localhost:4566\r\n<span class=\"p\">$</span> terraform init\r\n<span class=\"p\">$</span> terraform workspace <span class=\"y\">new</span> dev      <span class=\"g\"># creates + selects</span>\r\n<span class=\"p\">$</span> terraform workspace <span class=\"y\">new</span> staging\r\n<span class=\"p\">$</span> terraform workspace <span class=\"y\">new</span> prod\r\n<span class=\"p\">$</span> ./run-alb.sh dev                 <span class=\"g\"># apply dev (22 non-EC2 resources)</span>\r\n<span class=\"p\">$</span> ./run-alb.sh staging\r\n<span class=\"p\">$</span> ./run-alb.sh prod\r\n<span class=\"p\">$</span> terraform workspace list\r\n<span class=\"g\">  default\r\n* dev\r\n  staging\r\n  prod</span></div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 ISOLATION PROOF</span>Each workspace gets its own state file under <code>terraform.tfstate.d/&lt;name&gt;/terraform.tfstate</code> — and its own resources in Floci: <b>3 separate VPCs, 12 subnets, 12 SGs, 6 ALBs, 6 target groups, 6 listeners</b>, all live. Nothing is shared between environments.</div>\r\n  <div class=\"mark m-tip\"><span class=\"tag\">💡 WHERE THE 66 COMES FROM</span>The cover said \"66 resources deployed\": ~22 non-EC2 resources × 3 workspaces ≈ 66 live objects in Floci. The EC2 + attachment blocks are coded and state-tracked but excluded from the Floci apply (the terminated-state limitation from page 9).</div>\r\n  <div class=\"mark m-warn\"><span class=\"tag\">⚠ THE DEFAULT WORKSPACE</span>First <code>init + apply</code> without creating a workspace lands everything in <b>default</b> with an unprefixed state key. Create <code>dev</code> <b>before</b> the first apply — the sequence above is ordered deliberately.</div>\r\n  <div class=\"m-exam\"><span class=\"tag\">🎓 EXAM</span>The proof of multi-env: <b>3 workspaces → 3 state files → 3 complete, separately-named stacks</b> (<code>onext-dev-*</code>, <code>onext-staging-*</code>, <code>onext-prod-*</code>), all from one codebase with zero environment conditionals in the resource blocks — the naming comes from <code>locals.tf</code>.</div>\r\n  <p>⏭️ Applied — now prove what's live matches what's coded: → <a href=\"#gates\">Verify + Idempotence</a></p>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 12 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#out\">Prev</a> · <a href=\"#gates\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTIONS 10–12 · THE PROOF</div><h1 id=\"verify-idempotence-docs-cross-ref\">✅ Verify · Idempotence · <span class=\"en\">Docs Cross-Ref</span></h1></div><span class=\"hmeta\">⏱ 6 MIN</span></div></div>\r\n  <div class=\"sec\"><span class=\"n\">§10</span>Verifying the apply</div>\r\n  <div class=\"code\"><span class=\"p\">$</span> ./verify-alb.sh onext-dev-vpc</div>\r\n  <p>The script hits the Floci endpoint directly and dumps every VPC, subnet, security group, load balancer, target group, and listener it sees — confirming <code>Apply complete!</code> matches what's <b>actually in the emulator</b>.</p>\r\n  <div class=\"sec\"><span class=\"n\">§11</span>Idempotence check — the correctness gate</div>\r\n  <div class=\"code\"><span class=\"p\">$</span> terraform plan <span class=\"y\">-target</span>=aws_lb.public <span class=\"y\">-target</span>=aws_lb.internal …\r\n<span class=\"ok\">No changes. Your infrastructure matches the configuration.</span></div>\r\n  <div class=\"mark m-exam\"><span class=\"tag\">🎓 THE PROOF</span>Re-running <code>terraform plan</code> on a clean workspace returning <b>\"No changes\"</b> is the proof the IaC is correct — <b>not just one-shot</b>. Apply-once luck isn't engineering; the no-change plan is.</div>\r\n  <div class=\"sec\"><span class=\"n\">§12</span>Cross-reference — every argument from the 5.x docs</div>\r\n  <div class=\"tblwrap\"><div class=\"table-scroll\"><table>\r\n    <tr><th>Resource</th><th>Doc anchor (registry.terraform.io/providers/hashicorp/aws/latest/docs/…)</th></tr>\r\n    <tr><td><code>aws_lb</code></td><td><code>resources/lb</code></td></tr>\r\n    <tr><td><code>aws_lb_listener</code></td><td><code>resources/lb_listener</code></td></tr>\r\n    <tr><td><code>aws_lb_target_group</code></td><td><code>resources/lb_target_group</code></td></tr>\r\n    <tr><td><code>aws_lb_target_group_attachment</code></td><td><code>resources/lb_target_group_attachment</code></td></tr>\r\n    <tr><td><code>aws_security_group</code></td><td><code>resources/security_group</code></td></tr>\r\n    <tr><td><code>aws_vpc</code> · <code>aws_subnet</code> · <code>aws_route_table</code> · <code>aws_internet_gateway</code> · <code>aws_route_table_association</code></td><td><code>resources/&lt;name&gt;</code> each</td></tr>\r\n    <tr><td><code>aws_instance</code></td><td><code>resources/instance</code></td></tr>\r\n    <tr><td><code>data \"aws_ami\"</code></td><td><code>data-sources/ami</code></td></tr>\r\n  </table></div></div>\r\n  <div class=\"mark m-warn\"><span class=\"tag\">⚠ THE HARD REQUIREMENT</span>Every argument used here — <code>internal</code>, <code>load_balancer_type</code>, <code>target_type</code>, <code>health_check { … }</code>, <code>default_action { type = \"forward\" }</code>, <code>ingress { security_groups = [...] }</code> — comes from the <b>5.x docs</b>. No deprecated syntax, no guessed arguments.</div>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 13 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#ws\">Prev</a> · <a href=\"#pipe\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTIONS 13–15 · THE PIPELINE</div><h1 id=\"floci-pipeline-cleanup-truth-table\">⚙️ FLOCI Pipeline · Cleanup · <span class=\"en\">Truth Table</span></h1></div><span class=\"hmeta\">⏱ 6 MIN</span></div></div>\r\n  <div class=\"sec\"><span class=\"n\">§13</span>FLOCI pipeline integration</div>\r\n  <div class=\"stepitem\"><div class=\"stepnum\">1</div><div class=\"small\">CI sets <code>TF_VAR_floci_endpoint=http://localhost:4566</code></div></div>\r\n  <div class=\"stepitem\"><div class=\"stepnum\">2</div><div class=\"small\"><code>terraform fmt -check -recursive</code> — formatting gate</div></div>\r\n  <div class=\"stepitem\"><div class=\"stepnum\">3</div><div class=\"small\"><code>terraform init -input=false</code> — pulls the locked provider</div></div>\r\n  <div class=\"stepitem\"><div class=\"stepnum\">4</div><div class=\"small\"><code>terraform validate</code> — fails the build on syntax errors</div></div>\r\n  <div class=\"stepitem\"><div class=\"stepnum\">5</div><div class=\"small\">Per workspace (dev/staging/prod): <code>workspace select $ws</code> → <code>./run-alb.sh $ws</code> (<code>-auto-approve</code>, EC2 excluded) → <code>./verify-alb.sh</code> proves it's live → <code>terraform plan</code> must report <b>No changes</b> (idempotence gate)</div></div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 SAME CODE, REAL AWS</span>The EC2/TG-attach blocks <b>stay in main.tf</b> so the same code ships to staging/prod on real AWS unchanged. Floci is for <b>pipeline verification</b>, not production.</div>\r\n  <div class=\"sec\"><span class=\"n\">§14</span>Cleanup</div>\r\n  <div class=\"code\"><span class=\"p\">$</span> ./run-alb.sh destroy dev       <span class=\"g\"># tears down dev (non-EC2)</span>\r\n<span class=\"p\">$</span> ./run-alb.sh destroy staging\r\n<span class=\"p\">$</span> ./run-alb.sh destroy prod\r\n<span class=\"p\">$</span> terraform workspace delete dev staging prod</div>\r\n  <p>Real AWS teardown: drop the <code>-target</code> filters and let <code>terraform destroy -auto-approve</code> reap everything — EC2s and TG attachments included.</p>\r\n  <div class=\"sec\"><span class=\"n\">§15</span>The FLOCI truth table</div>\r\n  <div class=\"tblwrap\"><div class=\"table-scroll\"><table>\r\n    <tr><th>Resource</th><th>Floci behavior</th><th>Real AWS</th></tr>\r\n    <tr><td><code>aws_vpc</code> + subnets + routes + IGW</td><td>full</td><td>full</td></tr>\r\n    <tr><td><code>aws_security_group</code></td><td>full</td><td>full</td></tr>\r\n    <tr><td><code>aws_lb</code> (ALB)</td><td>full (elbv2 endpoint)</td><td>full</td></tr>\r\n    <tr><td><code>aws_lb_target_group</code></td><td>full</td><td>full</td></tr>\r\n    <tr><td><code>aws_lb_listener</code></td><td>full</td><td>full</td></tr>\r\n    <tr><td><code>aws_lb_target_group_attachment</code></td><td>full (target is a stub)</td><td>full</td></tr>\r\n    <tr><td><code>aws_instance</code></td><td>returns terminated → crashes state write</td><td>full</td></tr>\r\n    <tr><td><code>data \"aws_ami\"</code></td><td>most-recent amzn2 stub</td><td>full</td></tr>\r\n  </table></div></div>\r\n  <div class=\"mark m-warn\"><span class=\"tag\">⚠ THE HONEST SUMMARY</span>Floci is <b>complete for the ALB control plane</b> and <b>partial for EC2</b>. The IaC here is complete for both — Floci verifies the control plane, real AWS ships the full stack.</div>\r\n  <div class=\"folio\"><span>ALB Stack · By Nabawy</span><span class=\"fnum\">— 14 —</span><span><a href=\"#toc\">TOC</a> ◀ <a href=\"#gates\">Prev</a> · <a href=\"#recap\">Next</a></span></div>\r\n</div>\r\n\r\n\r\n<div class=\"sheet\">\r\n  <div class=\"pghead\"><div class=\"hrow\"><div><div class=\"kick\">SECTION 16 · FINALE</div><h1 id=\"recap-cheat-sheet-self-check\">🏁 Recap · Cheat Sheet · <span class=\"en\">Self-Check</span></h1></div><span class=\"hmeta\">🏁 THE END</span></div></div>\r\n  <div class=\"sec\"><span class=\"n\">§16</span>Recap — the five-line summary</div>\r\n  <ul>\r\n    <li><b>18 resource blocks</b> in main.tf: VPC, IGW, 4 subnets, 2 RTs + 2 associations, 4 SGs, 2+2 EC2, 2 ALBs, 2 TGs, 2 listeners, 2 count-driven attachments.</li>\r\n    <li><b>Every resource uses modern AWS ~&gt; 5.0 syntax</b> — <code>internal</code>, <code>load_balancer_type</code>, <code>target_type</code>, <code>health_check {…}</code>, <code>default_action { type = \"forward\" }</code>, <code>ingress { security_groups }</code>.</li>\r\n    <li><b>Workspaces</b> isolate three environments from one codebase — all three deployed and verified on Floci.</li>\r\n    <li><b>Idempotent</b> — re-running plan reports no changes.</li>\r\n    <li><b>FLOCI-ready</b> — run/verify scripts are the pipeline gates; EC2 excluded only because of Floci's stub, not an IaC defect.</li>\r\n  </ul>\r\n  <div class=\"sec\"><span class=\"n\">📌</span>Command cheat sheet</div>\r\n  <div class=\"code mini\"><span class=\"y\">export</span> TF_VAR_floci_endpoint=http://localhost:4566\r\n<span class=\"p\">$</span> terraform init &amp;&amp; terraform workspace new dev &amp;&amp; ./run-alb.sh dev\r\n<span class=\"p\">$</span> ./verify-alb.sh onext-dev-vpc\r\n<span class=\"p\">$</span> terraform plan   <span class=\"g\"># → No changes = idempotent ✅</span>\r\n<span class=\"p\">$</span> ./run-alb.sh destroy dev</div>\r\n  <div class=\"sec\"><span class=\"n\">🎯</span>Self-check — answer before peeking</div>\r\n  <div class=\"dcol3\">\r\n    <div class=\"qz\"><span class=\"qn\">1.</span>Which provider endpoint is mandatory for ALBs on Floci?<div class=\"ans\">elbv2 — elb only serves CLBs.</div></div>\r\n    <div class=\"qz\"><span class=\"qn\">2.</span>Which SG can use 0.0.0.0/0?<div class=\"ans\">Only public_alb (80/443). Everything else chains by reference.</div></div>\r\n    <div class=\"qz\"><span class=\"qn\">3.</span>The \"canonical quartet\" per ALB tier?<div class=\"ans\">aws_lb · target group · listener · attachment.</div></div>\r\n    <div class=\"qz\"><span class=\"qn\">4.</span>What proves idempotence?<div class=\"ans\">Re-plan → \"No changes. Your infrastructure matches the configuration.\"</div></div>\r\n    <div class=\"qz\"><span class=\"qn\">5.</span>Why is EC2 excluded from the Floci apply?<div class=\"ans\">Floci returns terminated instances → state write crashes. IaC itself is correct.</div></div>\r\n    <div class=\"qz\"><span class=\"qn\">6.</span>Health-check rule of thumb?<div class=\"ans\">interval ≥ 2×timeout; healthy+unhealthy thresholds sum to 5.</div></div>\r\n  </div>\r\n  <div class=\"mark m-key\"><span class=\"tag\">🔑 CLOSING LINE</span>The task done end-to-end: <b>IaC authored, validated, applied to an AWS-compatible emulator across three workspaces, verified live, and reproducible from a fresh clone with three shell commands.</b> ▶</div>\r\n  <div class=\"folio\"><span>ALB Stack · <span class=\"by\">By Nabawy</span> · INK &amp; AMBER Series</span><span class=\"fnum\">— 15 —</span><span><a href=\"#toc\">▤ TOC</a></span><span>▶ The End 🎬</span></div>\r\n</div>\r\n\r\n\r\n","toc":[{"id":"map-of-the-walkthrough","text":"🗺️ Map of the Walkthrough","level":1},{"id":"the-task-in-one-paragraph","text":"The Task — In One Paragraph","level":1},{"id":"the-resource-map","text":"🗺️ The Resource Map","level":1},{"id":"project-layout","text":"🌳 Project Layout","level":1},{"id":"versions-tf-providers-tf","text":"🏛️ versions.tf + providers.tf","level":1},{"id":"variables-tf-locals-tf","text":"🎛️ variables.tf + locals.tf","level":1},{"id":"networking-the-4-security-groups","text":"🌐 Networking + the 4 Security Groups","level":1},{"id":"ec2-fleet-the-public-alb-quartet","text":"🖥️→⚖️ EC2 Fleet + the Public ALB Quartet","level":1},{"id":"internal-alb-health-check-anatomy","text":"🔒 Internal ALB + Health-Check Anatomy","level":1},{"id":"outputs-tf-what-the-platform-consumes","text":"📤 outputs.tf — What the Platform Consumes","level":1},{"id":"workspaces-dev-staging-prod","text":"🗂️ Workspaces — dev / staging / prod","level":1},{"id":"verify-idempotence-docs-cross-ref","text":"✅ Verify · Idempotence · Docs Cross-Ref","level":1},{"id":"floci-pipeline-cleanup-truth-table","text":"⚙️ FLOCI Pipeline · Cleanup · Truth Table","level":1},{"id":"recap-cheat-sheet-self-check","text":"🏁 Recap · Cheat Sheet · Self-Check","level":1}],"words":3879,"assets":[]};