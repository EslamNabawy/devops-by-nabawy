window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["ansible-01-01-foundations-why-automation"]={"html":"<h1 id=\"01-foundations-why-automation\">01 — Foundations: Why Automation</h1>\n\n\n<h2 id=\"what-the-automation-problem\">WHAT — The automation problem</h2>\n<p>Manual operation scales linearly with machines and fails quietly. Shell scripts capture <em>commands</em> but not <em>intent</em>: which machines, what state, what if state already exists, how to verify, how to retry. Configuration drifts. Knowledge lives in runbooks and heads.</p>\n<p>Ansible addresses this by declaring <strong>desired state</strong> for <strong>selected targets</strong> and converging each target toward that state, reporting whether change was needed.</p>\n<h2 id=\"why-why-ansible-exists\">WHY — Why Ansible exists</h2>\n<p>Problem it solves: repeatable, auditable, idempotent configuration and orchestration without per-host agents or custom orchestration code for simple cases.</p>\n<p>Where it fits:</p>\n<pre><code><span class=\"hljs-title class_\">Provisioning</span> (<span class=\"hljs-title class_\">Terraform</span>) → <span class=\"hljs-title class_\">Configuration</span> (<span class=\"hljs-title class_\">Ansible</span>) → <span class=\"hljs-title class_\">Orchestration</span> (<span class=\"hljs-title class_\">K8s</span>) → <span class=\"hljs-title class_\">Verification</span> (<span class=\"hljs-title class_\">Observability</span>)\n</code></pre>\n<p>Ansible owns the configuration layer: OS, packages, files, services, users, application config — on bare metal, VMs, cloud instances, network devices where supported.</p>\n<h2 id=\"where-does-it-fit-ecosystem-lane\">WHERE DOES IT FIT — Ecosystem lane</h2>\n<ul>\n<li>Script (Bash): \"run this command again\" → no convergence check</li>\n<li>Image baking (Packer): snapshot state → not day-2 config</li>\n<li>Provisioner (Terraform): creates infrastructure → hands off to Ansible for inside-OS config</li>\n<li>Orchestrator (K8s): schedules containers → Ansible can provision/configure K8s nodes themselves</li>\n</ul>\n<p>Decision tree (preview, detailed in PDF3):</p>\n<pre><code>WHAT ARE YOU AUTOMATING? → <span class=\"hljs-title class_\">Infrastructure</span> → <span class=\"hljs-title class_\">Terraform</span>/<span class=\"hljs-title class_\">OpenTofu</span>\n                        → <span class=\"hljs-title class_\">Host</span>/app config → <span class=\"hljs-title class_\">Ansible</span>\n</code></pre>\n<p>Same question, three answers — the comparison that decides the tool:</p>\n<div class=\"table-scroll\"><table>\n<thead>\n<tr>\n<th>Question</th>\n<th>Shell loop</th>\n<th>Ansible</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Which machines?</td>\n<td>Hosts hardcoded in the loop</td>\n<td>Inventory selects by group and pattern</td>\n</tr>\n<tr>\n<td>Already correct?</td>\n<td>Runs again blindly</td>\n<td>Reports <code>OK</code>, changes nothing</td>\n</tr>\n<tr>\n<td>Proof it worked?</td>\n<td>Exit code of the last host</td>\n<td>Per-host recap, every host</td>\n</tr>\n<tr>\n<td>Repeat next week?</td>\n<td>Hope nothing changed</td>\n<td>Converge again with the same file</td>\n</tr>\n</tbody>\n</table></div>\n<h2 id=\"terraform-vs-ansible-provision-vs-configure\">TERRAFORM VS ANSIBLE — Provision vs configure</h2>\n<p>Terraform <strong>provisions</strong> infrastructure (creates it: VPCs, VMs, DNS, load balancers) and tracks it in a state file. Ansible <strong>configures</strong> what already exists (OS, packages, files, services, users) and converges it on every run, keeping no state of its own.</p>\n<div class=\"table-scroll\"><table>\n<thead>\n<tr>\n<th>Aspect</th>\n<th>Terraform / OpenTofu</th>\n<th>Ansible</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Owns</td>\n<td>Infrastructure lifecycle (create, change, destroy)</td>\n<td>Inside-OS desired state (configure, converge)</td>\n</tr>\n<tr>\n<td>State</td>\n<td>State file (<code>terraform.tfstate</code>) maps config to real resources</td>\n<td>Stateless; compares desired vs current on the host each run</td>\n</tr>\n<tr>\n<td>Runs</td>\n<td><code>plan</code> previews infra diff; <code>apply</code> changes infra</td>\n<td><code>--check --diff</code> previews config diff; run converges hosts</td>\n</tr>\n<tr>\n<td>Re-run cost</td>\n<td>Re-plans against APIs; safe but slow</td>\n<td>Re-converges; <code>OK</code> where already correct, fast</td>\n</tr>\n<tr>\n<td>Secrets</td>\n<td>Provider creds + sensitive outputs</td>\n<td>Vault-encrypted vars, <code>no_log</code>, become scoping</td>\n</tr>\n</tbody>\n</table></div>\n<p>Combined workflow (the normal production shape):</p>\n<pre><code><span class=\"hljs-title class_\">Terraform</span> apply → outputs <span class=\"hljs-title class_\">IPs</span>/hostnames → generated inventory → <span class=\"hljs-title class_\">Ansible</span> configures OS/apps → services verified\n</code></pre>\n<pre><code class=\"language-bash\">terraform output -json &gt; tf-out.json   <span class=\"hljs-comment\"># ips of new VMs</span>\nansible-playbook -i inventories/prod site.yml\n</code></pre>\n<p>Handoff rule: Terraform hands <strong>addresses</strong>, Ansible takes <strong>responsibility inside the machine</strong>. Generate inventory from Terraform outputs (dynamic inventory plugin or <code>terraform output</code> templated into <code>hosts.yml</code>) — never hand-copy IPs between tools.</p>\n<p>Anti-patterns:</p>\n<ul>\n<li>Creating VPCs/VMs with Ansible modules → duplicates Terraform state, two sources of truth</li>\n<li>Configuring apps with Terraform <code>provisioner</code> blocks beyond bootstrap → provisioners run once at create time, never converge drift; hand off to Ansible after boot</li>\n<li>Storing Terraform outputs with secrets in plain files → mark <code>sensitive = true</code>, feed Ansible via Vault</li>\n</ul>\n<h2 id=\"how-it-works-high-level-loop\">HOW IT WORKS — High-level loop</h2>\n<pre><code><span class=\"hljs-keyword\">USER</span> <span class=\"hljs-title\">→ ansible-playbook</span> → CONTROL <span class=\"hljs-keyword\">NODE</span> <span class=\"hljs-title\">(inventory</span>, vars, tasks) → SSH/WinRM → MANAGED <span class=\"hljs-keyword\">NODE</span> <span class=\"hljs-title\">(module</span>) → TARGET STATE → RESULT (OK/CHANGED/FAILED)\n</code></pre>\n<h2 id=\"practical-not-yet-executable-intentional\">PRACTICAL — Not yet executable (intentional)</h2>\n<p>This chapter builds motivation. Execution begins in Ch 02–03. No commands to mis-run yet.</p>\n<h2 id=\"what-can-fail-misunderstanding-automation\">WHAT CAN FAIL — Misunderstanding automation</h2>\n<div class=\"table-scroll\"><table>\n<thead>\n<tr>\n<th>Symptom</th>\n<th>Likely Cause</th>\n<th>Prevention</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Automation reintroduces drift</td>\n<td>Scripts without idempotency</td>\n<td>Teach CHANGED vs OK early (Ch 05)</td>\n</tr>\n<tr>\n<td>Over-automation of infra as config</td>\n<td>Using Ansible to create VPCs</td>\n<td>Handoff callout: provision vs configure</td>\n</tr>\n</tbody>\n</table></div>\n<h2 id=\"real-world-connection\">REAL-WORLD CONNECTION</h2>\n<p>SRE uses Ansible to converge fleet after patch window: same playbook against staging then production, diff mode first, check mode for approval.</p>\n<h2 id=\"interview-check\">INTERVIEW CHECK</h2>\n<p><strong>Q:</strong> Why not just run shell scripts via SSH loop?</p>\n<p><strong>A:</strong> Scripts describe commands; automation must describe targets, desired state, convergence check, error handling, and audit result. Ansible's model adds selection, declaration, and verification around the transport that a loop lacks.</p>\n<h2 id=\"next-concept\">NEXT CONCEPT</h2>\n<p>We know <em>why</em> to automate. Next: <em>how</em> Ansible reaches machines — agentless architecture.</p>\n<h2 id=\"icon-markers\">Icon Markers</h2>\n<ul>\n<li>\n\n</li>\n<li>\n\n</li>\n<li>\n\n</li>\n</ul>\n","toc":[{"id":"01-foundations-why-automation","text":"01 — Foundations: Why Automation","level":1},{"id":"what-the-automation-problem","text":"WHAT — The automation problem","level":2},{"id":"why-why-ansible-exists","text":"WHY — Why Ansible exists","level":2},{"id":"where-does-it-fit-ecosystem-lane","text":"WHERE DOES IT FIT — Ecosystem lane","level":2},{"id":"terraform-vs-ansible-provision-vs-configure","text":"TERRAFORM VS ANSIBLE — Provision vs configure","level":2},{"id":"how-it-works-high-level-loop","text":"HOW IT WORKS — High-level loop","level":2},{"id":"practical-not-yet-executable-intentional","text":"PRACTICAL — Not yet executable (intentional)","level":2},{"id":"what-can-fail-misunderstanding-automation","text":"WHAT CAN FAIL — Misunderstanding automation","level":2},{"id":"real-world-connection","text":"REAL-WORLD CONNECTION","level":2},{"id":"interview-check","text":"INTERVIEW CHECK","level":2},{"id":"next-concept","text":"NEXT CONCEPT","level":2},{"id":"icon-markers","text":"Icon Markers","level":2}],"words":631,"assets":[]};