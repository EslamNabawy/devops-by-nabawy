window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["cicd-002-content-analysis-continuous-delivery-humble-farley-vs-site-c"]={"html":"<h1 id=\"content-analysis-continuous-delivery-humble-amp-farley-vs-site-content\">Content Analysis — \"Continuous Delivery\" (Humble &amp; Farley) vs. Site Content</h1>\n<p>Source: local PDF extract (<code>Continuous Delivery Reliable Software Releases Through Build, Test And Deployment Automation.pdf</code>, ~194k words, read in full via three parallel passes over Parts I–III).\nSite state at analysis time: 44 books (the prompt's \"48\" predates the four lab merges), 9 learning paths.\nAll notes below are paraphrased; no sentences from the book are reproduced.</p>\n<h2 id=\"application-status-execution-pass-2026-09-15\">Application status (execution pass, 2026-09-15)</h2>\n<ul>\n<li>Phase 1 (P0): report contained no P0 items — nothing to fix.</li>\n<li>Phase 2 (P1) applied in <code>41dbb4b</code>: data-migration section in <code>14-reliability-and-recovery</code>\n(+ interview Q), migration bullets in <code>10-deployment-strategies</code> (.md + <code>pdf-20</code> callouts for\nmigration sequencing and branch by abstraction), capacity-testing section in <code>04-testing</code>.</li>\n<li>Phase 3 (P2) applied in <code>2466c21</code>: glossary CD split; deployment-auto trunk wording softened;\ntest doubles + executable acceptance specs in <code>04-testing</code>; preflight/build-master/ratcheting in\n<code>02-continuous-integration</code> (.md + <code>pdf-21</code> callout); release checklist + injection timing in\n<code>09-environments-and-release</code>; integration-pipeline callout in <code>pdf-03</code>; walking-skeleton phrase in\nlabs path desc; ROADMAP rows updated. Build + <code>qc-check</code> ALL GREEN after each phase; 0 dead anchors.</li>\n<li>Judgment calls: full-manual books (<code>pdf-03/20/21</code>) got unnumbered <code>.kv</code> callout mirrors instead of new\nnumbered sections, so no section renumbering and no TOC churn; <code>.md</code> edits carry the full prose.</li>\n<li>Governance appendix: resolved without a new book — scoped at ~30 lines (maturity ladder, pipeline-as-evidence, change control, risk one-liner, value-stream mapping), closest fit is <code>11-security/cicd-security.md</code> (approvals, audit trails, separation of duties already live there), added as section 6. A standalone book would be one thin section; not recommended.</li>\n<li>Skipped: everything under \"Not recommended for action,\" per instructions.</li>\n</ul>\n<h2 id=\"summary\">Summary</h2>\n<p>The site holds up well against this source. Its core spine — build once and promote the same digest,\nthe deploy/release/promote vocabulary split, stop-the-line red-main discipline, trunk-plus-flags,\nexpand-contract migrations, hermetic builds with lockfiles, the test pyramid with contract tests and a\nflaky-quarantine policy, progressive delivery with kill criteria — matches the book's central arguments,\nand in several places the site is deliberately more current than the book (merge queues, SBOM/signing,\npolicy gates, error budgets, ephemeral preview environments). Where the site diverges from the book's\nstricter stances (short-lived branches with PR gates instead of trunk-only, coverage-as-signal instead of\nfixed thresholds), the site is right and the book is dated.</p>\n<p>The headline gap is data: the book devotes a full chapter to versioned database migrations, decoupled\nmigration sequencing, test-data strategy, and rollback without data loss, while the site covers only the\nexpand-contract pattern (in four places) with none of the surrounding practice. The second gap is\ncapacity/performance testing — the book treats it as a pipeline stage with its own vocabulary and\nmethod, and the site has zero coverage (no load, capacity, soak, or performance-testing content\nanywhere outside SLO definitions). Both are P1. Everything else is P2 polish: executable acceptance\nspecs, commit-stage discipline details, release-strategy checklists, config-management depth, component\npipelines/branch-by-abstraction, and a governance/maturity appendix the site may reasonably decline.</p>\n<h2 id=\"terminology-corrections\">Terminology corrections</h2>\n<ul>\n<li>Glossary <code>CD</code> entry lumps Delivery and Deployment into one line, while <code>07-continuous-delivery</code>\nteaches the distinction carefully (a human/policy decision vs. automatic). Fix: split the glossary row\ninto two entries pointing at <code>07-continuous-delivery/continuous-delivery.md</code> and\n<code>08-continuous-deployment/continuous-deployment.md</code>. P2, files: <code>GLOSSARY.md</code>.</li>\n<li><code>08-continuous-deployment</code> prerequisites say unfinished work hides behind flags and \"never branches,\"\nwhile <code>02-continuous-integration</code> allows branches that live hours-to-days. Both are defensible in\ncontext, but a reader doing both paths back-to-back meets two absolute-sounding rules. Fix: soften the\ndeployment-auto line to match the CI book (short-lived branches acceptable, long-lived ones not), or add\none sentence explaining the stricter bar applies once a service auto-ships. P2, files:\n<code>08-continuous-deployment/continuous-deployment.md</code>.</li>\n<li>No other terminology fix recommended: deploy vs. release vs. promote, artifact vs. binary, staging vs.\nproduction, smoke vs. E2E are all used the way the book uses them.</li>\n</ul>\n<h2 id=\"content-gaps-missing-concepts\">Content gaps (missing concepts)</h2>\n<ul>\n<li><strong>P1 — Database migration practice.</strong> The site teaches expand-contract (<code>10-deployment-strategies</code>,\n<code>14-reliability-and-recovery</code>, <code>08-continuous-deployment</code>, Lab 05) but not: versioned incremental\nmigration scripts with a runner, forward/reverse script discipline, dual-compatible (old-code-on-new-\nschema) rollout sequencing, verifying migrations inside the pipeline, rollback-without-data-loss\nprocedures, or test-data strategy (minimal constructed data vs. production dumps, seeded state via APIs).\nHome: extend <code>14-reliability-and-recovery/rollback-recovery.md</code> (data-safe rollback section) and\n<code>10-deployment-strategies/deployment-strategies.md</code> (migration sequencing section); if either grows past\n~60 lines, split a new <code>12.x</code>-adjacent data book instead.</li>\n<li><strong>P1 — Capacity and performance testing as a pipeline stage.</strong> The book's method (quantified NFR\nthresholds, scenario-derived load from acceptance flows, isolated perf environment, warm-up, trend\nthresholds that ratchet) has no counterpart on site; <code>04-testing/testing-strategy.md</code> stops at E2E and\n<code>13-observability-and-feedback</code> defines SLOs without saying how load behavior gets proven pre-release.\nHome: new section in <code>04-testing/testing-strategy.md</code> (placement: post-merge/staging stage, vocabulary:\nthroughput vs. capacity vs. scalability, anti-patterns: unquantified goals, shared-host measurements).</li>\n<li><strong>P2 — Executable acceptance specifications.</strong> The site has the pyramid and contract tests but nothing on\nacceptance criteria written as runnable specs, the analyst-tester-developer kickoff habit, or\nGiven-When-Then structuring. Home: short section in <code>04-testing/testing-strategy.md</code>.</li>\n<li><strong>P2 — Commit-stage discipline details.</strong> <code>02-continuous-integration</code> covers stop-the-line and trunk;\nmissing: pretested/preflight builds (validate before landing on main), a rotating build-master role for\nlarge teams, and ratcheting quality gates (fail only when warnings/duplication increase). Home: extend\nsection 3 or 4 of <code>02-continuous-integration/continuous-integration.md</code>.</li>\n<li><strong>P2 — Release strategy/plan checklists.</strong> <code>07-continuous-delivery</code> teaches the flow; the book's\nstakeholder template (owners, asset config, environments, approvals, monitoring, data, disaster recovery,\nsupport) would make a practical appendix. Home: new final section in\n<code>07-continuous-delivery/continuous-delivery.md</code> or <code>09-environments-and-release/environments-release.md</code>.</li>\n<li><strong>P2 — Configuration-management depth.</strong> <code>09-environments-and-release</code> covers config-vs-code and IaC, but\nthe book's \"version everything, inject config as late as possible, keep environments rebuildable from\nautomation\" deserves one consolidated section (injection timing: build vs. package vs. deploy vs. runtime;\nenvironment parity as a reproducibility test). Home: extend\n<code>09-environments-and-release/environments-release.md</code>.</li>\n<li><strong>P2 — Component pipelines and branch by abstraction.</strong> Feature flags exist (<code>10-deployment-strategies</code>);\nthe abstraction-branch technique for large refactors without long branches, and the component-pipeline /\nintegration-pipeline split for multi-binary systems, do not. Home: short section in\n<code>10-deployment-strategies/deployment-strategies.md</code> (abstraction) and <code>06-ci-cd-pipelines/pipelines.md</code>\n(integration pipeline).</li>\n<li><strong>P2 — Test-double taxonomy.</strong> The pyramid names layers but never distinguishes dummy/fake/stub/spy/mock\nor warns against over-mocked interaction tests. Home: 5-line addition to <code>04-testing/testing-strategy.md</code>.</li>\n<li><strong>P2 (optional, may decline) — Governance appendix.</strong> Maturity model, value-stream mapping, risk process,\ncompliance-as-automation-evidence. None exists on site; the site is a practitioner reference, not a\nmanagement guide, so declining is legitimate — but if accepted, home is a new reference book, not an\ninsertion into existing ones.</li>\n</ul>\n<h2 id=\"framing-accuracy-corrections\">Framing/accuracy corrections</h2>\n<ul>\n<li>Nothing on site currently contradicts the book in a way that needs fixing. The two spots checked most\ncarefully both resolve in the site's favor: staging-data handling (sanitized snapshots, never prod data —\nmatches the book's cautions) and blue-green database changes (site prescribes expand-contract throughout\nand never suggests the book's read-only-window maneuver, which modern practice has superseded).</li>\n<li>One wording watch-item, not a correction: <code>10-deployment-strategies</code> line 70 correctly scopes\nexpand-contract as \"necessary but not sufficient\" for stateful systems — keep that framing when the P1\ndata work lands, so the new migration content reads as completing the thought rather than contradicting it.</li>\n</ul>\n<h2 id=\"structural-sequencing-observations\">Structural/sequencing observations</h2>\n<ul>\n<li>Path order (<code>start-here</code> → <code>ci-core</code> → <code>delivery</code> → <code>jenkins-core</code> → <code>jenkins-advanced</code> → <code>platforms</code> →\n<code>labs</code>) mirrors the book's own dependency logic (version everything → CI → pipeline → delivery →\necosystem/tooling). No reorder recommended.</li>\n<li>The content gaps cluster exactly where the site's structure has no home: data has no book or path slot\n(Ch12 equivalent), capacity has no stage slot (Ch9 equivalent), governance has no appendix slot (Ch15\nequivalent). When the P1 items are scheduled, decide first whether data gets its own book (recommended if\nthe migration-practice content exceeds one section) or extends <code>14-reliability-and-recovery</code>, since that\nchoice ripples through <code>TOPIC_INDEX.md</code>, <code>ROADMAP.md</code>, <code>GLOSSARY.md</code>, <code>CONTENT_MAP.md</code>, <code>books.json</code>,\nand <code>paths.json</code> per project rules.</li>\n<li>The book's \"walking skeleton\" idea (minimal end-to-end pipeline in iteration zero, then grow) is implicitly\nhow Labs 01–04 sequence (pipeline → artifacts → deployment) but is never named; consider naming it in the\nlabs path intro when convenient, P2.</li>\n</ul>\n<h2 id=\"not-recommended-for-action\">Not recommended for action</h2>\n<ul>\n<li><strong>Trunk-only absolutism.</strong> The book permits branching in narrow exceptions; the site teaches short-lived\nbranches + PR gates + merge queues + flags. The site's synthesis is current mainstream practice — do not\nregress toward the stricter rule.</li>\n<li><strong>Fixed coverage thresholds.</strong> The book's era-typical 75–80% stance is already superseded on site\n(<code>04-testing</code> treats coverage as a smoke signal and calls out assert-free gaming). No change.</li>\n<li><strong>Absolute build-time budgets.</strong> The book's ten-minute commit-stage ideal survives on site in modernized\nform (<code>04-testing</code> placement rules: PR pipeline under ~10 minutes, slower suites post-merge). No change.</li>\n<li><strong>2010 tool references</strong> (period CI servers, build tools, config managers, middleware). The site already\nteaches the modern equivalents (Actions/runners, BuildKit, IaC, lockfiles, SBOM). Do not backfill the\nbook's tooling.</li>\n<li><strong>Manual approval gates as the default risk control.</strong> The book leans on human gates; the site teaches\npolicy gates, progressive delivery, and kill criteria. The site is current — no change.</li>\n<li><strong>VCS history chapter.</strong> Pre-Git-dominated comparisons have no action value for the site's Git-based\n<code>01-source-control</code> book.</li>\n<li><strong>Heavyweight compliance framing (ITIL/PRINCE2-era).</strong> Unless the optional governance appendix is\ncommissioned, leave regulated-industry guidance at its current level (delivery-vs-deployment choice table\n<ul>\n<li>decision guide row).</li>\n</ul>\n</li>\n<li><strong>Virtualization-skeptic capacity advice.</strong> The book's avoid-virtual-hosts rule is dated; when writing the\nP1 capacity section, base it on ephemeral production-like environments, not on the book's constraint.</li>\n</ul>\n","toc":[{"id":"content-analysis-continuous-delivery-humble-amp-farley-vs-site-content","text":"Content Analysis — \"Continuous Delivery\" (Humble &amp; Farley) vs. Site Content","level":1},{"id":"application-status-execution-pass-2026-09-15","text":"Application status (execution pass, 2026-09-15)","level":2},{"id":"summary","text":"Summary","level":2},{"id":"terminology-corrections","text":"Terminology corrections","level":2},{"id":"content-gaps-missing-concepts","text":"Content gaps (missing concepts)","level":2},{"id":"framing-accuracy-corrections","text":"Framing/accuracy corrections","level":2},{"id":"structural-sequencing-observations","text":"Structural/sequencing observations","level":2},{"id":"not-recommended-for-action","text":"Not recommended for action","level":2}],"words":1595,"assets":[]};