window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["ansible-34-ansible-pdf-project-prompt-series"]={"html":"<h1 id=\"ansible-pdf-project-prompt-series\">Ansible PDF Project — Prompt Series</h1>\n<p>Five prompts, run in order, one per stage. Don't send the next one until the current stage's output has actually been reviewed — that's the whole point of the milestone gates in the spec.</p>\n<p>Before Prompt 1: make sure <code>ANSIBLE_PDF_PROJECT_SPEC.md</code> is in the repo root (empty repo otherwise is fine — the agent creates everything else).</p>\n<hr />\n<h2 id=\"prompt-1-kickoff-milestone-1-research-content-specs\">Prompt 1 — Kickoff (Milestone 1: research → content → specs)</h2>\n<p>Use the file already created: <strong><code>ansible-pdf-kickoff-wrapper-prompt.md</code></strong>. Paste its full contents as your message to the agent. It tells the agent to read <code>ANSIBLE_PDF_PROJECT_SPEC.md</code> in full, scaffold the <code>/research /content /specs /assets</code> repo layout, run the entire research-through-specifications pipeline (spec Sections 3–26), and <strong>stop</strong> — no PDF generation yet. It also locks in Playwright/Chromium as the renderer and sets the copyright/commit discipline for the whole project.</p>\n<p><strong>Expected output:</strong> all Milestone 1 files (research maps, content Markdown for every planned book/chapter, PDF architecture spec, theme/layout/pagination specs, asset + diagram registries, <code>README.md</code>), committed, with the agent explicitly asking for your review rather than continuing.</p>\n<p><strong>Do not send Prompt 2 until you've actually read through the content Markdown and specs</strong> — this is the cheapest point in the whole project to catch a bad curriculum structure, before any PDF design work is sunk into it.</p>\n<hr />\n<h2 id=\"prompt-2-approve-milestone-1-generate-pdf-1\">Prompt 2 — Approve Milestone 1, generate PDF 1</h2>\n<p>Send once you've reviewed Milestone 1's output and are satisfied (or after requesting revisions and receiving them):</p>\n<blockquote>\n<p>Milestone 1 is approved. Proceed to Milestone 2: generate PDF 1 only, using the approved content, structure, visual system, and pagination rules from the specs. Render it via Playwright/Chromium per the kickoff instructions. After generating, run the Section 60–61 QA process (page-by-page visual inspection, technical inspection, layout audit) on PDF 1 yourself before presenting it, and report what that self-QA found even if you already fixed it. Then stop — do not generate PDFs 2–4 yet.</p>\n</blockquote>\n<p><strong>Expected output:</strong> one PDF file (PDF 1 only), committed, plus a short self-QA summary. Open and actually read PDF 1 page by page yourself — this is the design-validation artifact the whole spec is built around; feedback here shapes everything after it.</p>\n<hr />\n<h2 id=\"prompt-3-feedback-on-pdf-1-update-global-spec-milestone-3\">Prompt 3 — Feedback on PDF 1 → update global spec (Milestone 3)</h2>\n<p>Send after reviewing PDF 1. Fill in your actual feedback — be specific (page numbers, what's wrong: density, a diagram, spacing, a typography choice, a broken transition, etc.):</p>\n<blockquote>\n<p>Here's feedback on PDF 1: [your specific notes — cite page numbers where possible].</p>\n<p>Fold this into the <strong>global</strong> specification (theme, layout, typography, pagination, diagram style, icon usage — whichever of these your feedback actually touches) per the spec's Milestone 3 rule, not just patched into PDF 1 alone. Update the spec files accordingly, then re-render PDF 1 against the corrected spec and confirm the fixes actually show up in the re-rendered file. Report which spec files changed. Then stop — do not generate PDFs 2–4 yet.</p>\n</blockquote>\n<p><strong>Expected output:</strong> updated spec files (not just an edited PDF 1), a re-rendered PDF 1, and a clear statement of what changed and why. If your feedback was purely cosmetic to one page and genuinely doesn't imply a global rule change, the agent should say so explicitly rather than inventing a global change to look thorough — but don't accept \"I patched PDF 1 directly\" as a substitute for a spec update when the issue clearly is systemic (e.g. a font size or spacing rule that'll recur in every PDF).</p>\n<hr />\n<h2 id=\"prompt-4-generate-remaining-pdfs-full-series-qa-milestone-4\">Prompt 4 — Generate remaining PDFs + full-series QA (Milestone 4)</h2>\n<p>Send once you're happy with the corrected PDF 1 and updated spec:</p>\n<blockquote>\n<p>The updated design system is approved. Proceed to Milestone 4: generate the remaining PDFs (2 through however many the spec's PDF-count decision settled on) using the finalized spec — they should inherit the corrected system automatically, not be built against the old one. Once all PDFs exist, run the full-series QA from spec Section 60–63: consistent design/terminology/iconography/diagrams across all PDFs, no duplicated explanations, no missing concepts versus the dependency graph, no broken references between PDFs, correct page numbering and A4 print correctness throughout. Report the QA results, including anything fixed and anything still open.</p>\n</blockquote>\n<p><strong>Expected output:</strong> all remaining PDFs, committed, plus a full-series QA report. Read at least the parts of PDFs 2–4 that cover topics you're less familiar with — that's where an unnoticed gap or inconsistency is most likely to survive review.</p>\n<hr />\n<h2 id=\"prompt-5-final-review-and-release-decision\">Prompt 5 — Final review and release decision</h2>\n<p>Send after reviewing the complete series and its QA report:</p>\n<blockquote>\n<p>Final review: [note anything still needing a fix, or confirm it's all good].</p>\n<p>Do not publish, upload, or distribute any of the PDFs anywhere. If there are fixes needed, apply them, re-run the relevant QA checks, and report back — I'll give explicit release approval separately once everything's confirmed.</p>\n</blockquote>\n<p><strong>Expected output:</strong> either a final round of fixes + confirmation, or — if you're satisfied — your own explicit next message telling the agent it's cleared for release/distribution, whatever that means for you (uploading somewhere, sharing a link, etc.). The agent should not take initiative on distribution regardless of how clean the QA report looks.</p>\n<hr />\n<h2 id=\"if-something-goes-wrong-mid-series\">If something goes wrong mid-series</h2>\n<p>If the agent generates a PDF (or skips ahead) without you having sent the corresponding approval prompt, stop it and point back to whichever gate it skipped — don't let a skipped gate slide just because the output happens to look fine. The gates exist specifically so problems get caught before they're baked into every subsequent PDF, not after.</p>\n","toc":[{"id":"ansible-pdf-project-prompt-series","text":"Ansible PDF Project — Prompt Series","level":1},{"id":"prompt-1-kickoff-milestone-1-research-content-specs","text":"Prompt 1 — Kickoff (Milestone 1: research → content → specs)","level":2},{"id":"prompt-2-approve-milestone-1-generate-pdf-1","text":"Prompt 2 — Approve Milestone 1, generate PDF 1","level":2},{"id":"prompt-3-feedback-on-pdf-1-update-global-spec-milestone-3","text":"Prompt 3 — Feedback on PDF 1 → update global spec (Milestone 3)","level":2},{"id":"prompt-4-generate-remaining-pdfs-full-series-qa-milestone-4","text":"Prompt 4 — Generate remaining PDFs + full-series QA (Milestone 4)","level":2},{"id":"prompt-5-final-review-and-release-decision","text":"Prompt 5 — Final review and release decision","level":2},{"id":"if-something-goes-wrong-mid-series","text":"If something goes wrong mid-series","level":2}],"words":937,"assets":[]};