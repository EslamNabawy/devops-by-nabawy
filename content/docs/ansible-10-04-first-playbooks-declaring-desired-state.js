window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["ansible-10-04-first-playbooks-declaring-desired-state"]={"html":"<p>{/* MIGRATED from: D:\\NTI Courses Master Folder\\ansible content\\content\\04-first-playbooks.md\nCourse metadata: course=ansible | ansible-core 2.16+, FQCN, Python 3.11+ */}</p>\n<h1 id=\"04-first-playbooks-declaring-desired-state\">04 — First Playbooks: Declaring Desired State</h1>\n<p>{/* icon: yaml | placement: chapter-opener <em>/}\n{/</em> asset: diagram-playbook-hierarchy | placement: after-section-introduction | purpose: play → task → module <em>/}\n{/</em> icon: ansible | placement: terminal-panel */}</p>\n<h2 id=\"what-playbook-is-desired-state-document\">WHAT — Playbook is desired-state document</h2>\n<p>A playbook maps <strong>hosts</strong> to <strong>tasks</strong> that invoke <strong>modules</strong> with <strong>arguments</strong>. It is YAML, declarative: describe end state, not step-by-step shell.</p>\n<h2 id=\"why-from-ad-hoc-to-repeatable\">WHY — From ad hoc to repeatable</h2>\n<p>Ad hoc proves one module once. Playbooks repeat, order, and audit multiple tasks. They are the unit committed to Git and triggered by CI.</p>\n<div class=\"table-scroll\"><table>\n<thead>\n<tr>\n<th>Aspect</th>\n<th>Ad hoc command</th>\n<th>Playbook</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Shape</td>\n<td>One module call from the CLI</td>\n<td>Plays and tasks in a YAML file</td>\n</tr>\n<tr>\n<td>Repeat</td>\n<td>Retype the command every time</td>\n<td>Run the file again, identical result</td>\n</tr>\n<tr>\n<td>Review</td>\n<td>Shell history on one laptop</td>\n<td>Git diff plus <code>--check --diff</code></td>\n</tr>\n<tr>\n<td>Handoff</td>\n<td>Tell someone the command</td>\n<td>Commit <code>site.yml</code>; CI runs it</td>\n</tr>\n</tbody>\n</table></div>\n<h2 id=\"yaml-syntax-before-semantics\">YAML — Syntax before semantics</h2>\n<pre><code class=\"language-yaml\"><span class=\"hljs-bullet\">-</span> <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">Configure</span> <span class=\"hljs-string\">web</span> <span class=\"hljs-string\">server</span>\n  <span class=\"hljs-attr\">hosts:</span> <span class=\"hljs-string\">web</span>\n  <span class=\"hljs-attr\">become:</span> <span class=\"hljs-literal\">true</span>          <span class=\"hljs-comment\"># privilege escalation</span>\n\n  <span class=\"hljs-attr\">tasks:</span>\n    <span class=\"hljs-bullet\">-</span> <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">Install</span> <span class=\"hljs-string\">nginx</span>\n      <span class=\"hljs-attr\">ansible.builtin.package:</span>\n        <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">nginx</span>\n        <span class=\"hljs-attr\">state:</span> <span class=\"hljs-string\">present</span>\n\n    <span class=\"hljs-bullet\">-</span> <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">Ensure</span> <span class=\"hljs-string\">nginx</span> <span class=\"hljs-string\">running</span>\n      <span class=\"hljs-attr\">ansible.builtin.service:</span>\n        <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">nginx</span>\n        <span class=\"hljs-attr\">state:</span> <span class=\"hljs-string\">started</span>\n        <span class=\"hljs-attr\">enabled:</span> <span class=\"hljs-literal\">true</span>\n</code></pre>\n<p>Hierarchy:</p>\n<pre><code>PLAY  <span class=\"hljs-punctuation\">(</span>hosts: web, become: true<span class=\"hljs-punctuation\">)</span>\n  +-- tasks\n       +-- TASK <span class=\"hljs-punctuation\">(</span>name: Install nginx<span class=\"hljs-punctuation\">)</span>\n            +-- MODULE <span class=\"hljs-punctuation\">(</span>ansible.builtin.package, FQCN<span class=\"hljs-punctuation\">)</span>\n            +-- ARGUMENTS <span class=\"hljs-punctuation\">(</span>name, state<span class=\"hljs-punctuation\">)</span>\n</code></pre>\n<p>Rule: FQCN always (<code>ansible.builtin.*</code>) — short names obsolete. YAML whitespace matters; 2-space indent.</p>\n<h2 id=\"how-it-works-execution\">HOW IT WORKS — Execution</h2>\n<pre><code>PLAY (web) → TASK 1 (package) → SSH → managed node →<span class=\"hljs-built_in\"> check </span>nginx absent → install → CHANGED\n          → TASK 2 (service) →<span class=\"hljs-built_in\"> check </span>stopped → start → CHANGED\n          → RESULT per host → NEXT PLAY\n</code></pre>\n<p>Play = which hosts; Task = what module with which args; Module = implementation that converges state.</p>\n<h2 id=\"practical-run-it\">PRACTICAL — Run it</h2>\n<pre><code>COMMAND\n────────────────────────\nansible-playbook -<span class=\"hljs-selector-tag\">i</span> inventory<span class=\"hljs-selector-class\">.ini</span> site<span class=\"hljs-selector-class\">.yml</span>\n</code></pre>\n<p>Meaning: execute playbook against inventory. Target: <code>web</code> group. Internal: load <code>site.yml</code>, select hosts, for each task send module, collect <code>changed/ok/failed</code>. Expected:</p>\n<pre><code>RESULT\n────────────────────────\nPLAY [Configure web server] ***\nTASK [Install nginx] *** changed: [web-01]\nTASK [Ensure nginx running] *** changed: [web-01]\nPLAY RECAP *** web-01 : <span class=\"hljs-attribute\">ok</span>=2 <span class=\"hljs-attribute\">changed</span>=2 <span class=\"hljs-attribute\">failed</span>=0\n</code></pre>\n<p>Why it matters: proves declarative convergence; re-run should yield <code>ok=2 changed=0</code>.</p>\n<p>Common failure: YAML parse error → <code>ERROR! Syntax Error</code> at line; fix indent or missing <code>:</code>.</p>\n<h2 id=\"what-can-fail\">WHAT CAN FAIL</h2>\n<ul>\n<li><code>hosts: web</code> matches zero → <code>skipping: no hosts matched</code> → verify with <code>--list-hosts</code></li>\n<li><code>become: true</code> without sudo → <code>Failed to escalate</code> → <code>ansible_become_user</code> / sudoers</li>\n<li>Module args typo → <code>unsupported parameter</code> → check <code>ansible-doc ansible.builtin.package</code></li>\n</ul>\n<h2 id=\"real-world-connection\">REAL-WORLD CONNECTION</h2>\n<p>Team commits <code>site.yml</code> to <code>infra/</code> repo; reviewer sees <code>state: present</code> intent, not <code>apt-get install</code> imperative.</p>\n<h2 id=\"lab-observe-execute-verify\">LAB — Observe / Execute / Verify</h2>\n<p>Observe: <code>nginx</code> absent (<code>systemctl status nginx</code> fails). Execute: <code>ansible-playbook -i inventory.ini site.yml --check --diff</code> (dry run). Verify: <code>--diff</code> shows planned changes. Execute for real, verify <code>ok</code> with <code>changed=2</code>, re-run verify <code>changed=0</code>.</p>\n<h2 id=\"interview-check\">INTERVIEW CHECK</h2>\n<p><strong>Q:</strong> Play vs task vs module?</p>\n<p><strong>A:</strong> Play selects hosts and holds tasks; task invokes one module with arguments; module does work on managed node and reports convergence.</p>\n<h2 id=\"next-concept\">NEXT CONCEPT</h2>\n<p>Playbooks declare state. Next: how modules enforce idempotency and report CHANGED vs OK.</p>\n<h2 id=\"icon-markers\">Icon Markers</h2>\n<ul>\n<li>{/* icon: yaml */}</li>\n<li>{/* icon: ansible */}</li>\n<li>{/* icon: configuration */}</li>\n<li>{/* icon: terminal */}</li>\n</ul>\n","toc":[{"id":"04-first-playbooks-declaring-desired-state","text":"04 — First Playbooks: Declaring Desired State","level":1},{"id":"what-playbook-is-desired-state-document","text":"WHAT — Playbook is desired-state document","level":2},{"id":"why-from-ad-hoc-to-repeatable","text":"WHY — From ad hoc to repeatable","level":2},{"id":"yaml-syntax-before-semantics","text":"YAML — Syntax before semantics","level":2},{"id":"how-it-works-execution","text":"HOW IT WORKS — Execution","level":2},{"id":"practical-run-it","text":"PRACTICAL — Run it","level":2},{"id":"what-can-fail","text":"WHAT CAN FAIL","level":2},{"id":"real-world-connection","text":"REAL-WORLD CONNECTION","level":2},{"id":"lab-observe-execute-verify","text":"LAB — Observe / Execute / Verify","level":2},{"id":"interview-check","text":"INTERVIEW CHECK","level":2},{"id":"next-concept","text":"NEXT CONCEPT","level":2},{"id":"icon-markers","text":"Icon Markers","level":2}],"words":427,"assets":[]};