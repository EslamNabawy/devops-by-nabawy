window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["cicd-128-jenkins-labs-handbook"]={"html":"<hr />\n<p>title: Jenkins_Labs_Handbook\nstatus: complete\nmerged_from:</p>\n<ul>\n<li>jenkins labs/GitHub_Jenkins_Webhook_CICD_Trigger_Setup.md</li>\n<li>jenkins labs/Jenkins_Docker_Build_Push_DockerHub_FLOCI_Deploy.md</li>\n<li>jenkins labs/Jenkins_Multibranch_Pipeline_With_Git.md</li>\n<li>jenkins labs/Jenkins_Shared_Library_Example_with_GitHub.md</li>\n<li>jenkins labs/Terraform_Jenkins_FLOCI_CICD_Pipeline.md</li>\n</ul>\n<hr />\n<h1 id=\"jenkins-labs-handbook\">Jenkins_Labs_Handbook</h1>\n<blockquote>\n<p>Merged handbook. Sources: jenkins labs/GitHub_Jenkins_Webhook_CICD_Trigger_Setup.md, jenkins labs/Jenkins_Docker_Build_Push_DockerHub_FLOCI_Deploy.md, jenkins labs/Jenkins_Multibranch_Pipeline_With_Git.md, jenkins labs/Jenkins_Shared_Library_Example_with_GitHub.md, jenkins labs/Terraform_Jenkins_FLOCI_CICD_Pipeline.md.</p>\n</blockquote>\n<hr />\n\n<h2 id=\"part-1-task-configure-github-webhook-to-automatically-trigger-a-jenkins-ci-cd-pi\">Part 1: Task: Configure GitHub Webhook to Automatically Trigger a Jenkins CI/CD Pipeline</h2>\n<h2 id=\"objective\">Objective</h2>\n<p>Set up a Jenkins pipeline that checks out code from a (private) GitHub repository using authenticated Git access, and configure a GitHub webhook so that every <code>git push</code> to the repository automatically triggers the Jenkins pipeline build — with no manual intervention.</p>\n<h2 id=\"goal\">Goal</h2>\n<p>By the end of this task, the environment should have:</p>\n<ol>\n<li>A Jenkins pipeline job that successfully checks out a specific branch of a GitHub repo using stored credentials.</li>\n<li>A GitHub webhook pointing at the Jenkins server.</li>\n<li>Jenkins configured to react to that webhook (GitHub hook trigger for GITScm polling).</li>\n<li>Confirmation that a new commit to the repo automatically starts a new Jenkins build.</li>\n</ol>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ul>\n<li>A GitHub account with a repository to use (can be private).</li>\n<li>A running Jenkins server, reachable from GitHub (if Jenkins is local/behind NAT or in WSL, it must be reachable via a public URL, tunnel, or the GitHub webhook target must be able to reach it — see Notes at the end).</li>\n<li>Basic Jenkins job creation permissions.</li>\n</ul>\n<h2 id=\"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth\">Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything</h2>\n<p>Do this entire section <strong>before</strong> starting Step 1. Gather every dependency and piece of input data up front so the task can run start-to-finish without stalling partway through to ask for something (especially important here, since webhook delivery and reachability issues are hard to debug mid-flow).</p>\n<h3 id=\"0-1-check-tools-environment\">0.1 Check tools/environment</h3>\n<ul>\n<li>Jenkins reachable: <code>curl -s -o /dev/null -w \"%{http_code}\" http://localhost:8080</code> (or <code>systemctl status jenkins</code>, or check the relevant container/process).</li>\n<li>GitHub CLI available (optional, useful for repo/webhook checks): <code>gh --version</code>. Not required — if absent, fall back to the GitHub web UI steps and note this to the user rather than silently installing it.</li>\n<li>GitHub CLI authenticated, if present: <code>gh auth status</code>.</li>\n<li>If Jenkins is only reachable on <code>localhost</code>/inside WSL: check whether a tunneling tool is already installed and running (e.g. <code>ngrok --version</code>, or check for an active <code>ngrok</code>/reverse-proxy process) — a public URL is required for GitHub to deliver webhooks.</li>\n</ul>\n<h3 id=\"0-2-check-credentials-and-account-data\">0.2 Check credentials and account data</h3>\n<ul>\n<li>Whether a usable, non-expired GitHub Personal Access Token (PAT) with <code>repo</code> scope already exists (GitHub → <strong>Settings → Developer settings → Personal access tokens</strong>). PAT creation cannot be scripted/automated — it requires the user acting in a browser — so this must be resolved in this pre-flight step, not mid-task.</li>\n<li>Whether matching Jenkins credentials already exist (<strong>Manage Jenkins → Credentials</strong>) for this GitHub account/repo.</li>\n<li>Jenkins admin access confirmed (can reach job configuration and <strong>Manage Jenkins → Credentials</strong>).</li>\n</ul>\n<h3 id=\"0-3-check-existing-repo-config-state\">0.3 Check existing repo/config state</h3>\n<ul>\n<li>Whether a Jenkins job already exists for this pipeline (browse the Jenkins dashboard, or query the Jenkins API).</li>\n<li>Whether a webhook already exists on the target GitHub repo (<strong>Repo → Settings → Webhooks</strong>) pointing at the right Jenkins URL.</li>\n<li>The repository's actual default branch name (<code>main</code>, <code>master</code>, or other) — get this from the repo itself rather than assuming.</li>\n<li>Whether Jenkins is publicly reachable already, or needs a tunnel set up (see 0.1).</li>\n</ul>\n<h3 id=\"0-4-ask-the-user-for-anything-missing-before-proceeding\">0.4 Ask the user for anything missing — before proceeding</h3>\n<p>Compile a single list of everything genuinely missing or ambiguous after the checks above (don't re-ask about things already confirmed present), then ask it all <strong>in one batch</strong> before Step 1. Typical items:</p>\n<ul>\n<li>The target GitHub repository (name/URL) and its branch, if not already known.</li>\n<li>A GitHub Personal Access Token — if none valid exists, ask the user to generate one (Settings → Developer settings → Personal access tokens → Generate new token (classic), <code>repo</code> scope) and paste it in, since this step cannot be done non-interactively.</li>\n<li>The Jenkins base URL to use, and confirmation of how it should be exposed publicly if it's only on <code>localhost</code>/WSL (e.g. \"should I set up an ngrok tunnel, or do you already have a public URL/reverse proxy for Jenkins?\").</li>\n<li>Desired job name, if different from the example.</li>\n</ul>\n<p>Only proceed to Step 1 once every required input is either confirmed present on the machine or supplied by the user — this avoids stopping mid-setup (e.g. after creating the Jenkins job) to ask for a token or a tunnel decision.</p>\n<h2 id=\"steps\">Steps</h2>\n<h3 id=\"1-create-or-reuse-a-basic-pipeline-job-in-jenkins\">1. Create or Reuse a Basic Pipeline Job in Jenkins</h3>\n<ul>\n<li>Check whether a suitable pipeline job already exists; if so, reuse it and skip to Step 2.</li>\n<li>Otherwise, in Jenkins, click <strong>New Item</strong>, choose <strong>Pipeline</strong>, and name it (e.g. <code>github-webhook-pipeline</code>).</li>\n<li>Under the <strong>Pipeline</strong> section, choose <strong>Pipeline script</strong>, and paste in a minimal sample pipeline script (any simple placeholder script, e.g. one that just echoes a message) to confirm the job runs successfully first.</li>\n<li>Save and run the job once to confirm it completes successfully before adding GitHub integration.</li>\n</ul>\n<h3 id=\"2-get-the-github-repository-url\">2. Get the GitHub Repository URL</h3>\n<ul>\n<li>Go to the target GitHub repository.</li>\n<li>Copy the repository's clone URL (HTTPS form), e.g. <code>https://github.com/&lt;username&gt;/&lt;repo&gt;.git</code>.</li>\n</ul>\n<h3 id=\"3-confirm-the-github-personal-access-token-pat-for-authentication\">3. Confirm the GitHub Personal Access Token (PAT) for Authentication</h3>\n<ul>\n<li>This should already be resolved in Step 0.4 (either an existing valid token was found, or the user supplied one). Do not attempt to generate a token programmatically here — proceed straight to using the token already in hand.</li>\n</ul>\n<h3 id=\"4-add-or-reuse-github-credentials-in-jenkins\">4. Add or Reuse GitHub Credentials in Jenkins</h3>\n<ul>\n<li>Check <strong>Manage Jenkins → Credentials</strong> for an existing credential tied to this GitHub account/repo; if one exists and is valid, select it and skip credential creation below.</li>\n<li>Otherwise, in the Jenkins job configuration, use <strong>Pipeline Syntax</strong> (the \"Pipeline Syntax\" link/generator available in the pipeline job config page).</li>\n<li>Select the <strong>checkout: Check out from version control</strong> sample step.</li>\n<li>Set <strong>SCM</strong> to <strong>Git</strong>.</li>\n<li>Paste the GitHub repository URL into <strong>Repository URL</strong>.</li>\n<li>Under <strong>Credentials</strong>, click <strong>Add → Jenkins</strong>, and create credentials of type <strong>Username with password</strong>:\n<ul>\n<li><strong>Username</strong>: the GitHub username confirmed in Step 0.</li>\n<li><strong>Password</strong>: the Personal Access Token confirmed/supplied in Step 0.4 (NOT the actual GitHub account password).</li>\n<li><strong>ID</strong>: a descriptive ID, e.g. <code>github-token</code>.</li>\n<li>Click <strong>Add</strong> to save the credential.</li>\n</ul>\n</li>\n<li>Select the credential from the dropdown.</li>\n<li>Set the <strong>Branch to build</strong> field to the branch name confirmed in Step 0.3.</li>\n<li>Click <strong>Generate Pipeline Script</strong> to produce the equivalent <code>checkout</code> Groovy snippet.</li>\n<li>Copy the generated snippet and paste it into the pipeline script, replacing the placeholder sample code from Step 1.</li>\n<li>Save the job and run it to confirm the checkout succeeds and pulls the repository into the Jenkins workspace.</li>\n</ul>\n<h3 id=\"5-configure-or-verify-the-webhook-on-the-github-side\">5. Configure or Verify the Webhook on the GitHub Side</h3>\n<ul>\n<li>Check the repo's <strong>Settings → Webhooks</strong> for an existing webhook pointing at this Jenkins instance's <code>/github-webhook/</code> endpoint. If one exists, confirm it shows a green checkmark (successful delivery) and skip to Step 6.</li>\n<li>Otherwise, click <strong>Add webhook</strong>.</li>\n<li>Set the <strong>Payload URL</strong> to the Jenkins GitHub webhook endpoint:<pre><code>http:<span class=\"hljs-regexp\">//</span>&lt;your-jenkins-url&gt;<span class=\"hljs-regexp\">/github-webhook/</span>\n</code></pre>\n(Note the trailing slash — it matters.)</li>\n<li>Set <strong>Content type</strong> as appropriate (typically <code>application/json</code>).</li>\n<li>Leave trigger events at the default (<code>push</code> events) unless a different trigger scope is needed.</li>\n<li>Save the webhook.</li>\n<li>Confirm GitHub shows a green checkmark next to the newly created webhook, indicating a successful initial ping/delivery.</li>\n</ul>\n<h3 id=\"6-configure-or-verify-the-trigger-on-the-jenkins-side\">6. Configure or Verify the Trigger on the Jenkins Side</h3>\n<ul>\n<li>In the Jenkins job configuration, under <strong>Build Triggers</strong>, check whether <strong>GitHub hook trigger for GITScm polling</strong> is already enabled.</li>\n<li>If not, enable it and save the job configuration.</li>\n</ul>\n<h3 id=\"7-test-the-end-to-end-automatic-trigger\">7. Test the End-to-End Automatic Trigger</h3>\n<ul>\n<li>Make a small change in the GitHub repository (e.g. edit <code>index.html</code> or any tracked file).</li>\n<li>Commit and push the change to the branch configured in Step 4.</li>\n<li>Confirm in the Jenkins job's build history that a <strong>new build starts automatically</strong> shortly after the push, without manually clicking \"Build Now.\"</li>\n<li>Check the console output of the new automatic build to confirm the checkout step pulled the latest commit.</li>\n</ul>\n<h2 id=\"notes-for-the-ai-agent-executing-this-task\">Notes for the AI Agent Executing This Task</h2>\n<ul>\n<li><strong>Resolve everything in Step 0 before starting</strong> — token generation, tunnel setup decisions, and branch/repo confirmation should never come up as a surprise after work has already begun (e.g. after a job or webhook has been half-configured).</li>\n<li><strong>Never store the raw GitHub password as a Jenkins credential</strong> — always use a Personal Access Token in place of the password, as GitHub has deprecated basic password authentication for Git operations.</li>\n<li><strong>Local/WSL reachability</strong>: GitHub's webhook delivery requires it to reach the Jenkins URL over the internet. If Jenkins is running only on <code>localhost</code> inside WSL or behind a home network/NAT, a tunneling tool (e.g. <code>ngrok</code>, <code>localhost.run</code>, or a reverse proxy with a public domain) will be needed to expose Jenkins publicly so GitHub's webhook can deliver events. This decision belongs in Step 0, not after the webhook is already being configured.</li>\n<li>Double-check the branch name matches the repository's actual default branch (<code>main</code> vs <code>master</code> vs others) — a mismatch will cause checkout failures.</li>\n<li>If the webhook delivery fails, GitHub's webhook settings page shows delivery attempts and response codes under <strong>Recent Deliveries</strong> — use that for debugging connectivity/auth issues.</li>\n</ul>\n<hr />\n\n<h2 id=\"part-2-task-jenkins-ci-cd-pipeline-build-tag-and-push-a-docker-image-to-docker-h\">Part 2: Task: Jenkins CI/CD Pipeline — Build, Tag, and Push a Docker Image to Docker Hub (with Simulated AWS Deployment via FLOCI)</h2>\n<h2 id=\"objective-2\">Objective</h2>\n<p>Build a Jenkins pipeline that pulls source code from a Git repository, builds a Docker image from it, authenticates to Docker Hub using credentials stored in Jenkins, tags the image with a build-specific tag, pushes it to Docker Hub, and (per the extended version of this workflow) deploys/runs the resulting application inside a simulated AWS environment (FLOCI) rather than a real cloud target.</p>\n<h2 id=\"goal-2\">Goal</h2>\n<p>By the end of this task, the environment should have:</p>\n<ol>\n<li>A Git repository containing <code>hello.py</code>, <code>requirements.txt</code>, a <code>Dockerfile</code>, and a <code>Jenkinsfile</code>.</li>\n<li>A Jenkins pipeline that: pulls the code → builds a Docker image → logs in to Docker Hub using stored Jenkins credentials → tags the image (<code>myusername/app:build-&lt;BUILD_NUMBER&gt;</code>) → pushes it to Docker Hub.</li>\n<li>(Extended flow) The pipeline additionally runs an authentication step, an image-tagging step keyed to the Jenkins build number, and an application-deployment step that runs the app inside FLOCI's simulated AWS infrastructure.</li>\n</ol>\n<h2 id=\"architecture-overview-from-diagrams\">Architecture Overview (from diagrams)</h2>\n<h3 id=\"core-build-and-push-flow-image-2\">Core build-and-push flow (Image 2)</h3>\n<pre><code><span class=\"hljs-title class_\">Git</span> <span class=\"hljs-title class_\">Source</span> <span class=\"hljs-title class_\">Repository</span>            <span class=\"hljs-title class_\">Jenkins</span> CI/CD <span class=\"hljs-title class_\">Pipeline</span> <span class=\"hljs-title class_\">Orchestrator</span>                    <span class=\"hljs-title class_\">Docker</span> <span class=\"hljs-title class_\">Hub</span> <span class=\"hljs-title class_\">Registry</span>\n(e.g., <span class=\"hljs-title class_\">GitHub</span>)                   -----------------------------------------------------  --------------------\nhello.py                         <span class=\"hljs-title class_\">Pull</span> <span class=\"hljs-title class_\">Code</span> (git clone [repo_url])\nrequirements.txt      --git      <span class=\"hljs-title class_\">Build</span> <span class=\"hljs-title class_\">Setup</span> (tool configure: docker, build-env)\n<span class=\"hljs-title class_\">Dockerfile</span>             clone--&gt;  <span class=\"hljs-title class_\">Docker</span> <span class=\"hljs-title class_\">Build</span> (docker build -t app:local)\n<span class=\"hljs-title class_\">Jenkinsfile</span>                      <span class=\"hljs-title class_\">Login</span> &amp; <span class=\"hljs-title class_\">Tag</span> (docker login using fetched creds;\n                                              docker tag app:local myusername/app:build-<span class=\"hljs-number\">123</span>)\n                                  <span class=\"hljs-title class_\">Docker</span> <span class=\"hljs-title class_\">Push</span> (docker push myusername/app:build-<span class=\"hljs-number\">123</span>)  --upload--&gt;  <span class=\"hljs-title class_\">User</span> <span class=\"hljs-title class_\">Repositories</span>\n                                                                                                    <span class=\"hljs-title class_\">Image</span> <span class=\"hljs-title class_\">Tiers</span>\n<span class=\"hljs-title class_\">Jenkins</span> <span class=\"hljs-title class_\">Credentials</span> <span class=\"hljs-title class_\">Store</span>  --fetch <span class=\"hljs-title class_\">Docker</span> <span class=\"hljs-title class_\">Hub</span> credentials--&gt;  (feeds into <span class=\"hljs-title class_\">Login</span> &amp; <span class=\"hljs-title class_\">Tag</span> step)\n</code></pre>\n<h3 id=\"extended-flow-with-numbered-stages-and-simulated-deployment-image-3\">Extended flow with numbered stages and simulated deployment (Image 3)</h3>\n<pre><code><span class=\"hljs-title class_\">GitHub</span> <span class=\"hljs-title class_\">Repository</span> [<span class=\"hljs-title class_\">Repo</span> <span class=\"hljs-title class_\">Name</span>]      <span class=\"hljs-title class_\">Jenkins</span> CI/CD <span class=\"hljs-title class_\">Server</span>                  <span class=\"hljs-title class_\">Simulated</span> AWS <span class=\"hljs-title class_\">Environment</span> (using FLOCI)\n------------------------------    ------------------------------------  -----------------------------------------\nhello.py                          <span class=\"hljs-number\">1</span>. <span class=\"hljs-title class_\">Environment</span> <span class=\"hljs-title class_\">Setup</span> (<span class=\"hljs-title class_\">Load</span> <span class=\"hljs-title class_\">Env</span>)        FLOCI AWS <span class=\"hljs-title class_\">Infrastructure</span> <span class=\"hljs-title class_\">Simulation</span>\nrequirements.txt      --trigger   <span class=\"hljs-number\">2</span>. <span class=\"hljs-title class_\">Code</span> <span class=\"hljs-title class_\">Checkout</span> (<span class=\"hljs-title class_\">Retrieve</span> <span class=\"hljs-title class_\">Repo</span>)        <span class=\"hljs-title class_\">Provider</span>: aws -&gt; floci simulation\n<span class=\"hljs-title class_\">Dockerfile</span>             (webhook)  <span class=\"hljs-number\">3</span>. <span class=\"hljs-title class_\">Docker</span> <span class=\"hljs-title class_\">Build</span> (v${BUILD_NUMBER})\n<span class=\"hljs-title class_\">Jenkinsfile</span>           --------&gt;   <span class=\"hljs-number\">4</span>. <span class=\"hljs-title class_\">Authentication</span> (<span class=\"hljs-title class_\">Login</span> to <span class=\"hljs-title class_\">Registry</span>)\n                                   <span class=\"hljs-number\">5</span>. <span class=\"hljs-title class_\">Image</span> <span class=\"hljs-title class_\">Tagging</span> (<span class=\"hljs-title class_\">Apply</span> <span class=\"hljs-title class_\">Build</span> <span class=\"hljs-title class_\">Tag</span>)\n                                   <span class=\"hljs-number\">6</span>. <span class=\"hljs-title class_\">Image</span> <span class=\"hljs-title class_\">Push</span> (to <span class=\"hljs-title class_\">Registry</span>)     -------------------&gt;  (chip/container icon)\n                                   <span class=\"hljs-number\">7</span>. <span class=\"hljs-title class_\">Application</span> <span class=\"hljs-title class_\">Deployment</span> (<span class=\"hljs-title class_\">Run</span> <span class=\"hljs-title class_\">App</span>)                    running hello.py\n</code></pre>\n<h2 id=\"prerequisites-2\">Prerequisites</h2>\n<ul>\n<li>A Git repository (e.g. on GitHub) containing <code>hello.py</code>, <code>requirements.txt</code>, a <code>Dockerfile</code>, and a <code>Jenkinsfile</code>.</li>\n<li>A running Jenkins server with Docker available to the Jenkins agent (Docker CLI/daemon accessible from wherever the pipeline executes).</li>\n<li>A Docker Hub account to push images to.</li>\n<li>Access to FLOCI (or equivalent AWS simulation environment) if the deployment stage is in scope.</li>\n</ul>\n<h2 id=\"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth-2\">Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything</h2>\n<p>Do this entire section before Step 1, so the pipeline can be built end-to-end without stalling mid-setup.</p>\n<h3 id=\"0-1-check-tools-environment-2\">0.1 Check tools/environment</h3>\n<ul>\n<li>Jenkins reachable: <code>curl -s -o /dev/null -w \"%{http_code}\" http://localhost:8080</code> (or check the relevant service/container).</li>\n<li>Docker available on the Jenkins agent/host: <code>docker --version</code> and confirm the Jenkins user/agent can actually run <code>docker</code> commands (e.g. is in the <code>docker</code> group, or the Jenkins Docker plugin is configured).</li>\n<li>Git available: <code>git --version</code>.</li>\n<li>Confirm the Jenkins <strong>Docker Pipeline plugin</strong> (or equivalent) and <strong>Credentials Binding plugin</strong> are installed (<strong>Manage Jenkins → Plugins</strong>), since the pipeline needs to both run <code>docker</code> commands and pull credentials securely.</li>\n</ul>\n<h3 id=\"0-2-check-credentials-and-account-data-2\">0.2 Check credentials and account data</h3>\n<ul>\n<li>Whether Docker Hub credentials already exist in Jenkins (<strong>Manage Jenkins → Credentials</strong>) — look for a credential ID intended for Docker Hub login before creating a duplicate.</li>\n<li>If none exist: a Docker Hub username and a Docker Hub access token (preferred over the account password — Docker Hub supports generating scoped access tokens under <strong>Account Settings → Security → New Access Token</strong>).</li>\n<li>Whether FLOCI access details (endpoint, credentials, or simulation config) are already documented, if the deployment stage (Image 3, step 7) is in scope.</li>\n</ul>\n<h3 id=\"0-3-check-existing-repo-config-state-2\">0.3 Check existing repo/config state</h3>\n<ul>\n<li>Whether the target repo already contains <code>hello.py</code>, <code>requirements.txt</code>, <code>Dockerfile</code>, and <code>Jenkinsfile</code>, or whether these need to be created.</li>\n<li>Whether a Jenkins pipeline job already exists for this project.</li>\n<li>What Docker Hub repository name/namespace (<code>myusername/app</code> or otherwise) the images should be pushed to, and whether that Docker Hub repo already exists (Docker Hub will auto-create it on first push if the account allows, but confirm expected visibility — public vs private).</li>\n<li>Whether the GitHub webhook trigger for this repo is already configured (see the companion webhook setup task file if not).</li>\n</ul>\n<h3 id=\"0-4-ask-the-user-for-anything-missing-before-proceeding-2\">0.4 Ask the user for anything missing — before proceeding</h3>\n<p>Compile everything unresolved after the checks above into one batch, asked upfront:</p>\n<ul>\n<li>Docker Hub username and access token, if no valid Jenkins credential for Docker Hub already exists.</li>\n<li>The Docker Hub image name/namespace to push to (e.g. <code>myusername/app</code>) and desired tagging convention (the diagrams use <code>build-${BUILD_NUMBER}</code>).</li>\n<li>Whether the Application Deployment stage (running the app inside FLOCI) is in scope for this run, or whether the task should stop after the Docker Push stage.</li>\n<li>If deployment is in scope: the FLOCI provider/connection details needed to run a container/app inside the simulation.</li>\n<li>The Git repository URL/branch, if not already known.</li>\n</ul>\n<p>Only proceed to Step 1 once every required input is confirmed present or supplied by the user.</p>\n<h2 id=\"steps-2\">Steps</h2>\n<h3 id=\"1-prepare-the-repository-skip-reuse-pieces-already-present\">1. Prepare the Repository (skip/reuse pieces already present)</h3>\n<ul>\n<li>Ensure the repo contains:\n<ul>\n<li><code>hello.py</code> — the sample application.</li>\n<li><code>requirements.txt</code> — Python dependencies.</li>\n<li><code>Dockerfile</code> — build instructions for the image.</li>\n<li><code>Jenkinsfile</code> — pipeline definition (built in Step 2).</li>\n</ul>\n</li>\n<li>Commit and push any missing pieces.</li>\n</ul>\n<h3 id=\"2-add-docker-hub-credentials-to-jenkins-skip-if-already-present-per-step-0-2\">2. Add Docker Hub Credentials to Jenkins (skip if already present, per Step 0.2)</h3>\n<ul>\n<li>In Jenkins, go to <strong>Manage Jenkins → Credentials</strong>.</li>\n<li>Add a credential of type <strong>Username with password</strong>:\n<ul>\n<li><strong>Username</strong>: Docker Hub username.</li>\n<li><strong>Password</strong>: Docker Hub access token (not the account password).</li>\n<li><strong>ID</strong>: a descriptive ID, e.g. <code>dockerhub-creds</code>.</li>\n</ul>\n</li>\n</ul>\n<h3 id=\"3-write-the-jenkinsfile-declarative-pipeline\">3. Write the Jenkinsfile (Declarative Pipeline)</h3>\n<p>Structure the pipeline stages to match the numbered flow in Image 3:</p>\n<pre><code class=\"language-groovy\">pipeline {\n    agent any\n    environment {\n        IMAGE_NAME = <span class=\"hljs-string\">\"myusername/app\"</span>\n        IMAGE_TAG  = <span class=\"hljs-string\">\"build-${env.BUILD_NUMBER}\"</span>\n    }\n    stages {\n        stage(<span class=\"hljs-string\">'Environment Setup'</span>) {\n            steps {\n                echo <span class=\"hljs-string\">'Loading environment/config...'</span>\n            }\n        }\n        stage(<span class=\"hljs-string\">'Code Checkout'</span>) {\n            steps {\n                checkout scm\n            }\n        }\n        stage(<span class=\"hljs-string\">'Docker Build'</span>) {\n            steps {\n                sh <span class=\"hljs-string\">\"docker build -t ${IMAGE_NAME}:local .\"</span>\n            }\n        }\n        stage(<span class=\"hljs-string\">'Authentication'</span>) {\n            steps {\n                withCredentials([usernamePassword(<span class=\"hljs-attr\">credentialsId:</span> <span class=\"hljs-string\">'dockerhub-creds'</span>,\n                                                   <span class=\"hljs-symbol\">usernameVariable:</span> <span class=\"hljs-string\">'DOCKER_USER'</span>,\n                                                   <span class=\"hljs-symbol\">passwordVariable:</span> <span class=\"hljs-string\">'DOCKER_PASS'</span>)]) {\n                    sh <span class=\"hljs-string\">'echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin'</span>\n                }\n            }\n        }\n        stage(<span class=\"hljs-string\">'Image Tagging'</span>) {\n            steps {\n                sh <span class=\"hljs-string\">\"docker tag ${IMAGE_NAME}:local ${IMAGE_NAME}:${IMAGE_TAG}\"</span>\n            }\n        }\n        stage(<span class=\"hljs-string\">'Image Push'</span>) {\n            steps {\n                sh <span class=\"hljs-string\">\"docker push ${IMAGE_NAME}:${IMAGE_TAG}\"</span>\n            }\n        }\n        stage(<span class=\"hljs-string\">'Application Deployment'</span>) {\n            when {\n                expression { <span class=\"hljs-keyword\">return</span> params.DEPLOY_TO_FLOCI == <span class=\"hljs-literal\">true</span> }\n            }\n            steps {\n                <span class=\"hljs-comment\">// Deployment command(s) depend on FLOCI's specific interface —</span>\n                <span class=\"hljs-comment\">// fill in per the details gathered in Step 0.4.</span>\n                echo <span class=\"hljs-string\">\"Deploying ${IMAGE_NAME}:${IMAGE_TAG} into FLOCI simulation...\"</span>\n            }\n        }\n    }\n}\n</code></pre>\n<ul>\n<li>Adjust <code>IMAGE_NAME</code> to the namespace confirmed in Step 0.4.</li>\n<li>The <code>Application Deployment</code> stage is conditional/optional — only include and run it if Step 0.4 confirmed it's in scope.</li>\n</ul>\n<h3 id=\"4-configure-the-pipeline-job-in-jenkins\">4. Configure the Pipeline Job in Jenkins</h3>\n<ul>\n<li>Create (or reuse, per Step 0.3) a Jenkins Pipeline job pointing at the Git repo, using \"Pipeline script from SCM\" so it reads the <code>Jenkinsfile</code> directly from the repo.</li>\n<li>Set up the GitHub webhook trigger (see the companion webhook setup task file for full webhook steps) so pushes to the repo trigger this pipeline automatically.</li>\n</ul>\n<h3 id=\"5-run-and-validate-end-to-end\">5. Run and Validate End-to-End</h3>\n<ul>\n<li>Trigger the pipeline (via webhook push or manually).</li>\n<li>Confirm each stage completes: Environment Setup → Code Checkout → Docker Build → Authentication → Image Tagging → Image Push.</li>\n<li>Confirm on Docker Hub that the new image tag (<code>myusername/app:build-&lt;N&gt;</code>) appears under the expected repository.</li>\n<li>If deployment is in scope, confirm FLOCI reflects the running/deployed application.</li>\n<li>Push a second commit and confirm the pipeline runs again automatically with an incremented <code>BUILD_NUMBER</code> tag.</li>\n</ul>\n<h2 id=\"notes-for-the-ai-agent-executing-this-task-2\">Notes for the AI Agent Executing This Task</h2>\n<ul>\n<li><strong>Never hardcode the Docker Hub password/token directly in the Jenkinsfile</strong> — always reference it via the Jenkins credentials store (<code>withCredentials</code>), as shown above.</li>\n<li>Prefer a Docker Hub <strong>access token</strong> over the account password for the stored credential — tokens can be scoped and revoked independently of the account login.</li>\n<li>If the Jenkins agent runs Docker-in-Docker or via a mounted socket, confirm the specific setup already in use on this machine (<code>docker info</code>) rather than assuming a particular Docker execution model.</li>\n<li>The <strong>Application Deployment</strong> stage is FLOCI-specific and not a standard Docker/AWS deployment target — get the actual FLOCI interface/command needed from the user in Step 0.4 rather than guessing at deployment syntax.</li>\n<li>If this task is combined with the companion GitHub webhook setup task or the Terraform/FLOCI provisioning task, do Step 0 pre-flight checks jointly rather than duplicating them across files.</li>\n</ul>\n<hr />\n\n<h2 id=\"part-3-task-set-up-a-jenkins-multibranch-pipeline-using-git\">Part 3: Task: Set Up a Jenkins Multibranch Pipeline Using Git</h2>\n<h2 id=\"objective-3\">Objective</h2>\n<p>Configure a Jenkins <strong>Multibranch Pipeline</strong> job that automatically discovers branches in a Git repository and builds each one using a <code>Jenkinsfile</code> committed to that branch — eliminating the need to manually create a separate Jenkins pipeline job per branch. The repository will use a fixed three-branch model — <strong><code>main</code></strong>, <strong><code>dev</code></strong>, and <strong><code>stg</code></strong> — all built by the same job, with per-branch pipeline behavior demonstrated via <code>when</code> conditionals.</p>\n<h2 id=\"goal-3\">Goal</h2>\n<p>By the end of this task, the environment should have:</p>\n<ol>\n<li>A Jenkins <strong>Multibranch Pipeline</strong> job connected to a Git repository (plain Git branch source — not GitHub-specific integration).</li>\n<li>Automatic branch scanning that creates a sub-job (folder item) for each matching branch, driven by a <code>Jenkinsfile</code> present in that branch.</li>\n<li>A repository with exactly three branches — <strong><code>main</code></strong>, <strong><code>dev</code></strong>, and <strong><code>stg</code></strong> — all included/built by the Multibranch Pipeline job (no exclusion filter needed since all three are wanted).</li>\n<li>A <code>Jenkinsfile</code> with a conditional (<code>when</code>) stage that only runs on the <code>stg</code> branch (or another branch-specific variation, per your convention), demonstrating per-branch pipeline behavior from a single shared file.</li>\n<li>Confirmation that deleting a branch upstream causes Jenkins to remove (\"orphan\") the corresponding job after a rescan.</li>\n</ol>\n<h2 id=\"key-concept\">Key Concept</h2>\n<p>A Multibranch Pipeline job is, structurally, <strong>a folder of individual pipeline jobs</strong> — one per discovered branch. Jenkins scans the repository, finds branches matching the configured filter, and for each one looks for a <code>Jenkinsfile</code> (by default) to build with. Branches without a <code>Jenkinsfile</code> are skipped/reported as such in the scan log. Individual branch jobs (e.g. <code>main</code>, <code>dev</code>, <code>stg</code>) cannot be configured directly — only the parent Multibranch Pipeline job's configuration controls behavior for all branches.</p>\n<h2 id=\"prerequisites-3\">Prerequisites</h2>\n<ul>\n<li>A running Jenkins server with the <strong>Git</strong> and <strong>Multibranch Pipeline</strong> (Pipeline: Multibranch) plugins installed.</li>\n<li>A Git repository (can be hosted anywhere reachable via a plain <code>https://</code> or <code>git://</code> clone URL — this task deliberately uses the plain Git branch source, not the GitHub-specific integration, even if the repo happens to be hosted on GitHub).</li>\n<li>Local Git command-line access to the repository, with push permissions.</li>\n<li>No webhook/inbound connectivity is assumed in this task (e.g. an air-gapped or firewalled Jenkins) — branch discovery is done via periodic/manual scans instead of webhook-triggered scans. If webhooks are available in this environment, that can be layered on separately (see the companion GitHub webhook setup task file).</li>\n</ul>\n<h2 id=\"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth-3\">Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything</h2>\n<p>Do this entire section before Step 1, so the job can be built end-to-end without stalling mid-setup.</p>\n<h3 id=\"0-1-check-tools-environment-3\">0.1 Check tools/environment</h3>\n<ul>\n<li>Jenkins reachable: <code>curl -s -o /dev/null -w \"%{http_code}\" http://localhost:8080</code> (or check the relevant service/container).</li>\n<li>Git available: <code>git --version</code>.</li>\n<li>Confirm the <strong>Git plugin</strong> and <strong>Pipeline: Multibranch</strong> plugin (and <strong>Pipeline: Basic Steps</strong>) are installed in Jenkins (<strong>Manage Jenkins → Plugins</strong>).</li>\n<li>Confirm whether Jenkins has outbound-only or fully air-gapped network access — this determines whether periodic scanning (rather than webhook-triggered scanning) is the right approach for this task.</li>\n</ul>\n<h3 id=\"0-2-check-credentials-and-account-data-3\">0.2 Check credentials and account data</h3>\n<ul>\n<li>Whether the target Git repository is public or private.\n<ul>\n<li>If public: no Jenkins credentials are needed for the branch source.</li>\n<li>If private: check <strong>Manage Jenkins → Credentials</strong> for an existing usable Git credential (SSH key or username/token) before creating a new one.</li>\n</ul>\n</li>\n<li>Confirm local Git push access is already authenticated (e.g. <code>git ls-remote &lt;repo-url&gt;</code> succeeds, or an SSH key / stored credential helper is already configured) — this is needed to push new branches and Jenkinsfile changes during the task.</li>\n</ul>\n<h3 id=\"0-3-check-existing-repo-config-state-3\">0.3 Check existing repo/config state</h3>\n<ul>\n<li>Whether the target repository already has <code>main</code>, <code>dev</code>, and <code>stg</code> branches, or whether any of them still need to be created.</li>\n<li>Whether a <code>Jenkinsfile</code> already exists on any of those branches.</li>\n<li>Whether a Multibranch Pipeline job with a similar name already exists in Jenkins.</li>\n<li>What the repository's default branch is named (<code>main</code>, <code>master</code>, etc.) — confirm rather than assume.</li>\n</ul>\n<h3 id=\"0-4-ask-the-user-for-anything-missing-before-proceeding-3\">0.4 Ask the user for anything missing — before proceeding</h3>\n<p>Compile everything unresolved after the checks above into one batch, asked upfront:</p>\n<ul>\n<li>The Git repository URL to use, and whether it's public or private.</li>\n<li>If private: the credential (SSH key, or username + token/password) to store in Jenkins for this branch source.</li>\n<li>The desired name for the Multibranch Pipeline job (the original tutorial names it identically to the repository name for clarity).</li>\n<li>Confirmation that the branch set should be exactly <code>main</code>, <code>dev</code>, <code>stg</code> (create any that don't already exist).</li>\n<li>What per-branch behavior is wanted in the <code>Jenkinsfile</code> (e.g. should <code>stg</code> run extra verification steps, should <code>dev</code> behave differently from <code>main</code>, etc.) — this task defaults to a single extra stage that only runs on <code>stg</code>, but adjust if a different convention is wanted.</li>\n<li>Confirmation of which \"Additional Behaviors\" to enable under the Git branch source (this task defaults to: <strong>Check out to matching local branch</strong>, <strong>Clean before checkout</strong>, <strong>Clean after checkout</strong>) — adjust if different behavior is wanted.</li>\n</ul>\n<p>Only proceed to Step 1 once every required input is confirmed present or supplied by the user.</p>\n<h2 id=\"steps-3\">Steps</h2>\n<h3 id=\"1-create-the-multibranch-pipeline-job-in-jenkins-skip-reuse-if-one-already-exist\">1. Create the Multibranch Pipeline Job in Jenkins (skip/reuse if one already exists, per Step 0.3)</h3>\n<ul>\n<li>In Jenkins, click <strong>New Item</strong>.</li>\n<li>Name the job to match the repository name (for clarity of mapping between repo and job).</li>\n<li>Select <strong>Multibranch Pipeline</strong>, then click <strong>OK</strong>.</li>\n</ul>\n<h3 id=\"2-add-a-git-branch-source\">2. Add a Git Branch Source</h3>\n<ul>\n<li>Under <strong>Branch Sources</strong>, click <strong>Add source → Git</strong> (not the GitHub-specific source — this task uses the generic Git source even if the repo happens to be hosted on GitHub).</li>\n<li><strong>Project Repository</strong>: paste the repository's clone URL (HTTPS, or SSH if using key-based auth).</li>\n<li><strong>Credentials</strong>: leave as <strong>None</strong> if the repo is public; otherwise select/add the credential confirmed in Step 0.4.</li>\n<li>Leave other branch source defaults as-is for the initial setup.</li>\n</ul>\n<h3 id=\"3-configure-branch-filtering-include-all-three-target-branches\">3. Configure Branch Filtering (Include All Three Target Branches)</h3>\n<ul>\n<li>Still in the job configuration, under the Git branch source's <strong>Behaviors</strong>, add <strong>Filter by name (with wildcards)</strong>.</li>\n<li>Set:\n<ul>\n<li><strong>Include</strong>: <code>main dev stg</code> (space-separated exact names — no wildcard needed since all three are known, fixed branch names)</li>\n<li><strong>Exclude</strong>: (leave blank — anything not in the include list is automatically excluded)</li>\n</ul>\n</li>\n<li>Add these additional behaviors:\n<ul>\n<li><strong>Check out to matching local branch</strong> — ensures the build checks out the actual branch HEAD rather than a detached commit.</li>\n<li><strong>Clean before checkout</strong></li>\n<li><strong>Clean after checkout</strong></li>\n</ul>\n</li>\n<li>Leave <strong>Build strategies</strong> and other sections at their defaults unless a specific need arises.</li>\n</ul>\n<h3 id=\"4-save-and-observe-the-initial-scan\">4. Save and Observe the Initial Scan</h3>\n<ul>\n<li>Click <strong>Save</strong>. This triggers an automatic scan of the repository.</li>\n<li>Check the scan log (<strong>Scan Multibranch Pipeline Log</strong> in the job's left nav): it will report which branches were found and whether a <code>Jenkinsfile</code> was present on each.</li>\n<li>At this point, if no <code>Jenkinsfile</code> exists yet on any branch, the job folder will show as empty (\"Jenkinsfile not found\").</li>\n</ul>\n<h3 id=\"5-add-a-jenkinsfile-to-the-repository\">5. Add a Jenkinsfile to the Repository</h3>\n<ul>\n<li>Locally, on the default branch, create a <code>Jenkinsfile</code> with a minimal pipeline, e.g.:</li>\n</ul>\n<pre><code class=\"language-groovy\">pipeline {\n    agent { label <span class=\"hljs-string\">'linux'</span> }\n    options {\n        buildDiscarder(logRotator(<span class=\"hljs-attr\">numToKeepStr:</span> <span class=\"hljs-string\">'5'</span>))\n        disableConcurrentBuilds()\n    }\n    stages {\n        stage(<span class=\"hljs-string\">'Hello'</span>) {\n            steps {\n                echo <span class=\"hljs-string\">'hello'</span>\n            }\n        }\n    }\n}\n</code></pre>\n<ul>\n<li>Commit and push the <code>Jenkinsfile</code> to the default branch.</li>\n</ul>\n<h3 id=\"6-trigger-a-rescan-no-webhooks-in-this-setup\">6. Trigger a Rescan (No Webhooks in This Setup)</h3>\n<ul>\n<li>Since this task assumes no inbound webhook connectivity, Jenkins will not automatically know about the new commit.</li>\n<li>Manually trigger a scan: go to the Multibranch Pipeline job and click <strong>Scan Multibranch Pipeline Now</strong>.</li>\n<li>Confirm in the scan log that the default branch now shows <strong>Jenkinsfile found</strong>, and that a build was automatically triggered for it.</li>\n<li>Optionally, under the job's periodic scan configuration, set a scan interval (e.g. every few minutes) so this happens automatically without manual triggering, appropriate for air-gapped/no-webhook environments.</li>\n</ul>\n<h3 id=\"7-create-the-dev-and-stg-branches\">7. Create the <code>dev</code> and <code>stg</code> Branches</h3>\n<ul>\n<li>Locally, create the two additional branches from the default branch (skip creating any that already exist, per Step 0.3):\n<ul>\n<li><code>dev</code></li>\n<li><code>stg</code></li>\n</ul>\n</li>\n<li>Push both branches to the remote repository.</li>\n<li>Trigger another manual scan (or wait for the periodic scan).</li>\n<li>Confirm the scan log reports all three branches (<code>main</code>, <code>dev</code>, <code>stg</code>) were found and each shows <strong>Jenkinsfile found</strong> (since they were branched from a commit that already has the <code>Jenkinsfile</code>), and that all three now appear as buildable jobs in the Multibranch Pipeline folder.</li>\n</ul>\n<h3 id=\"8-add-branch-specific-pipeline-behavior\">8. Add Branch-Specific Pipeline Behavior</h3>\n<ul>\n<li>On the <code>stg</code> branch, modify the <code>Jenkinsfile</code> to add a stage that only runs on <code>stg</code>, e.g.:</li>\n</ul>\n<pre><code class=\"language-groovy\">stage(<span class=\"hljs-string\">'Staging Verification (stg only)'</span>) {\n    when {\n        expression { <span class=\"hljs-keyword\">return</span> env.BRANCH_NAME == <span class=\"hljs-string\">'stg'</span> }\n    }\n    steps {\n        sh <span class=\"hljs-string\">'cat README.md'</span>\n    }\n}\n</code></pre>\n<ul>\n<li>Also make an unrelated content change (e.g. edit <code>README.md</code>) to have something visible to confirm per-branch checkout behavior.</li>\n<li>Commit both changes on <code>stg</code> and push.</li>\n<li>Trigger a scan/build for <code>stg</code> and confirm:\n<ul>\n<li>The build checks out the <code>stg</code> branch specifically (visible in the build log as a branch checkout, not a detached commit checkout).</li>\n<li>The new \"Staging Verification\" stage runs and prints the branch's <code>README.md</code> content.</li>\n</ul>\n</li>\n<li>Run the <code>main</code> and <code>dev</code> branch jobs again and confirm the \"Staging Verification\" stage does <strong>not</strong> run on either (the <code>when</code> condition correctly restricts it to <code>stg</code> only).</li>\n</ul>\n<h3 id=\"9-merge-changes-back-to-main-and-observe-orphan-cleanup-optional\">9. Merge Changes Back to Main and Observe Orphan Cleanup (Optional)</h3>\n<ul>\n<li>Locally, check out the default branch, merge in the changes from <code>stg</code> (Jenkinsfile + README changes) if that fits your workflow — this repo does not use pull requests in this workflow, matching a lightweight GitHub-flow-like approach.</li>\n<li>Push the updated default branch.</li>\n<li>If at any point a branch (e.g. an older test branch) is deleted both locally and on the remote, trigger a rescan and confirm the scan log reports it as an <strong>orphaned item</strong> and removes its job from the Multibranch Pipeline folder — while <code>main</code>, <code>dev</code>, and <code>stg</code> remain intact as the three tracked branches.</li>\n<li>Run the <code>main</code> branch job once more and confirm:\n<ul>\n<li>It now includes the merged-in \"Staging Verification\" stage definition, but the stage is <strong>skipped</strong> (shown as skipped due to the <code>when</code> conditional, since <code>main</code> isn't <code>stg</code>).</li>\n</ul>\n</li>\n</ul>\n<h2 id=\"notes-for-the-ai-agent-executing-this-task-3\">Notes for the AI Agent Executing This Task</h2>\n<ul>\n<li><strong>This task intentionally avoids GitHub-specific Jenkins integration</strong> — use the plain <strong>Git</strong> branch source, even though the example repository may be hosted on GitHub. A separate, GitHub-specific version of this workflow (with webhook-driven scanning) is a distinct follow-on task, not this one.</li>\n<li><strong>No webhook is configured here on purpose</strong> — this setup simulates an air-gapped/no-inbound-access Jenkins environment, so branch discovery relies on manual or periodic scans rather than push-triggered scans. Don't add a webhook as part of this task unless the user explicitly asks for it.</li>\n<li>Individual branch jobs inside a Multibranch Pipeline folder (e.g. <code>main</code>, <code>dev</code>, <code>stg</code>) are <strong>not directly configurable</strong> — all behavior (branch filtering, checkout behavior, scan triggers) is controlled from the parent Multibranch Pipeline job's configuration only.</li>\n<li>The <code>env.BRANCH_NAME</code> variable is what allows a single shared <code>Jenkinsfile</code> to behave differently per branch via <code>when</code> conditionals — this is the core mechanism that avoids needing a separate Jenkinsfile per branch.</li>\n<li>Orphaned branch cleanup only happens <strong>after a rescan</strong> — deleting a remote branch does not immediately remove its Jenkins job; the next scan (manual, periodic, or webhook-triggered) is what evaluates and removes orphaned items.</li>\n<li>If this task is combined with the companion GitHub webhook setup or Docker/Terraform pipeline tasks, do Step 0 pre-flight checks jointly rather than duplicating them across files.</li>\n</ul>\n<hr />\n\n<h2 id=\"part-4-task-set-up-a-jenkins-shared-library-linked-to-a-github-repository\">Part 4: Task: Set Up a Jenkins Shared Library Linked to a GitHub Repository</h2>\n<h2 id=\"objective-4\">Objective</h2>\n<p>Configure Jenkins to use a <strong>Global Shared Library</strong> hosted on GitHub, so that reusable Groovy logic (e.g. a UAT/approval check) can be called from both scripted and declarative Jenkins pipelines — without embedding raw Groovy logic directly inside every pipeline definition.</p>\n<h2 id=\"goal-4\">Goal</h2>\n<p>By the end of this task, the environment should have:</p>\n<ol>\n<li>A Jenkins instance with a configured Global Pipeline Library pointing at a GitHub repo.</li>\n<li>A test pipeline (both a scripted-style and a declarative-style version) that imports a class from that shared library and uses it to conditionally run build steps based on whether an <code>approved.txt</code> file exists in a <code>tools/</code> folder.</li>\n</ol>\n<h2 id=\"prerequisites-4\">Prerequisites</h2>\n<ul>\n<li>Jenkins reachable (e.g. <code>http://localhost:8080</code>), running on the local machine or inside WSL.</li>\n<li>A GitHub account and a GitHub repository to act as the shared library (e.g. named <code>jenkins-shared-library</code>).</li>\n<li>Git available locally / in WSL.</li>\n<li>Admin access to Jenkins (Manage Jenkins permissions).</li>\n</ul>\n<h2 id=\"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth-4\">Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything</h2>\n<p>Do this entire section <strong>before</strong> starting Step 1. The goal is to gather every dependency and piece of input data up front, so the task can run start-to-finish without stopping partway through to ask for something.</p>\n<h3 id=\"0-1-check-tools-environment-4\">0.1 Check tools/environment</h3>\n<ul>\n<li>Jenkins reachable: <code>curl -s -o /dev/null -w \"%{http_code}\" http://localhost:8080</code> (or check the relevant port/service, e.g. <code>systemctl status jenkins</code>, or check if a Jenkins process/container is active).</li>\n<li>Git available: <code>git --version</code>.</li>\n<li>GitHub CLI available (optional but useful for repo checks/creation): <code>gh --version</code>. If missing, it is NOT required — fall back to manual GitHub web steps, but note this to the user rather than silently installing it.</li>\n<li>GitHub CLI authenticated, if <code>gh</code> is present: <code>gh auth status</code>.</li>\n</ul>\n<h3 id=\"0-2-check-credentials-and-account-data-4\">0.2 Check credentials and account data</h3>\n<ul>\n<li>GitHub account username to use for the shared library repo.</li>\n<li>Whether Jenkins needs to reach the GitHub repo privately (private repo) or the repo is public. If private, a Jenkins credential (GitHub username + Personal Access Token) will be needed — check <strong>Manage Jenkins → Credentials</strong> for an existing usable one before assuming none exists.</li>\n<li>Jenkins admin login/access confirmed (can reach <strong>Manage Jenkins → Configure System</strong>).</li>\n</ul>\n<h3 id=\"0-3-check-existing-repo-config-state-4\">0.3 Check existing repo/config state</h3>\n<ul>\n<li>Whether a <code>jenkins-shared-library</code> (or similarly named) GitHub repo already exists under the user's account (<code>gh repo list</code> if available, or <code>git ls-remote &lt;candidate-url&gt;</code>).</li>\n<li>Whether a Global Pipeline Library is already registered in Jenkins (<strong>Manage Jenkins → Configure System → Global Pipeline Libraries</strong>).</li>\n<li>Whether a pipeline job with a similar name already exists in Jenkins.</li>\n</ul>\n<h3 id=\"0-4-ask-the-user-for-anything-missing-before-proceeding-4\">0.4 Ask the user for anything missing — before proceeding</h3>\n<p>Compile a single list of everything that is genuinely missing or ambiguous after the checks above (do not ask about things already confirmed present). Typical items that may need to be asked in one batch, upfront:</p>\n<ul>\n<li>The GitHub username/account to use, if not inferable from <code>gh auth status</code> or existing config.</li>\n<li>Whether the shared library repo should be created new, or an existing repo name/URL should be reused.</li>\n<li>Whether the repo will be public or private (this determines whether a Jenkins credential/PAT is needed at all).</li>\n<li>If private: a GitHub Personal Access Token, OR explicit permission to generate one interactively (token generation itself requires the user in a browser — see Notes).</li>\n<li>Jenkins base URL, if not <code>http://localhost:8080</code>.</li>\n<li>Desired names for: the shared library (Jenkins), the pipeline job(s), and the Groovy package/class if different from the example (<code>com.&lt;yourname&gt;.UatInput</code>).</li>\n</ul>\n<p>Ask all of this <strong>at once</strong>, before Step 1, so the run isn't interrupted midway. Only proceed to Step 1 once every required input is either confirmed present on the machine or supplied by the user.</p>\n<h2 id=\"repository-structure-required-for-the-shared-library\">Repository Structure Required for the Shared Library</h2>\n<p>The shared library repo must follow Jenkins' expected layout:</p>\n<pre><code>(root)\n └── src<span class=\"hljs-symbol\">/</span>\n      └── com<span class=\"hljs-symbol\">/</span>\n           └── <span class=\"hljs-symbol\">&lt;yourname&gt;</span><span class=\"hljs-symbol\">/</span>\n                └── UatInput.groovy\n</code></pre>\n<p>Example <code>UatInput.groovy</code> class logic:</p>\n<ul>\n<li>Define a class (e.g. <code>UatInput</code>) with a method <code>buildIsUatApproved()</code>.</li>\n<li>That method should check whether a file named <code>tools/approved.txt</code> exists in the workspace.\n<ul>\n<li>If it exists → return <code>true</code>.</li>\n<li>If it doesn't exist (or is named/considered \"not approved\") → return <code>false</code>.</li>\n</ul>\n</li>\n</ul>\n<h2 id=\"steps-4\">Steps</h2>\n<h3 id=\"1-create-the-shared-library-repository-on-github-skip-if-it-already-exists\">1. Create the Shared Library Repository on GitHub (skip if it already exists)</h3>\n<ul>\n<li>If a suitable repository doesn't already exist, create a new GitHub repository (e.g. <code>jenkins-shared-library</code>).</li>\n<li>Check whether <code>src/com/&lt;yourname&gt;/UatInput.groovy</code> already exists in that repo; if not, add it with the class described above.</li>\n<li>Commit and push to the <code>master</code> (or <code>main</code>) branch.</li>\n<li>Copy the repository's clone URL — it will be needed in Jenkins configuration.</li>\n</ul>\n<h3 id=\"2-register-the-shared-library-in-jenkins-skip-if-already-registered\">2. Register the Shared Library in Jenkins (skip if already registered)</h3>\n<ul>\n<li>In Jenkins, go to <strong>Manage Jenkins → Configure System</strong>.</li>\n<li>Scroll down to the <strong>Global Pipeline Libraries</strong> section.</li>\n<li>If a library already points at this repo with a usable name, note its name and skip to Step 3.</li>\n<li>Otherwise, click <strong>Add</strong>.</li>\n<li>Fill in:\n<ul>\n<li><strong>Name</strong>: a short identifier, e.g. <code>shared-library</code> (this exact name is referenced later in pipeline code).</li>\n<li><strong>Default version</strong>: the branch to pull from, e.g. <code>master</code>.</li>\n<li><strong>Retrieval method</strong>: Modern SCM.</li>\n<li><strong>Source Code Management</strong>: GitHub.</li>\n<li><strong>Project Repository</strong>: paste the GitHub repo URL from Step 1.</li>\n</ul>\n</li>\n<li>Click <strong>Validate</strong> to confirm the connection works.</li>\n<li>Remove/ignore any unnecessary optional fields (credentials aren't required if the repo is public).</li>\n<li>Click <strong>Apply</strong>, then <strong>Save</strong>.</li>\n</ul>\n<h3 id=\"3-create-a-test-pipeline-job-skip-creation-if-one-already-exists\">3. Create a Test Pipeline Job (skip creation if one already exists)</h3>\n<ul>\n<li>Check Jenkins for an existing pipeline job intended for this test; if one exists and is configured similarly, reuse it instead of creating a new one.</li>\n<li>Otherwise, create a <strong>New Item → Pipeline</strong> project (e.g. named <code>shared-library-test-pipeline</code>).</li>\n<li>In the <strong>Pipeline</strong> section of the job configuration:\n<ul>\n<li>Check whether \"Use Groovy Sandbox\" is already unchecked; if not, <strong>uncheck it</strong> — this avoids sandbox restrictions when calling shared library classes.</li>\n</ul>\n</li>\n</ul>\n<h3 id=\"4-write-a-scripted-pipeline-that-uses-the-shared-library\">4. Write a Scripted Pipeline That Uses the Shared Library</h3>\n<p>In the Pipeline script box, use syntax equivalent to:</p>\n<pre><code class=\"language-groovy\"><span class=\"hljs-meta\">@Library</span>(<span class=\"hljs-string\">'shared-library'</span>) _\n<span class=\"hljs-keyword\">import</span> com.&lt;yourname&gt;.UatInput\n\n<span class=\"hljs-keyword\">def</span> uatInput = <span class=\"hljs-keyword\">new</span> UatInput()\nprintln <span class=\"hljs-string\">\"Build is UAT approved: ${uatInput.buildIsUatApproved()}\"</span>\n</code></pre>\n<ul>\n<li><code>@Library('shared-library')</code> must match the exact name given in Step 2.</li>\n<li>The import path must match the package/folder structure of the Groovy file (<code>com/&lt;yourname&gt;/UatInput.groovy</code> → <code>com.&lt;yourname&gt;.UatInput</code>).</li>\n<li>Save and run the build.</li>\n<li>Verify in the <strong>Console Output</strong> that it prints <code>true</code> when <code>tools/approved.txt</code> exists in the workspace, and <code>false</code> when it does not.</li>\n</ul>\n<h3 id=\"5-extend-to-a-declarative-pipeline\">5. Extend to a Declarative Pipeline</h3>\n<p>Replace the scripted script with a declarative pipeline that uses the shared library class inside a <code>when</code> condition, e.g.:</p>\n<pre><code class=\"language-groovy\"><span class=\"hljs-meta\">@Library</span>(<span class=\"hljs-string\">'shared-library'</span>) _\n<span class=\"hljs-keyword\">import</span> com.&lt;yourname&gt;.UatInput\n\n<span class=\"hljs-keyword\">def</span> uatInput = <span class=\"hljs-keyword\">new</span> UatInput()\n\npipeline {\n    agent any\n    stages {\n        stage(<span class=\"hljs-string\">'Run only if approval exists'</span>) {\n            when {\n                expression { <span class=\"hljs-keyword\">return</span> uatInput.buildIsUatApproved() }\n            }\n            steps {\n                echo <span class=\"hljs-string\">'The build has been approved.'</span>\n                <span class=\"hljs-comment\">// Add real build steps here (Maven, Docker, ant, etc.)</span>\n            }\n        }\n    }\n}\n</code></pre>\n<ul>\n<li>Save and run the build.</li>\n<li>Confirm the stage executes (and echoes output) only when <code>tools/approved.txt</code> exists.</li>\n<li>Remove or rename the file, re-run the build, and confirm the stage is skipped (no output from inside the <code>when</code> block) without failing the build.</li>\n</ul>\n<h3 id=\"6-validate-end-to-end\">6. Validate End-to-End</h3>\n<ul>\n<li>Test with the approval file present → confirm <code>true</code> / stage runs.</li>\n<li>Test with the approval file absent or renamed → confirm <code>false</code> / stage is skipped.</li>\n<li>Confirm no changes to the shared library repo are needed to reuse this pattern in other pipelines — only the <code>@Library</code> declaration and import.</li>\n</ul>\n<h2 id=\"notes-for-the-ai-agent-executing-this-task-4\">Notes for the AI Agent Executing This Task</h2>\n<ul>\n<li><strong>Do all discovery and questions in Step 0</strong> — don't ask the user for input mid-task (e.g. after already starting to create a repo). Batch every open question into one round of clarification before Step 1 begins.</li>\n<li>If Jenkins is running inside WSL or accessed from Windows via <code>localhost</code>, ensure the correct port (default <code>8080</code>) is exposed/forwarded.</li>\n<li>If the shared library GitHub repo is private, a Jenkins credential (username + GitHub Personal Access Token) will need to be created and selected in the Global Pipeline Library configuration (see the companion webhook task file for how to generate a GitHub PAT). Generating a new PAT requires the user to act in a browser (GitHub does not expose PAT creation via API/CLI for security reasons) — if one doesn't already exist, ask the user to generate it and provide it rather than attempting to script around this.</li>\n<li>Groovy Sandbox must be disabled for this simple example; for production use, consider approving specific method signatures via <strong>In-process Script Approval</strong> instead of fully disabling the sandbox.</li>\n</ul>\n<hr />\n\n<h2 id=\"part-5-task-ci-cd-pipeline-for-terraform-infrastructure-provisioning-via-jenkins\">Part 5: Task: CI/CD Pipeline for Terraform Infrastructure Provisioning via Jenkins into a Simulated AWS Environment (FLOCI)</h2>\n<h2 id=\"objective-5\">Objective</h2>\n<p>Build a Jenkins pipeline that is triggered by a GitHub webhook, runs a full Terraform workflow (init → workspace select → plan → manual approve/abort → apply) against infrastructure-as-code stored in a GitHub repo, and provisions the resulting infrastructure into <strong>FLOCI</strong>, a simulated AWS environment — rather than a real AWS account. This is intended as a safe sandbox for practicing Terraform + Jenkins CI/CD without incurring real cloud costs or risk.</p>\n<h2 id=\"goal-5\">Goal</h2>\n<p>By the end of this task, the environment should have:</p>\n<ol>\n<li>A GitHub repository containing Terraform code (<code>VPC.tf</code>, <code>Variables.tf</code>, <code>Provider.tf</code>), environment-specific var files (<code>dev</code>, <code>stg.tfvars</code>, <code>Prod</code>), and a <code>Jenkinsfile</code> defining the pipeline.</li>\n<li>A Jenkins pipeline, triggered by a GitHub webhook, with stages: <strong>Checkout → Terraform Init → Workspace Select → Plan (with manual Approve/Abort gate) → Apply → Post (Success/Fail → Mail)</strong>.</li>\n<li>The pipeline's <code>Provider.tf</code> configured so the Terraform CLI executes against <strong>FLOCI</strong> (an AWS API simulation environment) instead of real AWS, creating simulated VPC, Subnets, EC2, and S3 Bucket resources.</li>\n</ol>\n<h2 id=\"architecture-overview-from-diagram\">Architecture Overview (from diagram)</h2>\n<pre><code><span class=\"hljs-title class_\">GitHub</span> <span class=\"hljs-title class_\">Repository</span>                <span class=\"hljs-title class_\">Jenkins</span> CI/CD <span class=\"hljs-title class_\">Server</span>                              FLOCI (AWS <span class=\"hljs-title class_\">Simulation</span> <span class=\"hljs-title class_\">Env</span>)\n------------------              --------------------------------------            ---------------------------\nVPC.tf                          <span class=\"hljs-title class_\">Pipeline</span>\n<span class=\"hljs-title class_\">Variables</span>.tf                      - <span class=\"hljs-title class_\">Stage</span>: <span class=\"hljs-title class_\">Checkout</span>\n<span class=\"hljs-title class_\">Provider</span>.tf                       - <span class=\"hljs-title class_\">Terraform</span> <span class=\"hljs-title class_\">Execution</span>\ndev                                   - <span class=\"hljs-title class_\">Stage</span>: <span class=\"hljs-title class_\">Terraform</span> <span class=\"hljs-title class_\">Init</span> (terraform init)\nstg.tfvars           --CI/CD--&gt;      - <span class=\"hljs-title class_\">Stage</span>: <span class=\"hljs-title class_\">Workspace</span> <span class=\"hljs-title class_\">Select</span> (workspace)   --<span class=\"hljs-title class_\">Executes</span> &amp; <span class=\"hljs-title class_\">Calls</span>--&gt;  <span class=\"hljs-title class_\">Terraform</span> CLI --<span class=\"hljs-title class_\">Simulated</span> AWS API--&gt; <span class=\"hljs-title class_\">Simulated</span> VPC\n<span class=\"hljs-title class_\">Prod</span>                  <span class=\"hljs-title class_\">Trigger</span>            - <span class=\"hljs-title class_\">Stage</span>: <span class=\"hljs-title class_\">Plan</span> (plan)                    <span class=\"hljs-title class_\">Provider</span>                              (+ <span class=\"hljs-title class_\">Simulated</span> <span class=\"hljs-title class_\">Subnets</span>,\n<span class=\"hljs-title class_\">Jenkinsfile</span>           (<span class=\"hljs-title class_\">Webhook</span>)              -&gt; <span class=\"hljs-title class_\">Approve</span>                                                                 <span class=\"hljs-title class_\">Simulated</span> <span class=\"hljs-title class_\">EC2</span>,\n                                              -&gt; <span class=\"hljs-title class_\">Abort</span>                                                                  <span class=\"hljs-title class_\">Simulated</span> <span class=\"hljs-title class_\">S3</span> <span class=\"hljs-title class_\">Bucket</span>)\n                                       - <span class=\"hljs-title class_\">Stage</span>: <span class=\"hljs-title class_\">Apply</span> (apply)\n                                   - <span class=\"hljs-title class_\">Stage</span>: <span class=\"hljs-title class_\">Post</span>\n                                       -&gt; <span class=\"hljs-title class_\">Success</span>\n                                       -&gt; <span class=\"hljs-title class_\">Fail</span> -&gt; <span class=\"hljs-title class_\">Mail</span>\n                                                                                  <span class=\"hljs-title class_\">Provider</span>.tf (defines FLOCI <span class=\"hljs-keyword\">as</span> the\n                                                                                   <span class=\"hljs-title class_\">Terraform</span> provider endpoint)\n</code></pre>\n<h2 id=\"prerequisites-5\">Prerequisites</h2>\n<ul>\n<li>A GitHub repository to hold the Terraform code and Jenkinsfile.</li>\n<li>A running Jenkins server with the <strong>Terraform</strong> and <strong>Pipeline</strong> plugins available (and Git/GitHub plugins for checkout + webhook triggering).</li>\n<li>Terraform CLI installed wherever the Jenkins agent executes the pipeline.</li>\n<li>Access to FLOCI (or equivalent AWS simulation environment) with whatever endpoint/credentials it requires in place of real AWS credentials.</li>\n<li>An SMTP/mail configuration in Jenkins if the \"Fail → Mail\" post-action is desired.</li>\n</ul>\n<h2 id=\"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth-5\">Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything</h2>\n<p>Do this entire section before Step 1, so the pipeline can be built end-to-end without stopping partway through to ask for something.</p>\n<h3 id=\"0-1-check-tools-environment-5\">0.1 Check tools/environment</h3>\n<ul>\n<li>Jenkins reachable: <code>curl -s -o /dev/null -w \"%{http_code}\" http://localhost:8080</code> (or check the relevant service/container).</li>\n<li>Terraform CLI installed on the Jenkins agent/host: <code>terraform -version</code>.</li>\n<li>Git available: <code>git --version</code>.</li>\n<li>Confirm the Jenkins <strong>Terraform plugin</strong> (or equivalent CLI wrapper approach) and <strong>Pipeline</strong> plugin are installed (<strong>Manage Jenkins → Plugins</strong>).</li>\n<li>Confirm Jenkins <strong>Mailer plugin</strong> (or equivalent) is installed/configured if a \"Fail → Mail\" step is wanted.</li>\n</ul>\n<h3 id=\"0-2-check-credentials-and-account-data-5\">0.2 Check credentials and account data</h3>\n<ul>\n<li>Whether FLOCI access details (endpoint URL, API key/token, or whatever FLOCI requires to be addressed as a Terraform provider target) already exist or are documented. FLOCI is a simulation tool, not real AWS, so standard AWS credential docs won't apply — the specific FLOCI setup instructions are needed here.</li>\n<li>Whether a Jenkins credential already exists for FLOCI/simulated-provider access (<strong>Manage Jenkins → Credentials</strong>).</li>\n<li>Whether GitHub credentials/webhook access are already configured (see the companion webhook setup task file if not).</li>\n<li>Mail server credentials/SMTP relay details, if the mail-on-failure step is in scope.</li>\n</ul>\n<h3 id=\"0-3-check-existing-repo-config-state-5\">0.3 Check existing repo/config state</h3>\n<ul>\n<li>Whether the target GitHub repo already contains <code>VPC.tf</code>, <code>Variables.tf</code>, <code>Provider.tf</code>, environment var files, and a <code>Jenkinsfile</code>, or whether these need to be created from scratch.</li>\n<li>Whether a Jenkins pipeline job already exists for this project.</li>\n<li>Whether Terraform workspaces (<code>dev</code>, <code>stg</code>, <code>prod</code>) already exist in the target backend, or need to be created (<code>terraform workspace list</code>).</li>\n<li>What Terraform state backend is in use (local state vs. remote backend) — this affects how <code>terraform init</code> behaves and whether extra backend credentials are needed.</li>\n</ul>\n<h3 id=\"0-4-ask-the-user-for-anything-missing-before-proceeding-5\">0.4 Ask the user for anything missing — before proceeding</h3>\n<p>Compile everything unresolved after the checks above into one batch of questions, asked upfront:</p>\n<ul>\n<li>How to configure <code>Provider.tf</code> to target FLOCI specifically — what endpoint, credentials, or provider block format FLOCI expects in place of a real <code>aws</code> provider block. (This is the single most important unknown — without it the \"Apply\" stage cannot actually target the simulation.)</li>\n<li>Which environments/workspaces to set up (<code>dev</code>, <code>stg</code>, <code>prod</code>) and their corresponding <code>.tfvars</code> file names/contents.</li>\n<li>Whether the manual Plan Approve/Abort gate should use Jenkins' <code>input</code> step (pausing the pipeline for a human), and who should be notified/authorized to approve it.</li>\n<li>Mail recipient(s) for the \"Fail → Mail\" post-action, if wanted.</li>\n<li>The GitHub repo URL/branch, if not already known from a prior task.</li>\n</ul>\n<p>Only proceed to Step 1 once every required input is either confirmed present or supplied by the user.</p>\n<h2 id=\"steps-5\">Steps</h2>\n<h3 id=\"1-prepare-the-github-repository-skip-reuse-pieces-already-present\">1. Prepare the GitHub Repository (skip/reuse pieces already present)</h3>\n<ul>\n<li>Ensure the repo contains:\n<ul>\n<li><code>VPC.tf</code> — VPC resource definitions.</li>\n<li><code>Variables.tf</code> — input variable declarations.</li>\n<li><code>Provider.tf</code> — provider block configured to target FLOCI's simulated AWS API (per Step 0.4 answer).</li>\n<li>Environment var files: <code>dev</code> (dir or <code>.tfvars</code>), <code>stg.tfvars</code>, <code>Prod</code> (dir or <code>.tfvars</code>) — one per environment/workspace.</li>\n<li>A <code>Jenkinsfile</code> at the repo root defining the pipeline (built in Step 2).</li>\n</ul>\n</li>\n<li>Commit and push any missing pieces.</li>\n</ul>\n<h3 id=\"2-write-the-jenkinsfile-declarative-pipeline\">2. Write the Jenkinsfile (Declarative Pipeline)</h3>\n<p>Structure the pipeline stages to match the diagram:</p>\n<pre><code class=\"language-groovy\">pipeline {\n    agent any\n    stages {\n        stage(<span class=\"hljs-string\">'Checkout'</span>) {\n            steps {\n                checkout scm\n            }\n        }\n        stage(<span class=\"hljs-string\">'Terraform Init'</span>) {\n            steps {\n                sh <span class=\"hljs-string\">'terraform init'</span>\n            }\n        }\n        stage(<span class=\"hljs-string\">'Workspace Select'</span>) {\n            steps {\n                sh <span class=\"hljs-string\">'terraform workspace select ${ENV_NAME} || terraform workspace new ${ENV_NAME}'</span>\n            }\n        }\n        stage(<span class=\"hljs-string\">'Plan'</span>) {\n            steps {\n                sh <span class=\"hljs-string\">'terraform plan -var-file=${TFVARS_FILE} -out=tfplan'</span>\n            }\n        }\n        stage(<span class=\"hljs-string\">'Approve'</span>) {\n            steps {\n                input <span class=\"hljs-attr\">message:</span> <span class=\"hljs-string\">'Apply this Terraform plan?'</span>, <span class=\"hljs-attr\">ok:</span> <span class=\"hljs-string\">'Approve'</span>\n            }\n        }\n        stage(<span class=\"hljs-string\">'Apply'</span>) {\n            steps {\n                sh <span class=\"hljs-string\">'terraform apply -auto-approve tfplan'</span>\n            }\n        }\n    }\n    post {\n        success {\n            echo <span class=\"hljs-string\">'Pipeline succeeded.'</span>\n        }\n        failure {\n            mail <span class=\"hljs-attr\">to:</span> <span class=\"hljs-string\">'&lt;recipient-from-step-0.4&gt;'</span>,\n                 <span class=\"hljs-symbol\">subject:</span> <span class=\"hljs-string\">\"Pipeline Failed: ${env.JOB_NAME} #${env.BUILD_NUMBER}\"</span>,\n                 <span class=\"hljs-symbol\">body:</span> <span class=\"hljs-string\">\"Check console output at ${env.BUILD_URL}\"</span>\n        }\n    }\n}\n</code></pre>\n<ul>\n<li>Replace <code>${ENV_NAME}</code> and <code>${TFVARS_FILE}</code> with actual parameters or a parameterized build (e.g. a Jenkins <code>choice</code> parameter for <code>dev</code>/<code>stg</code>/<code>prod</code>).</li>\n<li>The <code>input</code> step is what implements the \"Approve / Abort\" branch from the diagram — declining the prompt aborts the pipeline before <code>apply</code> runs.</li>\n</ul>\n<h3 id=\"3-configure-the-pipeline-job-in-jenkins\">3. Configure the Pipeline Job in Jenkins</h3>\n<ul>\n<li>Create (or reuse, per Step 0.3) a Jenkins Pipeline job pointing at the GitHub repo, using \"Pipeline script from SCM\" so it reads the <code>Jenkinsfile</code> directly from the repo.</li>\n<li>Set up the GitHub webhook trigger (see the companion webhook setup task file for full webhook steps) so pushes to the repo trigger this pipeline automatically.</li>\n</ul>\n<h3 id=\"4-configure-provider-tf-to-target-floci\">4. Configure <code>Provider.tf</code> to Target FLOCI</h3>\n<ul>\n<li>Set the Terraform provider block to point at FLOCI's simulated AWS API per the details gathered in Step 0.4, instead of a real AWS account/region/credentials.</li>\n<li>Confirm <code>terraform init</code> succeeds against this provider configuration.</li>\n</ul>\n<h3 id=\"5-run-and-validate-end-to-end-2\">5. Run and Validate End-to-End</h3>\n<ul>\n<li>Trigger the pipeline (via webhook push or manually).</li>\n<li>Confirm each stage completes: Checkout → Init → Workspace Select → Plan.</li>\n<li>Confirm the pipeline pauses at the Approve/Abort gate and waits for manual input.</li>\n<li>Approve the plan and confirm Apply runs successfully.</li>\n<li>Confirm FLOCI reflects the simulated VPC, Subnets, EC2, and S3 Bucket resources described in the Terraform code.</li>\n<li>Test the Abort path: reject the plan and confirm Apply does not run and the pipeline stops cleanly.</li>\n<li>Introduce a deliberate failure (e.g. invalid Terraform syntax) and confirm the \"Fail → Mail\" post-action sends a notification.</li>\n</ul>\n<h2 id=\"notes-for-the-ai-agent-executing-this-task-5\">Notes for the AI Agent Executing This Task</h2>\n<ul>\n<li><strong>FLOCI specifics are the main unknown</strong> — this is not a standard AWS provider setup, so don't assume real AWS credential patterns (IAM roles, <code>~/.aws/credentials</code>, etc.) apply. Get the actual FLOCI provider configuration format from the user in Step 0.4 rather than guessing.</li>\n<li>The Approve/Abort gate is a manual human checkpoint by design — do not attempt to auto-approve it unless explicitly instructed, since its purpose is to prevent unreviewed infrastructure changes from being applied.</li>\n<li>Keep Terraform state per-environment isolated (via workspaces or separate state files) so that a <code>dev</code> apply cannot accidentally affect <code>stg</code>/<code>prod</code> state.</li>\n<li>If this task is combined with the companion GitHub webhook setup task, do Step 0 pre-flight checks jointly rather than duplicating them.</li>\n</ul>\n","toc":[{"id":"jenkins-labs-handbook","text":"Jenkins_Labs_Handbook","level":1},{"id":"part-1-task-configure-github-webhook-to-automatically-trigger-a-jenkins-ci-cd-pi","text":"Part 1: Task: Configure GitHub Webhook to Automatically Trigger a Jenkins CI/CD Pipeline","level":2},{"id":"objective","text":"Objective","level":2},{"id":"goal","text":"Goal","level":2},{"id":"prerequisites","text":"Prerequisites","level":2},{"id":"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth","text":"Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything","level":2},{"id":"0-1-check-tools-environment","text":"0.1 Check tools/environment","level":3},{"id":"0-2-check-credentials-and-account-data","text":"0.2 Check credentials and account data","level":3},{"id":"0-3-check-existing-repo-config-state","text":"0.3 Check existing repo/config state","level":3},{"id":"0-4-ask-the-user-for-anything-missing-before-proceeding","text":"0.4 Ask the user for anything missing — before proceeding","level":3},{"id":"steps","text":"Steps","level":2},{"id":"1-create-or-reuse-a-basic-pipeline-job-in-jenkins","text":"1. Create or Reuse a Basic Pipeline Job in Jenkins","level":3},{"id":"2-get-the-github-repository-url","text":"2. Get the GitHub Repository URL","level":3},{"id":"3-confirm-the-github-personal-access-token-pat-for-authentication","text":"3. Confirm the GitHub Personal Access Token (PAT) for Authentication","level":3},{"id":"4-add-or-reuse-github-credentials-in-jenkins","text":"4. Add or Reuse GitHub Credentials in Jenkins","level":3},{"id":"5-configure-or-verify-the-webhook-on-the-github-side","text":"5. Configure or Verify the Webhook on the GitHub Side","level":3},{"id":"6-configure-or-verify-the-trigger-on-the-jenkins-side","text":"6. Configure or Verify the Trigger on the Jenkins Side","level":3},{"id":"7-test-the-end-to-end-automatic-trigger","text":"7. Test the End-to-End Automatic Trigger","level":3},{"id":"notes-for-the-ai-agent-executing-this-task","text":"Notes for the AI Agent Executing This Task","level":2},{"id":"part-2-task-jenkins-ci-cd-pipeline-build-tag-and-push-a-docker-image-to-docker-h","text":"Part 2: Task: Jenkins CI/CD Pipeline — Build, Tag, and Push a Docker Image to Docker Hub (with Simulated AWS Deployment via FLOCI)","level":2},{"id":"objective-2","text":"Objective","level":2},{"id":"goal-2","text":"Goal","level":2},{"id":"architecture-overview-from-diagrams","text":"Architecture Overview (from diagrams)","level":2},{"id":"core-build-and-push-flow-image-2","text":"Core build-and-push flow (Image 2)","level":3},{"id":"extended-flow-with-numbered-stages-and-simulated-deployment-image-3","text":"Extended flow with numbered stages and simulated deployment (Image 3)","level":3},{"id":"prerequisites-2","text":"Prerequisites","level":2},{"id":"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth-2","text":"Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything","level":2},{"id":"0-1-check-tools-environment-2","text":"0.1 Check tools/environment","level":3},{"id":"0-2-check-credentials-and-account-data-2","text":"0.2 Check credentials and account data","level":3},{"id":"0-3-check-existing-repo-config-state-2","text":"0.3 Check existing repo/config state","level":3},{"id":"0-4-ask-the-user-for-anything-missing-before-proceeding-2","text":"0.4 Ask the user for anything missing — before proceeding","level":3},{"id":"steps-2","text":"Steps","level":2},{"id":"1-prepare-the-repository-skip-reuse-pieces-already-present","text":"1. Prepare the Repository (skip/reuse pieces already present)","level":3},{"id":"2-add-docker-hub-credentials-to-jenkins-skip-if-already-present-per-step-0-2","text":"2. Add Docker Hub Credentials to Jenkins (skip if already present, per Step 0.2)","level":3},{"id":"3-write-the-jenkinsfile-declarative-pipeline","text":"3. Write the Jenkinsfile (Declarative Pipeline)","level":3},{"id":"4-configure-the-pipeline-job-in-jenkins","text":"4. Configure the Pipeline Job in Jenkins","level":3},{"id":"5-run-and-validate-end-to-end","text":"5. Run and Validate End-to-End","level":3},{"id":"notes-for-the-ai-agent-executing-this-task-2","text":"Notes for the AI Agent Executing This Task","level":2},{"id":"part-3-task-set-up-a-jenkins-multibranch-pipeline-using-git","text":"Part 3: Task: Set Up a Jenkins Multibranch Pipeline Using Git","level":2},{"id":"objective-3","text":"Objective","level":2},{"id":"goal-3","text":"Goal","level":2},{"id":"key-concept","text":"Key Concept","level":2},{"id":"prerequisites-3","text":"Prerequisites","level":2},{"id":"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth-3","text":"Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything","level":2},{"id":"0-1-check-tools-environment-3","text":"0.1 Check tools/environment","level":3},{"id":"0-2-check-credentials-and-account-data-3","text":"0.2 Check credentials and account data","level":3},{"id":"0-3-check-existing-repo-config-state-3","text":"0.3 Check existing repo/config state","level":3},{"id":"0-4-ask-the-user-for-anything-missing-before-proceeding-3","text":"0.4 Ask the user for anything missing — before proceeding","level":3},{"id":"steps-3","text":"Steps","level":2},{"id":"1-create-the-multibranch-pipeline-job-in-jenkins-skip-reuse-if-one-already-exist","text":"1. Create the Multibranch Pipeline Job in Jenkins (skip/reuse if one already exists, per Step 0.3)","level":3},{"id":"2-add-a-git-branch-source","text":"2. Add a Git Branch Source","level":3},{"id":"3-configure-branch-filtering-include-all-three-target-branches","text":"3. Configure Branch Filtering (Include All Three Target Branches)","level":3},{"id":"4-save-and-observe-the-initial-scan","text":"4. Save and Observe the Initial Scan","level":3},{"id":"5-add-a-jenkinsfile-to-the-repository","text":"5. Add a Jenkinsfile to the Repository","level":3},{"id":"6-trigger-a-rescan-no-webhooks-in-this-setup","text":"6. Trigger a Rescan (No Webhooks in This Setup)","level":3},{"id":"7-create-the-dev-and-stg-branches","text":"7. Create the dev and stg Branches","level":3},{"id":"8-add-branch-specific-pipeline-behavior","text":"8. Add Branch-Specific Pipeline Behavior","level":3},{"id":"9-merge-changes-back-to-main-and-observe-orphan-cleanup-optional","text":"9. Merge Changes Back to Main and Observe Orphan Cleanup (Optional)","level":3},{"id":"notes-for-the-ai-agent-executing-this-task-3","text":"Notes for the AI Agent Executing This Task","level":2},{"id":"part-4-task-set-up-a-jenkins-shared-library-linked-to-a-github-repository","text":"Part 4: Task: Set Up a Jenkins Shared Library Linked to a GitHub Repository","level":2},{"id":"objective-4","text":"Objective","level":2},{"id":"goal-4","text":"Goal","level":2},{"id":"prerequisites-4","text":"Prerequisites","level":2},{"id":"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth-4","text":"Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything","level":2},{"id":"0-1-check-tools-environment-4","text":"0.1 Check tools/environment","level":3},{"id":"0-2-check-credentials-and-account-data-4","text":"0.2 Check credentials and account data","level":3},{"id":"0-3-check-existing-repo-config-state-4","text":"0.3 Check existing repo/config state","level":3},{"id":"0-4-ask-the-user-for-anything-missing-before-proceeding-4","text":"0.4 Ask the user for anything missing — before proceeding","level":3},{"id":"repository-structure-required-for-the-shared-library","text":"Repository Structure Required for the Shared Library","level":2},{"id":"steps-4","text":"Steps","level":2},{"id":"1-create-the-shared-library-repository-on-github-skip-if-it-already-exists","text":"1. Create the Shared Library Repository on GitHub (skip if it already exists)","level":3},{"id":"2-register-the-shared-library-in-jenkins-skip-if-already-registered","text":"2. Register the Shared Library in Jenkins (skip if already registered)","level":3},{"id":"3-create-a-test-pipeline-job-skip-creation-if-one-already-exists","text":"3. Create a Test Pipeline Job (skip creation if one already exists)","level":3},{"id":"4-write-a-scripted-pipeline-that-uses-the-shared-library","text":"4. Write a Scripted Pipeline That Uses the Shared Library","level":3},{"id":"5-extend-to-a-declarative-pipeline","text":"5. Extend to a Declarative Pipeline","level":3},{"id":"6-validate-end-to-end","text":"6. Validate End-to-End","level":3},{"id":"notes-for-the-ai-agent-executing-this-task-4","text":"Notes for the AI Agent Executing This Task","level":2},{"id":"part-5-task-ci-cd-pipeline-for-terraform-infrastructure-provisioning-via-jenkins","text":"Part 5: Task: CI/CD Pipeline for Terraform Infrastructure Provisioning via Jenkins into a Simulated AWS Environment (FLOCI)","level":2},{"id":"objective-5","text":"Objective","level":2},{"id":"goal-5","text":"Goal","level":2},{"id":"architecture-overview-from-diagram","text":"Architecture Overview (from diagram)","level":2},{"id":"prerequisites-5","text":"Prerequisites","level":2},{"id":"step-0-pre-flight-check-verify-tools-credentials-and-data-before-executing-anyth-5","text":"Step 0: Pre-Flight Check — Verify Tools, Credentials, and Data Before Executing Anything","level":2},{"id":"0-1-check-tools-environment-5","text":"0.1 Check tools/environment","level":3},{"id":"0-2-check-credentials-and-account-data-5","text":"0.2 Check credentials and account data","level":3},{"id":"0-3-check-existing-repo-config-state-5","text":"0.3 Check existing repo/config state","level":3},{"id":"0-4-ask-the-user-for-anything-missing-before-proceeding-5","text":"0.4 Ask the user for anything missing — before proceeding","level":3},{"id":"steps-5","text":"Steps","level":2},{"id":"1-prepare-the-github-repository-skip-reuse-pieces-already-present","text":"1. Prepare the GitHub Repository (skip/reuse pieces already present)","level":3},{"id":"2-write-the-jenkinsfile-declarative-pipeline","text":"2. Write the Jenkinsfile (Declarative Pipeline)","level":3},{"id":"3-configure-the-pipeline-job-in-jenkins","text":"3. Configure the Pipeline Job in Jenkins","level":3},{"id":"4-configure-provider-tf-to-target-floci","text":"4. Configure Provider.tf to Target FLOCI","level":3},{"id":"5-run-and-validate-end-to-end-2","text":"5. Run and Validate End-to-End","level":3},{"id":"notes-for-the-ai-agent-executing-this-task-5","text":"Notes for the AI Agent Executing This Task","level":2}],"words":7354,"assets":[]};