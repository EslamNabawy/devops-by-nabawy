window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["aws-04-aws-academy-cloud-foundations-module-4-cloud-security"]={"html":"<p>{/* MIGRATED from: D:\\NTI Courses Master Folder\\AWS Master Folder\\aws\\study-notes\\Module4-CloudSecurity-StudyNotes.md\nCourse metadata: course=aws | AWS Academy Cloud Foundations | companion: modules md files/AWS_Module4_CloudSecurity_Notes.md */}</p>\n<h1 id=\"aws-academy-cloud-foundations-module-4-cloud-security\">AWS Academy Cloud Foundations — Module 4: Cloud Security</h1>\n<h2 id=\"study-notes\">Study Notes</h2>\n<p><em>Companion to: <code>modules md files/AWS_Module4_CloudSecurity_Notes.md</code></em></p>\n<hr />\n<h2 id=\"1-shared-responsibility-the-one-model-to-memorize\">1. Shared Responsibility — The One Model to Memorize</h2>\n<p>Security in AWS is <em>always</em> shared, and the single most examined idea in\nthis module is where the split falls. Two short phrases carry it:</p>\n<p><strong>AWS: \"Security OF the cloud.\"</strong> Everything you never see: physical data\ncenters and hardware, the host operating system and virtualization layer,\nthe global network of Regions and AZs, patching of managed services (the\nRDS database engine, the Lambda runtime), and the availability and\ndurability of the platform itself.</p>\n<p><strong>Customer: \"Security IN the cloud.\"</strong> Everything you touch or configure:\nyour data and its encryption at rest, in transit, and in use; your IAM\nusers, groups, roles, and policies; guest OS patching on resources you\nmanage; security group and NACL rules; application code and its\ndependencies; client-side data.</p>\n<p>The pattern to internalize rather than memorize rows: <strong>the more you\nconfigure, the more you own.</strong> On EC2 (IaaS) you own the guest OS upward.\nOn RDS (managed) AWS patches the database engine while you keep IAM,\nnetworking, encryption configuration, and data. On Lambda (serverless) AWS\nowns runtime, scaling, and patching while you keep the function code, its\nIAM role, and its data. On S3 AWS owns durability and infrastructure while\nyou keep bucket and IAM policies, encryption choices, versioning, and the\ndata itself. And your data is always yours — no service tier transfers that.</p>\n<p><strong>Mnemonic:</strong> AWS = \"of\" · Customer = \"in\".</p>\n<p>The classic trap, stated plainly: <em>\"AWS is responsible for security\"</em> is\nhalf-true. AWS owns OF — physical, host, hypervisor. You always own your\ndata, your IAM, and any OS or network controls on resources you manage.</p>\n<hr />\n<h2 id=\"2-iam-the-gate-to-everything\">2. IAM — The Gate to Everything</h2>\n<p>Identity and Access Management is the gate every AWS API call passes\nthrough first. Three facts frame the whole section: <strong>IAM is global</strong> (a\nuser created in one Region exists everywhere), <strong>evaluation is deny-first</strong>\n(an explicit deny beats any number of allows), and <strong>everything is an API\ncall</strong> (so controlling IAM controls the account).</p>\n<p>Four entities, four distinct jobs. A <strong>user</strong> is a long-term identity for a\nperson or an application, carrying durable credentials — give each human\ntheir own user, never share. A <strong>group</strong> bundles users so a policy attached\nonce flows to all members; groups organize, they cannot be <em>assumed</em>.\nA <strong>role</strong> is assumed temporarily through the Security Token Service (STS)\nfor a limited session: EC2 instances reaching S3, Lambda execution,\ncross-account access, federated single sign-on. No long-term credentials\nexist for a role, which is exactly why roles are safer. A <strong>policy</strong> is the\nJSON document (Effect + Action + Resource) attached to users, groups, or\nroles that states what is allowed or denied.</p>\n<p>Policies come in flavors worth distinguishing. <strong>Identity-based</strong> policies\nattach to users, groups, or roles. <strong>Resource-based</strong> policies live inline\non the resource itself — S3 buckets, SNS topics, SQS queues. <strong>AWS managed</strong>\npolicies (like ReadOnlyAccess) are pre-built starting points.\n<strong>Customer managed</strong> policies are yours: versioned, reusable, narrowable.\n<strong>Inline</strong> policies are embedded directly in a single identity. Above them\nall sit <strong>Service Control Policies (SCPs)</strong> from Organizations (Module 2):\nthey filter the maximum an account's policies can grant, and they never\ngrant anything themselves.</p>\n<h3 id=\"credentials-by-use\">Credentials, by use</h3>\n<p>Console <strong>passwords</strong> sign humans into the console — pair them with MFA and\na real password policy. <strong>Access keys</strong> drive the CLI, SDKs, and API calls —\nrotate them, never hard-code or commit them, and prefer roles wherever a\nmachine (not a person) needs access. <strong>MFA devices</strong> (virtual or hardware)\nprotect sign-in and privileged actions; required for root and admins.\n<strong>Key pairs</strong> split SSH/RDP access: the public key stays with AWS, and the\nprivate key's secrecy is entirely your problem. <strong>STS session tokens</strong> are\nwhat assuming a role returns: temporary, auto-expiring, the mechanism that\nmakes roles work.</p>\n<h3 id=\"principles-that-win-marks\">Principles that win marks</h3>\n<p><strong>Least privilege</strong> — grant only what is needed today, expand deliberately\nlater. <strong>Prefer roles over long-term keys</strong> for services and cross-account\npaths. <strong>Explicit deny wins</strong> over any allow, which is how break-glass\ndenials and guardrails stay reliable. <strong>Root is not for daily use</strong> — ever.</p>\n<hr />\n<h2 id=\"3-hardening-a-new-account-do-this-first-in-this-order\">3. Hardening a New Account — Do This First, In This Order</h2>\n<p>The module teaches account hardening as a sequence, and the order is the\nlesson — each step protects the next:</p>\n<ol>\n<li><strong>Lock the root user.</strong> Enable MFA on root, set a strong password,\ndelete any root access keys, store credentials offline. Root is\nunlimited and irreplaceable; it must also be almost never used.</li>\n<li><strong>Create an admin principal.</strong> An IAM user or role (or SSO identity)\nwith AdministratorAccess, MFA enabled — this becomes the daily driver\ngoing forward.</li>\n<li><strong>Enforce MFA broadly.</strong> Every IAM user with console access, especially\nanyone holding sensitive permissions. Consider MFA conditions on\nprivileged API calls too.</li>\n<li><strong>Apply least-privilege policies.</strong> AdministratorAccess stops at the\nadmin principal. Normal users start from AWS managed policies and narrow\nwith custom policies; assign through groups so permissions stay\nreviewable.</li>\n<li><strong>Turn on visibility.</strong> CloudTrail in all Regions writing to encrypted\nS3 (the audit trail of <em>who did what</em>), Config (configuration history\nplus rules evaluating <em>what changed</em>), GuardDuty (threat detection for\n<em>what looks malicious</em>).</li>\n<li><strong>Organize and guard.</strong> Multi-account means Organizations plus SCPs plus\nconsolidated CloudTrail — and consistent tagging, which is governance as\nmuch as cost control (Module 2).</li>\n<li><strong>Run Access Analyzer</strong> to surface unintended resource access the\npolicies above missed.</li>\n</ol>\n<p>The trap embedded in step one: root access keys must be <em>deleted</em>, never\nused for programmatic access. Code and scripts get IAM users or, better,\nroles — root credentials touch nothing routine.</p>\n<hr />\n<h2 id=\"4-protecting-data-three-states-three-toolkits\">4. Protecting Data — Three States, Three Toolkits</h2>\n<p>Data needs different protection depending on where it is. <strong>At rest</strong> —\ndisks, snapshots, backups — the threat is theft or leakage of the stored\nobject, and the answer is server-side encryption: SSE-S3, SSE-KMS, or\nSSE-C for S3 objects; EBS and RDS encryption; KMS Customer Master Keys,\nAWS-managed or customer-managed, underneath nearly all of it. <strong>In\ntransit</strong> — bytes on the wire — the threat is interception, and the answer\nis TLS/HTTPS (certificates from ACM, terminated at load balancers,\nCloudFront, or API Gateway), VPNs, Direct Connect with MACsec, and S3\nendpoint policies keeping traffic off the public internet. <strong>In use</strong> —\ndata being processed — the threat is exposure during computation, and the\nanswer is CloudHSM (dedicated hardware security modules), Nitro Enclaves,\nor encrypting client-side before upload so AWS never sees plaintext.</p>\n<h3 id=\"s3-encryption-options-compared-by-key-holder\">S3 encryption options, compared by key holder</h3>\n<p><strong>SSE-S3 (AES-256)</strong> keeps keys with AWS: the default, zero setup, right\nwhen control requirements are light. <strong>SSE-KMS</strong> routes through KMS with an\nAWS-managed CMK: same convenience plus CloudTrail-audited key usage, right\nwhen you must prove who decrypted what. <strong>SSE-C</strong> lets you bring your own\nkey per request while AWS performs the encryption: right when policy\ndemands customer-held keys but operations stay server-side. <strong>Client-side\nencryption</strong> never lets AWS see plaintext at all: right for the strictest\nregimes, at the cost of owning the whole key lifecycle.</p>\n<p><strong>Mnemonic:</strong> S3 · KMS · C · C → SSE-S3 · SSE-KMS · SSE-C · Client-side.</p>\n<p>EBS encryption (AES-256 at rest, KMS-managed, transparent to EC2) and RDS\nencryption (at rest via KMS plus in transit via TLS, enabled at creation)\nfollow the same pattern: declare it, KMS enforces it, the service absorbs\nthe complexity.</p>\n<hr />\n<h2 id=\"5-kms-secrets-and-certificates-know-which-solves-what\">5. KMS, Secrets, and Certificates — Know Which Solves What</h2>\n<p><strong>KMS</strong> is the center of gravity: create, rotate, and control encryption\nkeys in one place, integrated with S3, EBS, RDS, and nearly everything\nelse. The exam's phrasing — \"every encrypted service calls KMS\" — is close\nto literally true. <strong>Secrets Manager</strong> stores database passwords and API\nkeys <em>with built-in rotation</em>; <strong>Parameter Store</strong> (Systems Manager) stores\nhierarchical key/value configuration with a free tier but no auto-rotation.\nThe decision between them is one question: does the secret need rotation?\nRotating → Secrets Manager. Static → Parameter Store. <strong>CloudHSM</strong> is the\nescalation beyond KMS: dedicated hardware security modules at FIPS 140-2\nLevel 3, for when shared-tenant key management is not enough. <strong>ACM</strong>\nprovisions and auto-renews TLS certificates for load balancers, CloudFront,\nand API Gateway — terminate TLS at the edge, keep encryption inside the VPC.</p>\n<hr />\n<h2 id=\"6-threat-detection-amp-security-services-one-liners-that-decide-scenarios\">6. Threat Detection &amp; Security Services — One-Liners That Decide Scenarios</h2>\n<p>The exam tests <em>which service fits the scenario</em>, not deep configuration.\nLearn each as a single sentence. <strong>CloudTrail</strong> records every API call:\n<em>who did what, when</em> — the audit trail. <strong>Config</strong> records <em>what changed</em>:\nresource configuration history plus rules evaluating compliance over time.\n<strong>GuardDuty</strong> detects <em>what looks malicious</em>: machine-learning threat\ndetection over API calls, network flow, and DNS. <strong>Security Hub</strong> is the\ncentral posture dashboard aggregating findings across accounts.\n<strong>Inspector</strong> scans EC2 instances and container images for known\nvulnerabilities. <strong>Macie</strong> finds sensitive data (PII, credentials) sitting\nin S3. <strong>WAF</strong> is the Layer-7 web application firewall in front of\nCloudFront and load balancers. <strong>Shield</strong> is DDoS protection — Standard\nfree and automatic, Advanced paid with response-team support.</p>\n<p>The visibility trio compresses to: Trail (who) · Config (what changed) ·\nGuardDuty (malicious). When a question asks for detection, start there.</p>\n<hr />\n<h2 id=\"7-compliance-reports-evidence-and-programs\">7. Compliance — Reports, Evidence, and Programs</h2>\n<p>Compliance on AWS has three layers. <strong>Programs</strong> are the certifications AWS\nholds: ISO 27001/27017/27018 globally, SOC 1/2/3, PCI DSS Level 1 for card\npayments, HIPAA-eligible services for US healthcare, FedRAMP for US\nfederal, GDPR alignment for EU data, GovCloud as an isolated region for US\npublic sector. <strong>Artifact</strong> is the self-service portal where you download\nthose reports for auditors — SOC, ISO, PCI — on demand, no ticket needed.\n<strong>Audit Manager</strong> produces continuous audit evidence for regulated\nworkloads, and <strong>Config</strong> enforces configuration-compliance rules over time.</p>\n<p><strong>Exam shorthand:</strong> \"need the SOC 2 report\" → Artifact. \"See every API\ncall\" → CloudTrail. \"Temporary S3 access for EC2\" → IAM Role, never access\nkeys. \"Who patches the EC2 guest OS?\" → customer. \"The host or hypervisor?\"\n→ AWS.</p>\n<hr />\n<h2 id=\"8-knowledge-check\">8. Knowledge Check</h2>\n<ol>\n<li><strong>Q:</strong> Who patches the hypervisor under your EC2 instance, and which half\nof the model is that?\n<strong>A:</strong> AWS — Security <em>OF</em> the cloud (infrastructure you never see).</li>\n<li><strong>Q:</strong> An S3 bucket policy versus an IAM user policy — which is\nresource-based, and why does it matter?\n<strong>A:</strong> The bucket policy: it travels with the resource and can grant\ncross-account access without touching the other account's IAM.</li>\n<li><strong>Q:</strong> Why are roles preferred over access keys for Lambda execution?\n<strong>A:</strong> Roles yield temporary STS credentials that auto-expire and rotate\n— no long-term secret exists to leak from code or logs.</li>\n<li><strong>Q:</strong> First three steps securing a new account, in order?\n<strong>A:</strong> MFA on root (plus deleting root keys) → admin principal with MFA\n→ MFA everywhere plus least-privilege policies. Order matters because\neach step protects the next.</li>\n<li><strong>Q:</strong> Data must be encrypted before it reaches AWS at all. Which option,\nand what does AWS never see?\n<strong>A:</strong> Client-side encryption — AWS never sees plaintext; you own the\nfull key lifecycle.</li>\n</ol>\n<hr />\n<h2 id=\"9-key-takeaways\">9. Key Takeaways</h2>\n<ul>\n<li>OF = AWS (physical, host, hypervisor). IN = you (data, IAM, OS,\nencryption). The line moves with managed-ness: EC2 you own more, Lambda\nAWS owns more.</li>\n<li>IAM: users long-term · groups bundle · roles temporary (STS) · policies\nJSON. Least privilege, MFA, roles over keys, explicit deny wins. Global.</li>\n<li>Harden in order: MFA root → admin principal → MFA all → least privilege →\nCloudTrail / Config / GuardDuty → Organizations + SCPs.</li>\n<li>Data: at rest (KMS/SSE) · in transit (TLS/ACM/VPN) · in use (CloudHSM).\nRotation need decides Secrets Manager vs Parameter Store.</li>\n<li>Artifact for reports; Trail/Config/GuardDuty for visibility; Hub/Macie/\nInspector/WAF/Shield as scenario one-liners.</li>\n</ul>\n<hr />\n<h2 id=\"10-glossary\">10. Glossary</h2>\n<ul>\n<li><strong>Root account</strong> — signup identity; unlimited, restricted use.</li>\n<li><strong>IAM</strong> — Identity and Access Management (global service).</li>\n<li><strong>User / Group / Role / Policy</strong> — long-term identity / bundle / assumed\ntemporary identity / JSON permission document.</li>\n<li><strong>STS</strong> — Security Token Service (temporary credentials).</li>\n<li><strong>SSO</strong> — Single Sign-On (one login across accounts).</li>\n<li><strong>MFA</strong> — Multi-Factor Authentication.</li>\n<li><strong>KMS / CMK</strong> — Key Management Service / Customer Master Key.</li>\n<li><strong>SSE-S3 / SSE-KMS / SSE-C</strong> — S3 encryption variants by key holder.</li>\n<li><strong>TLS / ACM</strong> — transit encryption / certificate manager.</li>\n<li><strong>VPN / Direct Connect</strong> — encrypted tunnel / dedicated fiber.</li>\n<li><strong>CloudHSM</strong> — dedicated HSM (FIPS 140-2 Level 3).</li>\n<li><strong>Nitro Enclaves</strong> — isolated compute for sensitive processing.</li>\n<li><strong>CloudTrail / Config / GuardDuty</strong> — audit / history / threats.</li>\n<li><strong>Security Hub / Macie / Inspector</strong> — posture / PII discovery / vulns.</li>\n<li><strong>WAF / Shield</strong> — Layer-7 firewall / DDoS protection.</li>\n<li><strong>Artifact / Audit Manager</strong> — compliance reports / audit evidence.</li>\n<li><strong>Secrets Manager / Parameter Store</strong> — secrets with / without rotation.</li>\n</ul>\n<hr />\n<h2 id=\"11-additional-resources\">11. Additional Resources</h2>\n<ul>\n<li><a href=\"https://aws.amazon.com/compliance/shared-responsibility-model/\" target=\"_blank\" rel=\"noopener noreferrer\">Shared Responsibility Model</a></li>\n<li><a href=\"https://docs.aws.amazon.com/IAM/latest/UserGuide/introduction.html\" target=\"_blank\" rel=\"noopener noreferrer\">IAM User Guide</a></li>\n<li><a href=\"https://aws.amazon.com/iam/features/credentials/best-practices/\" target=\"_blank\" rel=\"noopener noreferrer\">IAM Best Practices</a></li>\n<li><a href=\"https://aws.amazon.com/cloudtrail/\" target=\"_blank\" rel=\"noopener noreferrer\">CloudTrail</a></li>\n<li><a href=\"https://aws.amazon.com/config/\" target=\"_blank\" rel=\"noopener noreferrer\">Config</a></li>\n<li><a href=\"https://aws.amazon.com/guardduty/\" target=\"_blank\" rel=\"noopener noreferrer\">GuardDuty</a></li>\n<li><a href=\"https://aws.amazon.com/security-hub/\" target=\"_blank\" rel=\"noopener noreferrer\">Security Hub</a></li>\n<li><a href=\"https://aws.amazon.com/kms/\" target=\"_blank\" rel=\"noopener noreferrer\">KMS</a></li>\n<li><a href=\"https://aws.amazon.com/secrets-manager/\" target=\"_blank\" rel=\"noopener noreferrer\">Secrets Manager</a></li>\n<li><a href=\"https://aws.amazon.com/artifact/\" target=\"_blank\" rel=\"noopener noreferrer\">Artifact</a></li>\n<li><a href=\"https://aws.amazon.com/compliance/\" target=\"_blank\" rel=\"noopener noreferrer\">Compliance Programs</a></li>\n</ul>\n","toc":[{"id":"aws-academy-cloud-foundations-module-4-cloud-security","text":"AWS Academy Cloud Foundations — Module 4: Cloud Security","level":1},{"id":"study-notes","text":"Study Notes","level":2},{"id":"1-shared-responsibility-the-one-model-to-memorize","text":"1. Shared Responsibility — The One Model to Memorize","level":2},{"id":"2-iam-the-gate-to-everything","text":"2. IAM — The Gate to Everything","level":2},{"id":"credentials-by-use","text":"Credentials, by use","level":3},{"id":"principles-that-win-marks","text":"Principles that win marks","level":3},{"id":"3-hardening-a-new-account-do-this-first-in-this-order","text":"3. Hardening a New Account — Do This First, In This Order","level":2},{"id":"4-protecting-data-three-states-three-toolkits","text":"4. Protecting Data — Three States, Three Toolkits","level":2},{"id":"s3-encryption-options-compared-by-key-holder","text":"S3 encryption options, compared by key holder","level":3},{"id":"5-kms-secrets-and-certificates-know-which-solves-what","text":"5. KMS, Secrets, and Certificates — Know Which Solves What","level":2},{"id":"6-threat-detection-amp-security-services-one-liners-that-decide-scenarios","text":"6. Threat Detection &amp; Security Services — One-Liners That Decide Scenarios","level":2},{"id":"7-compliance-reports-evidence-and-programs","text":"7. Compliance — Reports, Evidence, and Programs","level":2},{"id":"8-knowledge-check","text":"8. Knowledge Check","level":2},{"id":"9-key-takeaways","text":"9. Key Takeaways","level":2},{"id":"10-glossary","text":"10. Glossary","level":2},{"id":"11-additional-resources","text":"11. Additional Resources","level":2}],"words":2224,"assets":[]};