window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["cicd-137-website-audit-content-display-ui-ux-all-required-modificatio"]={"html":"<h1 id=\"website-audit-content-display-ui-ux-all-required-modifications\">Website Audit — Content, Display, UI/UX — All Required Modifications</h1>\n<p><strong>Date:</strong> 2026-09-14\n<strong>Scope:</strong> live site <code>https://eslamnabawy.github.io/cicd-by-nabawy/</code> + source <code>website/dist/</code>, <code>website/content/books.json</code>, <code>website/content/paths.json</code>, <code>website/scripts/build.py</code> (1753 lines), <code>GLOSSARY.md</code>\n<strong>Method:</strong> live browser pass (map, shelf, reader, book landing, lab reader, glossary, roadmap, updates) + dist/code inspection + manifest counts. Previous <code>WEBSITE_IMPROVEMENT_PLAN.md</code> (39-book era) is stale — site is now 48 books with P0/P1 partly shipped. This file supersedes it as the fix list.</p>\n<h2 id=\"resolution-log-2026-09-15-orchestrated-fix-pass-cicd-by-nabawy-source-main\">Resolution log — 2026-09-15 (orchestrated fix pass, <code>cicd-by-nabawy-source</code> <code>main</code>)</h2>\n<ul>\n<li><strong>Phase 0 (handoff TOC bug):</strong> prior session's per-heading anchors were real but incomplete — scroll-spy still tracked page-level ids (highlight died entirely) and bare <code>&lt;h2&gt;</code>/<code>&lt;h3&gt;</code> (237/511) got no ids. Fixed <code>mark_heading</code> regex, rewrote <code>tocUpdate</code> to resolve per-heading anchors (exactly one <code>.cur</code>), rebuilt reader TOC post-strip reusing injected ids (chapter <code>h2</code>s live inside stripped <code>phead</code> chrome). Verified: 62/62 reader pages, 0 dead anchors, click→1 highlight, scroll advances, mobile drawer. Commit <code>daae575</code>.</li>\n<li><strong>A1:</strong> resolved by RENAMING the shelf strip to \"★ First week — new to CI/CD\" (user decision) instead of moving books — extending the path would have gutted CI Core (5→3) and Labs. Single taxonomy preserved.</li>\n<li><strong>A2:</strong> fixed — Topic column renders real <code>read/{id}.html</code> links (50/50 via inverted MD_MAP), Link column is a <code>¶</code> permalink, counter fixed (<code>&lt;tr </code> not <code>&lt;tr&gt;</code>).</li>\n<li><strong>A5:</strong> per-book git dates at build time + ≤60-day ribbon; caveat: repo history is 2 days deep so all rows read 2026-09-14 today — mechanism correct, dates diverge going forward. Commit <code>c8c0162</code> (Phase 1).</li>\n<li><strong>B1:</strong> fixed — <code>markdown_pages</code> emits <code>&lt;li class=\"step\"&gt;</code> for <code>[ ]</code> items; existing <code>labChecks()</code> now finds them. Verified persist/reload/reset.</li>\n<li><strong>B2:</strong> fixed — fenced code emits <code>&lt;pre class=\"codeblock\" data-lang&gt;</code> (Copy/Wrap via existing codehead, lang label fixed to evaluate <code>dataset.lang</code>); pdf <code>.terminal</code> blocks got <code>white-space:pre</code>. Verified yaml label + copy + wrap + terminal render.</li>\n<li><strong>B4:</strong> done — Pillow PNG social cards (49 files, optional dep with SVG fallback), <code>og:image</code> → <code>.png</code>.</li>\n<li><strong>C1:</strong> done — 44px touch targets on reader + non-reader mobile headers, glossary gained theme toggle, no overflow at 320px. Esc/drawer behavior pre-existing. Commit <code>9a64704</code> (Phases 2+3).</li>\n<li><strong>A3:</strong> fixed — track prefixes (<code>Lab 01 · Core</code> vs <code>Lab J-01 · Jenkins</code>), ids untouched.</li>\n<li><strong>A4:</strong> fixed — roadmap rebuilt as 9-path ordered stepper (Quick topics gone), per-stop time, done-ticks from stored progress, dynamic SEO count.</li>\n<li><strong>B3:</strong> verified already-clean — tokens cover all 3 themes, full settings present (no code change).</li>\n<li><strong>B5:</strong> lightbox/lazy/alt pre-existing; added sticky thead + zebra tables.</li>\n<li><strong>C2:</strong> done — shelf 261KB → 167KB (body-free inline index + compact JSON; full index fetched over HTTP, file:// keeps light index); rails collapse at 6 with View-all toggle; filter auto-expands.</li>\n<li><strong>C3:</strong> done — ranked dropdown pre-existing; added in-reader Enter-cycling (<code>match n of m</code>) + Esc-clears.</li>\n<li><strong>C4–C8 remainder:</strong> preferences Reset button added; font preconnect ×9 heads; print forces light ink; roadmap done-ticks; shelf resume/progress pre-existing. NOT done (deferred): per-section read-dots, history row, focus-trap/<code>aria-modal</code>, drawer focus management, PWA, Lab 09, cheatsheet board, highlights/notes — propose as next batch.\n<strong>Good news:</strong> metadata model, paths, book landing pages, nested TOC, filters, related footer, updates page all live. Fixes below are what remains.</li>\n</ul>\n<hr />\n<h2 id=\"a-content-correctness-bugs-fix-first-trust-damage\">A. Content correctness bugs (fix first — trust damage)</h2>\n<h3 id=\"a1-two-different-start-here-definitions-on-the-same-page-confirmed\">A1. Two different \"Start Here\" definitions on the same page — CONFIRMED</h3>\n<ul>\n<li>Evidence: <code>books.json</code> path <code>start-here</code> = 2 books (<code>foundations</code>, <code>git-branching</code>, ~50 min). Shelf hero says \"★ START HERE — NEW TO CI/CD, 5 books · ~155 min\" (adds Pipelines + CI + Lab 01). Map page \"Start Here, 2 books · ~50 min\".</li>\n<li>Impact: user counts 2 vs 5, time 50 vs 155. Looks like drift.</li>\n<li>Fix: one source of truth. Either extend <code>paths.json</code> <code>start-here</code> to 5 and rebuild map, or rename hero to \"Suggested first week\" and keep path at 2. Rebuild + verify counts match on map/shelf/reader breadcrumbs. File: <code>website/content/paths.json</code>, <code>website/scripts/build.py</code> (<code>paths_section_html</code>, shelf hero block).</li>\n</ul>\n<h3 id=\"a2-glossary-topic-column-renders-raw-markdown-link-column-dead-confirmed\">A2. Glossary Topic column renders raw Markdown, Link column dead — CONFIRMED</h3>\n<ul>\n<li>Evidence: live glossary rows show literal <code>[Artifact Management](05-artifacts-and-packaging/artifact-management.md)</code> in Topic, <code>#</code> in Link. Header says \"0 terms · shared by books\" while table has ~50+ rows.</li>\n<li>Impact: looks broken, links unusable, term count wrong.</li>\n<li>Fix: run glossary topics through existing <code>md_inline()</code> at build; Link column should deep-link to book reader (<code>read/{id}.html</code>) or drop the column and make Term anchorable (<code>glossary.html#artifact</code>). Fix counter from parsed rows. Files: <code>build.py</code> glossary template, <code>GLOSSARY.md</code> (also verify no dupes — current parse shows 0 dupes, old Canary dupe is gone).</li>\n</ul>\n<h3 id=\"a3-lab-naming-collisions-confirmed\">A3. Lab naming collisions — CONFIRMED</h3>\n<ul>\n<li>Evidence: <code>labs</code> path (14 books) mixes <code>Lab 01–08</code> generic series with <code>Lab 01–05</code> Jenkins/descriptive titles (<code>Lab 01: First Pipeline</code> vs <code>Lab 01: Automated Integration Foundations</code>, <code>Lab 02: Build &amp; Test</code> vs <code>Lab 02: Container Delivery</code>, etc.) + <code>Jenkins Labs</code> omnibus.</li>\n<li>Impact: \"Up next\" and Related show near-duplicate titles side by side; users can't tell which track they're on.</li>\n<li>Fix: rename with track prefix at manifest level (<code>Lab 01 · Core: First Pipeline</code>, <code>Lab J-01 · Jenkins: Automated Integration…</code>) or split <code>labs</code> path into <code>labs-core</code> + <code>labs-jenkins</code>. Update <code>books.json</code> titles, <code>paths.json</code>, rebuild. No content rewrite needed.</li>\n</ul>\n<h3 id=\"a4-roadmap-page-duplicates-map-instead-of-being-a-path-confirmed\">A4. Roadmap page duplicates Map instead of being a path — CONFIRMED</h3>\n<ul>\n<li>Evidence: <code>/roadmap/</code> header reads \"MAP — Every book, one map. 48 docs grouped by category\" with a \"Quick topics · 7\" group that exists in neither <code>paths.json</code> (9 paths) nor shelf paths. It is category grouping, not ordered by dependency.</li>\n<li>Impact: Roadmap ≠ path; \"Quick topics\" is a third taxonomy (paths vs categories vs quick topics).</li>\n<li>Fix: rebuild roadmap as linear stepper in <code>paths.json</code> order with mile numbers, per-stop time + level + done check from progress; delete \"Quick topics\" grouping or define it in <code>paths.json</code> so all three pages share one taxonomy. File: <code>build.py</code> <code>build_roadmap_page</code>.</li>\n</ul>\n<h3 id=\"a5-updates-page-is-a-static-snapshot-confirmed\">A5. Updates page is a static snapshot — CONFIRMED</h3>\n<ul>\n<li>Evidence: <code>updates.html</code> header \"P0+P1 shipped Sep 2026\", all 48 rows <code>v2.0 / 2026-09-01</code>.</li>\n<li>Impact: identical dates = no real freshness signal; returning users can't see what's new.</li>\n<li>Fix: per-book <code>updated</code> from git log at build (or manual <code>CHANGELOG.md</code>), \"New/Updated\" ribbon if &lt;60 days, changelog entries on updates page. Files: <code>books.json</code> dates, <code>build.py</code> updates template.</li>\n</ul>\n<hr />\n<h2 id=\"b-reader-display-gaps-content-is-good-presentation-leaks\">B. Reader display gaps (content is good, presentation leaks)</h2>\n<h3 id=\"b1-lab-verification-checklist-is-plain-text-reset-checks-does-nothing-confirmed\">B1. Lab Verification checklist is plain text, \"Reset checks\" does nothing — CONFIRMED</h3>\n<ul>\n<li>Evidence: live <code>read/lab-01.html</code> Verification shows <code>[ ] PR shows green check…</code> as text; <code>input[type=checkbox]</code> count = 0 despite header \"Reset checks\".</li>\n<li>Impact: lab progress can't be tracked; spec's persisted lab-checkbox feature is half-shipped.</li>\n<li>Fix: render <code>[ ]</code> lines as real checkboxes persisted per book (<code>cicdlib:checks:{id}</code>), wire Reset button. File: <code>build.py</code> reader post-process + <code>READER_JS</code>.</li>\n</ul>\n<h3 id=\"b2-code-blocks-0-lt-pre-gt-on-lab-page-copy-is-unstructured-confirmed\">B2. Code blocks: 0 <code>&lt;pre&gt;</code> on lab page, copy is unstructured — CONFIRMED</h3>\n<ul>\n<li>Evidence: workflow YAML renders inside generic block with a bare \"Copy\" prefixing the whole text; no <code>&lt;pre&gt;</code>, no language label, no wrap toggle. Copy likely grabs prose + code together.</li>\n<li>Impact: copy-paste breaks YAML indentation; no distinction between command vs output.</li>\n<li>Fix: emit semantic <code>&lt;pre&gt;&lt;code class=\"lang-yaml\"&gt;</code> at build from PDF code extraction; header bar (lang + filename + Copy-code-only + Wrap); terminal blocks split <code>data-cmd</code> (copied) vs output (dimmed, not copied). Files: <code>build.py</code> <code>parse_book</code>/<code>scope_css</code>, <code>READER_JS</code>.</li>\n</ul>\n<h3 id=\"b3-light-theme-leaks-no-sepia-in-reader-chrome-partially-shipped\">B3. Light theme leaks + no Sepia in reader chrome (partially shipped)</h3>\n<ul>\n<li>Evidence: <code>build.py</code> has <code>sepia</code> ×112 and <code>[data-theme=light]</code> overrides, but PDF-embedded tables/diagrams/callouts historically keep dark backgrounds; reader toolbar detected has Save/Back/A+ but no visible theme control in sampled HTML (<code>themeBtn=false</code> on reader string scan).</li>\n<li>Impact: half-themed pages, contrast failures on tables/diagrams in light/sepia.</li>\n<li>Fix: scope all PDF remnants (<code>.readbody table, pre, .callout, .terminal, svg</code>) to site tokens per theme; ensure theme switcher (Dark/Light/Sepia) exists on reader, not just landing. Screenshot-diff 4 pages × 3 themes before sign-off.</li>\n</ul>\n<h3 id=\"b4-covers-are-initials-only-cf-block\">B4. Covers are initials only (\"CF\" block)</h3>\n<ul>\n<li>Evidence: book landing shows flat <code>CF</code> tile; no category-hue band, glyph, difficulty dots, or OG differentiation.</li>\n<li>Impact: library looks like an index, low visual memory; OG images are <code>.svg</code> (Twitter/Discord don't unfurl SVG — need PNG).</li>\n<li>Fix: build-time SVG covers (hue band + initial + pipeline glyph + difficulty dots) reused for card, landing, and OG; generate PNG fallbacks for <code>og:image</code>. Files: <code>build.py</code> <code>og_svg</code> + new <code>make_og.py</code>, card/landing templates.</li>\n</ul>\n<h3 id=\"b5-tables-diagrams-fragile-on-mobile-images-never-lazy\">B5. Tables/diagrams fragile on mobile; images never lazy</h3>\n<ul>\n<li>Evidence: <code>loading=lazy</code> ×0 in <code>build.py</code>; tables rely on overflow fallback; no lightbox wiring found on sampled pages (<code>lightbox</code> ×7 in code but no viewer confirmed live).</li>\n<li>Impact: architecture diagrams unreadable at 360px; shelf (259KB) + index (168KB) + search.json (125KB) all parse on load.</li>\n<li>Fix: diagram click → lightbox with zoom/pan + always-rendered caption/alt; sticky table headers + zebra + scroll-shadow hint; <code>loading=lazy</code> + width/height on all content images.</li>\n</ul>\n<hr />\n<h2 id=\"c-ui-ux-gaps\">C. UI/UX gaps</h2>\n<h3 id=\"c1-reader-has-compact-mobile-chrome-other-pages-use-old-generic-header-confirmed\">C1. Reader has compact mobile chrome; other pages use old generic header — CONFIRMED (matches UX_AUDIT.md)</h3>\n<ul>\n<li>Fix: unify single-row mobile header everywhere (logo mark + search-icon overlay + theme dot + menu). Separate TOC drawer vs Settings drawer, <code>Esc</code> closes. Then re-check 320px (wordmark currently hides by design — confirm touch targets ≥44px on device).</li>\n</ul>\n<h3 id=\"c2-shelf-density-confirmed-shelf-html-259kb-repeated-cards-rails\">C2. Shelf density — CONFIRMED (shelf.html 259KB, repeated cards/rails)</h3>\n<ul>\n<li>Fix: cap rails to 6 visible + \"View all\" (deep-link filtered shelf), collapse past paths behind \"Show path\", keep result-count live. Target shelf &lt;180KB.</li>\n</ul>\n<h3 id=\"c3-search-is-title-desc-only-on-shelf-search-json-125kb-ships-but-ranking-unclea\">C3. Search is title/desc-only on shelf; search.json (125KB) ships but ranking unclear</h3>\n<ul>\n<li>Fix: rank headings + tags above body, excerpt jump with prev/next + highlight-all, <code>Esc</code> clears; offline/<code>file://</code> fallback (inline index as <code>&lt;script&gt;</code> when fetch fails). No new deps.</li>\n</ul>\n<h3 id=\"c4-progress-scroll-only-save-exists-but-history-completed-unclear\">C4. Progress = scroll % only; Save exists but History/Completed unclear</h3>\n<ul>\n<li>Evidence: <code>bookmark</code> ×3, <code>history</code> ×5 in <a href=\"http://build.py\" target=\"_blank\" rel=\"noopener noreferrer\">build.py</a> — feature is thin.</li>\n<li>Fix: per-section read dots (seen 3s in viewport) + \"Mark complete\" per chapter; library rows: Continue (cover + % + page), History (last 6), Bookmarks; completed → \"Read again / Review cheatsheet\" instead of vanishing.</li>\n</ul>\n<h3 id=\"c5-settings-limited-theme-zoom-a-width\">C5. Settings limited (Theme, zoom-A+, width)</h3>\n<ul>\n<li>Fix: add line-height (1.6/1.75/1.9), font (Sans/Serif), Reset button. Replace <code>zoom</code> with <code>font-size: calc(18px * var(--fs))</code> so headings scale and AT text-scaling isn't broken.</li>\n</ul>\n<h3 id=\"c6-accessibility-holes-confirmed-by-code-grep\">C6. Accessibility holes — CONFIRMED by code grep</h3>\n<ul>\n<li>Missing: <code>preconnect</code> ×0 (fonts <code>@import</code> blocks render), <code>focus-trap</code> ×0, <code>aria-modal</code> ×0 (drawers trap nothing), <code>aria-current</code> ×3 only, <code>aria-label</code> on rails/arrows/search unverified, <code>--cat</code> yellows (#EAB308) on dark fail small-text contrast.</li>\n<li>Fix: <code>preconnect + display=swap + subset</code> fonts; drawer focus trap + <code>aria-modal</code>; <code>aria-current</code> on TOC; labeled rails/arrows/search; <code>--cat-ink</code> text-on-accent tokens for AA.</li>\n</ul>\n<h3 id=\"c7-seo-partial\">C7. SEO partial</h3>\n<ul>\n<li>Have: canonical, OG/Twitter tags, JSON-LD, sitemap, robots, <code>theme-color</code>, <code>prefetch</code> on book pages. Missing: OG PNGs, breadcrumb JSON-LD with prev/next in readers, <code>map.html</code> (451-byte redirect) weak as shared/standalone doc.</li>\n</ul>\n<h3 id=\"c8-print-css-thin-404-exists-but-empty-state-copy-thin\">C8. Print CSS thin; 404 exists but empty-state copy thin</h3>\n<ul>\n<li>Fix: print forces light ink, expands rails, page-breaks per <code>.page</code>, never cuts code; 404 + empty search get search box + 3 suggested books.</li>\n</ul>\n<hr />\n<h2 id=\"d-prioritized-fix-list\">D. Prioritized fix list</h2>\n<h3 id=\"p0-trust-reading-1-week\">P0 (trust + reading, ~1 week)</h3>\n<ol>\n<li>A1 Start-Here single source of truth</li>\n<li>A2 glossary markdown-link + counter fix</li>\n<li>B1 lab checkboxes real + persisted, Reset wired</li>\n<li>B2 code/terminal semantics + copy-code-only</li>\n<li>B3 light/sepia leak sweep + reader theme switcher</li>\n<li>C6 a11y minimum (focus trap, aria-current/labels, font preconnect, reduced-motion already ×5 — verify)</li>\n<li>C8 print + 404/empty states</li>\n</ol>\n<h3 id=\"p1-discovery-retention-2-weeks\">P1 (discovery + retention, ~2 weeks)</h3>\n<ol>\n<li>A3 lab rename / split paths</li>\n<li>A4 roadmap as ordered stepper, kill third taxonomy</li>\n<li>B4 real covers + OG PNGs</li>\n<li>B5 lightbox + tables + lazy images</li>\n<li>C2 shelf density + size budget</li>\n<li>C3 ranked global search + excerpt jumps + file:// fallback</li>\n<li>C4 progress v2 (section dots, history, completed)</li>\n<li>C5 settings (line-height, font, reset, no-zoom)</li>\n<li>A5 real per-book dates + changelog ribbons</li>\n</ol>\n<h3 id=\"p2-delight-scale\">P2 (delight + scale)</h3>\n<ol>\n<li>Highlights/notes (local-only), \"Was this useful?\" per page (<code>mailto:</code> prefilled, local tally)</li>\n<li>Popular-search hints from local query log</li>\n<li>Cheatsheet filterable board (Jenkins/Docker/Git/Kubectl/Terraform) with per-row copy</li>\n<li>Lab 09 proposal (GHA → ArgoCD GitOps end-to-end) to close vendor-neutral gap</li>\n<li>Contrast/Dim-OLED themes, <code>prefers-color-scheme</code> default on first visit</li>\n<li>PWA manifest (GitHub Pages HTTPS only, keep <code>file://</code> fallback documented)</li>\n</ol>\n<hr />\n<h2 id=\"e-build-qc-changes-website-scripts-build-py-scripts-qc-check-py\">E. Build/QC changes (<code>website/scripts/build.py</code>, <code>scripts/qc-check.py</code>)</h2>\n<ul>\n<li>Glossary render through <code>md_inline()</code>; assert no <code>[</code>…<code>](</code> literals in output; assert term counter &gt; 0.</li>\n<li><code>[ ]</code> → checkbox transform + <code>Reset checks</code> wiring test.</li>\n<li>Code-fence → <code>&lt;pre&gt;&lt;code class=\"lang-*\"&gt;</code> + header bar; fail on bare-\"Copy\" text nodes.</li>\n<li>Token-only theming check: no hard dark colors inside <code>.readbody</code> overrides; screenshot diff 4 pages × 3 themes.</li>\n<li>Taxonomy check: every <code>path</code> in <code>books.json</code> exists in <code>paths.json</code>; no \"Quick topics\" unless defined; shelf hero count == path count.</li>\n<li><code>loading=lazy</code> + <code>width/height</code> on content images; fonts <code>preconnect</code>; shelf size budget alert &gt;200KB.</li>\n<li>Sitemap <code>lastmod</code> from per-book <code>updated</code>; OG PNG existence check; breadcrumb JSON-LD on readers.</li>\n<li>Rebuild rule stays: any <code>pdf/*</code>, <code>books.json</code>, <code>paths.json</code>, <code>GLOSSARY.md</code>, <code>build.py</code> change → rebuild + <code>qc-check.py</code> + republish <code>dist/</code>.</li>\n</ul>\n<h2 id=\"f-verification-checklist-sign-off-per-fix-batch\">F. Verification checklist (sign-off per fix batch)</h2>\n<ul>\n<li>[ ] Map/shelf/reader Start-Here counts + minutes identical</li>\n<li>[ ] Glossary: 0 raw <code>[</code> links, counter correct, anchor per term, filter works</li>\n<li>[ ] Lab 01: checkboxes toggle + persist reload, Reset clears, copy-code-only 10/10</li>\n<li>[ ] 4 pages × 3 themes screenshot: no dark leaks, AA contrast spot-check</li>\n<li>[ ] Keyboard-only journey (library → book → reader → search → settings), axe 0 critical</li>\n<li>[ ] Lighthouse ≥95 perf/a11y on index/shelf/reader/glossary/roadmap</li>\n<li>[ ] <code>file://</code> double-click test of <code>dist/index.html</code> passes</li>\n<li>[ ] Link checker 0 broken; <code>zoom</code> fully removed</li>\n</ul>\n","toc":[{"id":"website-audit-content-display-ui-ux-all-required-modifications","text":"Website Audit — Content, Display, UI/UX — All Required Modifications","level":1},{"id":"resolution-log-2026-09-15-orchestrated-fix-pass-cicd-by-nabawy-source-main","text":"Resolution log — 2026-09-15 (orchestrated fix pass, cicd-by-nabawy-source main)","level":2},{"id":"a-content-correctness-bugs-fix-first-trust-damage","text":"A. Content correctness bugs (fix first — trust damage)","level":2},{"id":"a1-two-different-start-here-definitions-on-the-same-page-confirmed","text":"A1. Two different \"Start Here\" definitions on the same page — CONFIRMED","level":3},{"id":"a2-glossary-topic-column-renders-raw-markdown-link-column-dead-confirmed","text":"A2. Glossary Topic column renders raw Markdown, Link column dead — CONFIRMED","level":3},{"id":"a3-lab-naming-collisions-confirmed","text":"A3. Lab naming collisions — CONFIRMED","level":3},{"id":"a4-roadmap-page-duplicates-map-instead-of-being-a-path-confirmed","text":"A4. Roadmap page duplicates Map instead of being a path — CONFIRMED","level":3},{"id":"a5-updates-page-is-a-static-snapshot-confirmed","text":"A5. Updates page is a static snapshot — CONFIRMED","level":3},{"id":"b-reader-display-gaps-content-is-good-presentation-leaks","text":"B. Reader display gaps (content is good, presentation leaks)","level":2},{"id":"b1-lab-verification-checklist-is-plain-text-reset-checks-does-nothing-confirmed","text":"B1. Lab Verification checklist is plain text, \"Reset checks\" does nothing — CONFIRMED","level":3},{"id":"b2-code-blocks-0-lt-pre-gt-on-lab-page-copy-is-unstructured-confirmed","text":"B2. Code blocks: 0 &lt;pre&gt; on lab page, copy is unstructured — CONFIRMED","level":3},{"id":"b3-light-theme-leaks-no-sepia-in-reader-chrome-partially-shipped","text":"B3. Light theme leaks + no Sepia in reader chrome (partially shipped)","level":3},{"id":"b4-covers-are-initials-only-cf-block","text":"B4. Covers are initials only (\"CF\" block)","level":3},{"id":"b5-tables-diagrams-fragile-on-mobile-images-never-lazy","text":"B5. Tables/diagrams fragile on mobile; images never lazy","level":3},{"id":"c-ui-ux-gaps","text":"C. UI/UX gaps","level":2},{"id":"c1-reader-has-compact-mobile-chrome-other-pages-use-old-generic-header-confirmed","text":"C1. Reader has compact mobile chrome; other pages use old generic header — CONFIRMED (matches UX_AUDIT.md)","level":3},{"id":"c2-shelf-density-confirmed-shelf-html-259kb-repeated-cards-rails","text":"C2. Shelf density — CONFIRMED (shelf.html 259KB, repeated cards/rails)","level":3},{"id":"c3-search-is-title-desc-only-on-shelf-search-json-125kb-ships-but-ranking-unclea","text":"C3. Search is title/desc-only on shelf; search.json (125KB) ships but ranking unclear","level":3},{"id":"c4-progress-scroll-only-save-exists-but-history-completed-unclear","text":"C4. Progress = scroll % only; Save exists but History/Completed unclear","level":3},{"id":"c5-settings-limited-theme-zoom-a-width","text":"C5. Settings limited (Theme, zoom-A+, width)","level":3},{"id":"c6-accessibility-holes-confirmed-by-code-grep","text":"C6. Accessibility holes — CONFIRMED by code grep","level":3},{"id":"c7-seo-partial","text":"C7. SEO partial","level":3},{"id":"c8-print-css-thin-404-exists-but-empty-state-copy-thin","text":"C8. Print CSS thin; 404 exists but empty-state copy thin","level":3},{"id":"d-prioritized-fix-list","text":"D. Prioritized fix list","level":2},{"id":"p0-trust-reading-1-week","text":"P0 (trust + reading, ~1 week)","level":3},{"id":"p1-discovery-retention-2-weeks","text":"P1 (discovery + retention, ~2 weeks)","level":3},{"id":"p2-delight-scale","text":"P2 (delight + scale)","level":3},{"id":"e-build-qc-changes-website-scripts-build-py-scripts-qc-check-py","text":"E. Build/QC changes (website/scripts/build.py, scripts/qc-check.py)","level":2},{"id":"f-verification-checklist-sign-off-per-fix-batch","text":"F. Verification checklist (sign-off per fix batch)","level":2}],"words":2156,"assets":[]};