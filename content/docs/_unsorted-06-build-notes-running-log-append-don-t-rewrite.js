window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["_unsorted-06-build-notes-running-log-append-don-t-rewrite"]={"html":"<h1 id=\"build-notes-running-log-append-don-t-rewrite\">Build Notes (running log — append, don't rewrite)</h1>\n<h2 id=\"2026-09-17-baseline-first-chapter-astro-7-3-3-starlight-0-42-1\">2026-09-17 — baseline + first chapter (Astro 7.3.3 + Starlight 0.42.1)</h2>\n<ul>\n<li>Plan said Astro 5; actual latest is Astro 7. Pinned <code>astro@7.3.3</code>, <code>@astrojs/starlight@0.42.1</code> (save-exact).</li>\n<li><code>src/content.config.ts</code> with <code>docsLoader()</code> + <code>docsSchema()</code> is REQUIRED (content layer). Without it: \"collection docs does not exist\".</li>\n<li>MDX: HTML comments <code>&lt;!-- --&gt;</code> are build errors. Use <code>{/* */}</code> everywhere (incl. migration provenance headers).</li>\n<li>Locales: single-language site MUST use <code>locales: { root: {...} }</code>. Named <code>locales: { en }</code> without root makes sidebar slug lookup resolve under <code>en/…</code> → \"slug does not exist\" while the page itself renders fine (see <code>slugToLocale</code> in Starlight: falls back to <code>defaultLocale.locale</code> when no <code>root</code> key).</li>\n<li>npm on this machine: <code>npm.ps1</code> blocked by execution policy; use full path <code>C:\\Users\\eslam\\AppData\\Local\\hermes\\node\\npm.cmd</code>. Node v22, npm 10.</li>\n<li>Starlight ships Pagefind + sitemap automatically. Sidebar <code>autogenerate</code> or explicit <code>slug</code> both fine once locale is root.</li>\n<li>Migration convention (Phase 4): <code>{/* MIGRATED from: &lt;source path&gt; (v, date) + metadata + PENDING list */}</code> header; internal links rewritten to future hub slugs (<code>/courses/&lt;course&gt;/&lt;page&gt;/</code>); mermaid kept as code fence until diagram rendering lands (Phase 6).</li>\n</ul>\n<h2 id=\"2026-09-17-terraform-level-2-start\">2026-09-17 — Terraform Level 2 start</h2>\n<ul>\n<li>Sidebar slug for a section index page: <code>index.mdx</code> inside a folder registers as the FOLDER slug (<code>courses/terraform/level2</code>), never <code>…/level2/index</code>. Reference without <code>/index</code>.</li>\n<li>Sidebar edits: never use oldString = bare group opener (<code>{</code> + <code>label:</code>) — it matches multiple groups. Always include an item line for uniqueness. (Broke Labs header twice.)</li>\n<li>Expressive-code has no <code>gitignore</code> language — use <code>txt</code> for gitignore blocks.</li>\n</ul>\n","toc":[{"id":"build-notes-running-log-append-don-t-rewrite","text":"Build Notes (running log — append, don't rewrite)","level":1},{"id":"2026-09-17-baseline-first-chapter-astro-7-3-3-starlight-0-42-1","text":"2026-09-17 — baseline + first chapter (Astro 7.3.3 + Starlight 0.42.1)","level":2},{"id":"2026-09-17-terraform-level-2-start","text":"2026-09-17 — Terraform Level 2 start","level":2}],"words":270,"assets":[]};