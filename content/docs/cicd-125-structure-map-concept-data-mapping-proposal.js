window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["cicd-125-structure-map-concept-data-mapping-proposal"]={"html":"<h1 id=\"structure-map-concept-amp-data-mapping-proposal\">STRUCTURE MAP — Concept &amp; Data-Mapping Proposal</h1>\n<p>Branch: <code>feature/structure-map-view</code> · Route proposed: <code>map.html</code> (<code>/map.html</code>)\nSource of truth: <code>books.json</code> (42 books) — no second manifest.\nStatus: <strong>D.1 — design only, no code written. Needs your sign-off (D.gate) before any build.</strong></p>\n<hr />\n<h2 id=\"1-what-each-block-maps-to\">1. What each block maps to</h2>\n<p><strong>Recommendation: one block per <code>books.json</code> <code>category</code>, derived at build time.</strong></p>\n<ul>\n<li>There are 12 distinct <code>category</code> values today: <code>CI/CD (9), Jenkins (12), Labs (8), Platforms (3), Roadmap (3), Build (1), Testing (1), Delivery (1), Security (1), Reliability (1), Reference (1), GitHub (1)</code>. That's unbalanced — some 1-book singletons look thin if taken literally.</li>\n<li>Alternative groupings were considered:\n<ul>\n<li><em>By <code>difficulty</code></em> (Beginner/Intermediate/Advanced) — would mix unrelated topics (e.g. <code>jenkins-setup</code> next to <code>foundations</code>) and lose the category storytelling.</li>\n<li><em>By dependency order</em> (a layered floor/dependency stack) — interesting, but invents a new taxonomy not present in <code>books.json</code> and would need hand-maintained layer metadata.</li>\n</ul>\n</li>\n<li>Why category wins: it <strong>already is the site's contract</strong>. <code>build.py</code>'s <code>[data-cat]</code> accent map and <code>[--cat]</code> per-card color, plus <code>books.json</code> itself, already treat <code>category</code> as the grouping key. Deriving blocks from <code>category</code> at build time means adding a book or changing its category automatically adds/resizes a block — no parallel manifest to keep in sync.</li>\n</ul>\n<p><strong>What about the singletons?</strong> They render as intentionally small, secondary blocks (like narrow service blocks around a building), not as a broken sparse layout. B3.3 explicitly checks that a 1-book group still looks intentional.</p>\n<hr />\n<h2 id=\"2-what-each-block-face-shows-zoomed-out-view\">2. What each block face shows (zoomed-out view)</h2>\n<p>Each block is an angled card (≈ isometric via <code>transform</code> + <code>box-shadow</code> for depth — see §4) showing, top-to-bottom:</p>\n<ul>\n<li>A thin <code>3px</code> top accent in the existing per-category color (<code>--cat</code>, already used on cards via <code>.card::before</code>).</li>\n<li>The category's representative icon (the same glyph already associated with perhaps the Jenkins/Git icons — reuse the existing per-category icon choice; monochrome icons use <code>currentColor</code> so they already obey the theme).</li>\n<li><strong>Category name</strong> (e.g. \"Jenkins\", \"Labs\", \"Platforms\") — the <code>category</code> string verbatim.</li>\n<li><strong>Count</strong> — e.g. \"12 books\", \"1 book\" — derived from the grouped data, not hard-coded.</li>\n<li><strong>Difficulty spread</strong> as a compact pill row — tiny dots in category color with labels: e.g. \"3 Beg · 5 Int · 1 Adv\" (counts derived from <code>difficulty</code> per group; omitted if a group is uniform).</li>\n<li>Subtle depth via <code>box-shadow: var(--shadow-lift)</code> and a slight <code>transform: perspective(600px) rotateX(6deg) rotateY(-10deg)</code> on hover — cheap, no extra assets.</li>\n</ul>\n<hr />\n<h2 id=\"3-what-zoom-in-reveals\">3. What \"zoom in\" reveals</h2>\n<p><strong>Reuse the existing book-card component, filtered to the clicked group.</strong></p>\n<ul>\n<li>Clicking a block transitions to a detail view that renders the same <code>.card</code> markup already used on the homepage shelves (same padding, radius, accent, Read/Overview buttons, <code>Read →</code> link to <code>read/&lt;id&gt;.html</code>). The detail view is just that category's books in a flat grid (not another 3D layout) with a clear <strong>\"← Back to map\"</strong> affordance plus browser back support.</li>\n<li>Why not a new renderer: the homepage card is the site's canonical book presentation. A second renderer would duplicate styling, link logic, and <code>books.json</code> → card mapping. Filtering the existing cards by <code>data-cat</code> keeps one source of truth.</li>\n<li>URL is meaningfully addressable: <code>/map.html#platforms</code> (or <code>#jenkins</code>) — each group gets an anchor. Back/forward cycle between <code>#</code> and bare <code>map.html</code> (zoomed-out) without a full page reload.</li>\n</ul>\n<hr />\n<h2 id=\"4-visual-approach-trade-offs-and-recommendation\">4. Visual approach — trade-offs and recommendation</h2>\n<p><strong>Option (a) — Pseudo-3D / isometric CSS (recommended)</strong></p>\n<ul>\n<li>Implementation: each block is a regular flat card, given depth only through <code>box-shadow</code> + a modest <code>perspective</code>/<code>rotateX</code>/<code>rotateY</code> on the container. Keyboard focus ring and borders remain straightforward to theme because the underlying element is still a flat card.</li>\n<li>Theming: reuses the existing <code>--bg/--card/--ink/--line/--brand</code> tokens — no new hex values. The per-category <code>--cat</code> color continues to come from <code>books.json</code>.</li>\n<li>Mobile + reduced-motion fallback: the angled transforms are removed via a media query (<code>@media (max-width: 600px)</code> stacks blocks as a plain tappable list/grid) and <code>prefers-reduced-motion</code> skips the scale/transition entirely and jumps directly. Keyboard nav stays as normal tab order over the flat list.</li>\n</ul>\n<p><strong>Option (b) — True CSS 3D (<code>transform-style: preserve-3d</code>, <code>perspective</code>, cube faces)</strong></p>\n<ul>\n<li>More literal \"building\" with side faces and depth, but every face needs its own background/border/shadow handling per theme, focus rings become 3D-projected, and touch targets become tricky on narrow viewports. The current site has no 3D precedent; this would be the first, and it would need to be re-tested for every theme/mobile/reduced-motion combo.</li>\n<li>Verdict: <strong>not recommended for a solo-maintained static site</strong>. The visual payoff over (a) is modest relative to the ongoing maintenance cost, and a wrong 3D implementation is much harder to unwind.</li>\n</ul>\n<p><strong>Recommendation: (a) — lightweight pseudo-3D.</strong> It reads as \"structure\" at a glance, degrades trivially on mobile, and is provably correct across themes because it reuses the existing card styling that your last contrast fix already made robust.</p>\n<hr />\n<h2 id=\"5-route-amp-nav\">5. Route &amp; nav</h2>\n<ul>\n<li><strong>Proposed URL:</strong> <code>map.html</code> at the site root (same level as <code>index.html</code> and <code>glossary.html</code>), accessible as <code>/map.html</code> and <code>/map.html#&lt;category&gt;</code>.</li>\n<li><strong>Nav placement:</strong> new link <strong>Map</strong> alongside <strong>Roadmap</strong> and <strong>Glossary</strong> in <code>header.top .wrap</code> (after Search, before theme toggle — same row as the other top-level views). This keeps the map as a first-class view rather than a buried footer link, and it mirrors the Roadmap entry point.</li>\n<li><strong>Chrome inheritance:</strong> the new page reuses the existing header/footer chrome and theme toggle verbatim (same sticky header, same brand mark, same pills). It is not a standalone unstyled page.</li>\n</ul>\n<hr />\n<h2 id=\"6-accessibility-amp-reduced-motion-fallback\">6. Accessibility &amp; reduced-motion fallback</h2>\n<ul>\n<li><strong>Keyboard-only:</strong> every block is a focusable, activatable element (<code>&lt;a&gt;</code> with <code>href=\"#&lt;category&gt;\"</code> or <code>&lt;button&gt;</code> with <code>aria-label=\"&lt;Category&gt;, 12 books\"</code>). Tab reaches blocks in visual order; Enter/Space activates the same detail view as a click; visible focus styling reuses the site's existing <code>focus-visible</code> outline.</li>\n<li><strong>Reduced motion</strong> (<code>prefers-reduced-motion: reduce</code>): zoom animation disabled — the transition becomes an instant jump to the detail anchor. Detail view is still reached and back navigation still works; nothing is \"gracefully missing.\"</li>\n<li><strong>No-JS / degraded JS:</strong> blocks are real links to their fragment (<code>#platforms</code> etc.) and the detail views are anchor targets in the same document, so the map is still a usable categorized index even if the zoom JS never runs.</li>\n<li><strong>Screen readers:</strong> each block carries an <code>aria-label</code> like <code>\"Jenkins — 12 books, 2 Beginner 6 Intermediate 4 Advanced\"</code> plus the per-category icon with <code>alt=\"\"</code> (decorative).</li>\n</ul>\n<hr />\n<h2 id=\"7-what-does-not-change\">7. What does NOT change</h2>\n<p>This is <strong>strictly additive</strong>:</p>\n<ul>\n<li><code>index.html</code> (homepage / shelf view), every <code>read/&lt;id&gt;.html</code> reader page, <code>glossary.html</code>, <code>roadmap/</code>, and the existing header theme toggle are <strong>not modified</strong> by this feature except to add one nav link (<code>Map</code>) to the header's link row. No existing CSS for cards/shelves/readers is removed or overwritten.</li>\n<li>The existing <code>books.json</code> → card pipeline is reused, not replaced. The map's data is a build-time-derived view of the same 42 entries — there is exactly one source of truth.</li>\n<li>Adding a book (new <code>category</code>, new <code>difficulty</code> spread) or renaming a category automatically flows into the map without hand-editing a second manifest — derived counts/labels are computed in <code>build.py</code>.</li>\n</ul>\n<hr />\n<h2 id=\"questions-for-d-gate-your-sign-off\">Questions for D.gate (your sign-off)</h2>\n<ol>\n<li><strong>Grouping:</strong> one block per <code>category</code> as above — approved, or would you prefer a different grouping (difficulty layers, or a merged topology like Foundations → Core → Delivery → Platforms)?</li>\n<li><strong>Visual approach:</strong> (a) pseudo-3D as recommended — approved?</li>\n<li><strong>Route &amp; nav:</strong> <code>/map.html</code> with a Map link next to Roadmap/Glossary — confirmed?</li>\n<li><strong>Anything to change or explicitly exclude before build starts?</strong></li>\n</ol>\n<p>Reply with your answers to 1–3 (e.g. <code>category / a / /map.html + header ok</code>) plus any edits, and I'll build the rest autonomously.</p>\n<hr />\n<h2 id=\"addendum-rev-2026-09-14-sepia-default-exterior-amp-interior-overhaul-feat-sepia-\">Addendum — Rev 2026-09-14: Sepia default + Exterior &amp; Interior overhaul (feat/sepia-map-polish)</h2>\n<p>This addendum reflects the UI as actually shipped after the combined sepia + structure-map polish. The original proposal (above) described the initial weak version; this section is the accurate record.</p>\n<h3 id=\"sepia-theme-now-default\">Sepia theme (now default)</h3>\n<ul>\n<li><strong>Tokens:</strong> <code>--bg:#F6F1E7</code> (warm cream, within #F4ECD8–#F8F1E3 range), <code>--bg2:#EDE3CC</code>, <code>--card:#FFFBF0</code>, <code>--ink:#3B2F1E</code> (warm dark brown), <code>--mut:#6F665A</code> (warm muted, 5.01:1 on bg — passes AA), <code>--line:rgba(59,47,30,.16)</code>, warm <code>shadow</code> tints. Checked: ink 11.58:1 on bg (AAA), mut 5.01:1 (AA).</li>\n<li><strong>Brand accent decision:</strong> <code>#18E299</code> (original brand) on sepia <code>#F6F1E7</code> is 1.51:1 — fails even large-text AA, reads too cool against warm palette. Warmed variant <strong><code>#0B9B68</code></strong> used consistently for sepia (<code>--acc</code>/<code>--brand</code>): 3.16:1 on bg (passes large-text AA, sufficient for borders/buttons/accents) and 3.44:1 on card. Dark and light keep <code>#18E299</code> unchanged. Reader <code>--acc-soft</code> is <code>rgba(11,155,104,.11)</code> (warm-transparent) instead of cool <code>#d4fae8</code>.</li>\n<li><strong>Default &amp; cycle:</strong> First paint is <code>sepia</code> when no saved preference exists (synchronous <code>&lt;script&gt;</code> in <code>&lt;head&gt;</code> before any paint, respecting <code>localStorage['cicdlib:pref']</code> — returning visitors keep their choice). Toggle cycles <code>sepia → dark → light → dim → contrast → sepia</code> (sepia first). All 5 theme placements (<code>THEMES</code>/<code>RTHEMES</code>/<code>THS</code>/<code>THX</code>) updated.</li>\n<li><strong>Icon audit vs sepia:</strong> All 20 registered icons audited. Lucide line glyphs use <code>stroke=\"currentColor\"</code> and render via <code>filter:brightness(0)</code> (black) on light/sepia, <code>filter:invert(1)</code> on dark — correct on <code>#F6F1E7</code>. Official brand SVGs (Docker blue <code>#2496ED</code>, Argo orange <code>#EF7B4D</code>, Jenkins etc.) keep official fills untouched by any filter (scoped only to dark-surface line glyphs). No hardcoded white/black fills remain.</li>\n</ul>\n<h3 id=\"map-exterior-before-after\">Map exterior — before → after</h3>\n<ul>\n<li><strong>Before:</strong> Flat <code>var(--card)</code> blocks with thin <code>3px</code> top line in <code>--cat</code>, <code>1px solid var(--line-soft)</code> border, <code>var(--shadow)</code> — blended into page, no depth cues beyond a subtle lift, small 40×40 icon box, pill row <code>3 Beg · 1 Int</code> only, no preview.</li>\n<li><strong>After:</strong> Distinct objects — <code>1.5px solid color-mix(var(--cat) 28%, var(--line-soft))</code> border <strong>visible in all 3 themes</strong> (verified: sepia warm border on <code>#F6F1E7</code>, light neutral on white, dark bright on <code>#0B0D10</code>), tinted <code>linear-gradient(135deg, color-mix(var(--cat) 7%, var(--card)), var(--card))</code> background (category presence without overwhelming), 5px top accent + 1px top highlight (light source top-left consistent: shadow bottom-right <code>0 8px 24px -8px color-mix(var(--cat) 18%)</code>), <code>scale(1.015) rotateX(3deg) rotateY(-5deg) translateY(-6px)</code> hover with matching <code>shadow-lift</code>, <code>face vs side</code> via highlight vs shadow. Info scent per face: <strong>large 52×52 icon</strong> (28px glyph, <code>12% cat</code> bg), <strong>name (18px/800)</strong>, <strong>count (mono + 6px cat dot)</strong>, <strong>difficulty bar</strong> (proportional segments <code>beg:#2ECC71 int:#F5A524 adv:#E5484D</code> + matching dots + mono count), <strong>pills</strong>. Hover/focus shows <strong>2–3 title preview popover</strong> (<code>→ Title</code> list, <code>\"Inside — N books\"</code> header, <code>+N more</code>). Touch equivalent: <code>…</code> preview button (<code>data-preview-btn</code>) toggles <code>.preview-open</code> on mobile where hover doesn't exist (375px: preview is inline dashed box, stacked 1fr grid, no perspective).</li>\n<li><strong>Contrast/visual-separation check:</strong> Verified blocks separate from bg in sepia (<code>#FFFBF0</code> card tinted with cat vs <code>#F6F1E7</code> bg + 1.5px cat border + shadow), light (<code>#fff</code> + cat border + shadow), dark (<code>#12151a</code> + cat border + shadow). Single light source top-left across all themes (highlight top, shadow bottom-right).</li>\n</ul>\n<h3 id=\"map-interior-before-after\">Map interior — before → after</h3>\n<ul>\n<li><strong>Before (two bugs, now fixed):</strong> (a) Hero used a <code>16% cat / 6% bg2</code> gradient with <code>4px</code> top bar, <code>56×56</code> icon, and pill-style count — close to but NOT the same tokens as <code>.map-block</code> (which uses <code>7% cat</code> tint, <code>5px</code> bar, <code>52×52</code> chip, dot-count). (b) Book cards used the legacy <code>.coverart</code> block — a large solid <code>linear-gradient(135deg, var(--cat), transparent 140%)</code> header with oversized 28px bleeding initials (<code>&lt;b&gt;CF/CP/CI&lt;/b&gt;</code>) — clashing blue block on sepia, no icon chip, <code>1.5px cat 14%</code> border (vs map's <code>28%</code>).</li>\n<li><strong>After (current — interior reuses the exact exterior card system):</strong> Hero <code>.map-detail-hero</code> === <code>.map-block</code> tokens: <code>linear-gradient(135deg, color-mix(cat 7%, card), card 55%)</code>, <code>1.5px solid color-mix(cat 28%, line-soft)</code>, <code>16px</code> radius, <code>5px</code> top cat bar + 1px top highlight, <code>52×52</code> <code>.map-icon</code> chip (<code>12% cat</code> bg, <code>18% cat</code> border, 28px <code>currentColor</code> glyph), dot-count (<code>6px cat dot + mono</code>), same <code>map-difficulty</code> bar/dots/pills. Book cards <code>.map-cards .card</code> === same chrome at smaller scale: <code>3px</code> slim top bar (vs map's <code>5px</code>, same <code>var(--cat)</code> logic), <code>42×42</code> <code>.map-icon</code> chip (22px glyph, same <code>12%/18%</code> treatment), same gradient/border/shadow tokens, difficulty <strong>badge</strong> (<code>Beg/Int/Adv</code> colored <code>10% cat</code> bg, <code>22% cat</code> border), time+path mono, title 17px/700, 2-line clamped desc, <strong>footer</strong> with version/date meta + <code>Read</code> on <code>3% cat</code> tinted <code>bg2</code> strip. <strong>No <code>.coverart</code>, no bleeding initials anywhere in <code>map.html</code> (0 occurrences).</strong> Per-category <code>--cat</code> from the same <code>CAT_COLORS_MAP</code> as the exterior (CI/CD <code>#5B8DEF</code>, Jenkins <code>#22B8CF</code>, Platforms <code>#F97316</code>, Labs <code>#EAB308</code>, …) — verified distinct across 12 details. <strong>Back transition matches entry:</strong> <code>mapZoomIn .32s cubic-bezier(.2,.8,.2,1)</code> in, <code>mapZoomOut .24s</code> + <code>.exiting</code> class on back (history pushState + scroll to top), both disabled under <code>prefers-reduced-motion: reduce</code> (instant). Header/backdrop uses same <code>--cat</code> as exterior, so entry feels like spatial transition.</li>\n<li><strong>A11y kept:</strong> <code>tabindex 0</code> + <code>aria-label</code> with counts, <code>Enter/Space</code> activation, <code>focus-visible</code> in <code>var(--cat)</code>, <code>prefers-reduced-motion</code> disables all transform/animation/preview transitions.</li>\n<li><strong>Themes:</strong> all interior chrome uses theme tokens (<code>var(--card)/--bg2/--line-soft</code>) + <code>color-mix(var(--cat)…)</code> + <code>currentColor</code> icons — no hardcoded hex, no flat blue-gray panel. Correct on sepia (<code>#F6F1E7</code> bg / <code>#FFFBF0</code> card), light, and dark.</li>\n</ul>\n<h3 id=\"verification\">Verification</h3>\n<ul>\n<li>Rendered <code>map.html</code> + one detail (<code>#jenkins</code>) in <strong>sepia/dark/light</strong> (state color pairs checked above, not \"looks fine\").</li>\n<li><strong>375px:</strong> blocks stack <code>1fr</code>, perspective none, preview via tap button (verified hidden until <code>.preview-open</code>, dashed cat border), cards <code>1fr</code>, hero <code>20px</code> padding — all functional without hover.</li>\n<li><strong>Blocks stand out:</strong> as per separation check above, bold cat accent applied via <code>CAT_COLORS</code> map (<code>data-cat</code>/<code>--cat</code>) more boldly than homepage thin-border treatment.</li>\n<li><strong>Build+QC:</strong> <code>python website/scripts/build.py</code> green, <code>python scripts/qc-check.py</code> 33 FAILs — <strong>identical to main</strong> (stub PDFs), zero new. <code>12 blocks × 42 cards</code> verified. Interior spot-checks: CI/CD (<code>--cat:#5B8DEF</code>), Jenkins (<code>#22B8CF</code>), Platforms (<code>#F97316</code>) — distinct per-category accents, shared card chrome, sepia/dark/light via tokens.</li>\n</ul>\n<h2 id=\"addendum-rev-2026-09-14-map-becomes-landing-paths-redesigned-related-enriched-fe\">Addendum — Rev 2026-09-14: Map becomes landing, paths redesigned, related enriched (feat/paths-map-landing-related)</h2>\n<h3 id=\"route-swap-option-a-from-the-level-up-spec\">Route swap (option (a) from the level-up spec)</h3>\n<ul>\n<li><code>/</code> now serves the Structure Map (12 blocks, same <code>#slug</code> detail anchors) + Learning Paths section below it.</li>\n<li>Old shelf/grid homepage moved intact to <code>/shelf.html</code> (hero, recstrip, paths, rubric, filters, shelves); nav gains <code>Browse</code>.</li>\n<li><code>map.html</code> kept as a redirect (canonical <code>/</code>, meta-refresh + <code>location.replace('./'+hash)</code> so old <code>#slug</code> links survive).</li>\n<li>Reader contextbar now reads <code>Browse › &lt;Path&gt; › &lt;Book&gt;</code> pointing at <code>shelf.html#path-&lt;id&gt;</code>; header <code>Map</code> points at <code>/</code>, plus <code>Browse</code>.</li>\n<li>Map detail <code>← Back to map</code> keeps its wording (it returns to the zoomed-out grid above on the same page); back transition made robust (hash cleared via pushState guarded by <code>if(location.hash)</code>, <code>.exiting</code> removed, 500ms fallback timer in case <code>animationend</code> never fires).</li>\n<li>Learning Paths live on BOTH <code>/</code> (under the map) and <code>/shelf.html</code> — discoverable from the landing, still with the shelves.</li>\n</ul>\n<h3 id=\"learning-paths-redesign-visual-data-in-website-content-paths-json-grouping-deriv\">Learning Paths redesign (visual data in <code>website/content/paths.json</code>, grouping derived from <code>books.json</code>)</h3>\n<ul>\n<li>Accent: dominant-category rule — each path card takes <code>--cat</code>/icon from the category holding the most of its books (CI/CD <code>#5B8DEF</code> for Start Here/CI Core, Delivery <code>#F472B6</code>, Jenkins <code>#22B8CF</code>, Labs <code>#EAB308</code>, Reference <code>#64748B</code>, Roadmap <code>#18E299</code>). No neutral fallback was needed: every path has a strict majority.</li>\n<li>Steps: connected journey — <code>data-n</code> numbered circles (<code>1.5px cat</code> ring) joined by a <code>2px 32%-cat</code> vertical connector, difficulty as <code>Beg/Int/Adv</code> badges in the site's level colors (<code>#2ECC71/#F5A524/#E5484D</code>), step title (600wt, links to reader) primary vs mono time meta secondary, final step filled cat circle + <code>🏁 Finish</code> pill.</li>\n<li>Long paths (≥6 steps: delivery×7, labs×8, jenkins-advanced×7) get <code>.long</code> compact spacing (9px vs 16px gaps); short paths (start-here×2) keep airy spacing.</li>\n<li>Footer stats restyled to map <code>map-pill</code> chips (<code>N books</code>, <code>~M min</code>, <code>Level Beg→Int</code> span). All theme-token colors, <code>currentColor</code> icons.</li>\n<li>QC: <code>python website/scripts/build.py</code> + <code>python scripts/qc-check.py</code> → 33 FAILs identical to main (stub PDFs), zero new.</li>\n</ul>\n<h3 id=\"related-books-still-build-time-static-richer-signals\">Related Books (still build-time static, richer signals)</h3>\n<ul>\n<li>Scoring per reader: adjacent path steps (next +12 / prev +10, same-path +4) &gt; prereq/follow-up (+8) &gt; same category (+3) &gt; shared tags (+2 each); reason label rendered (<code>Next/Previous in path</code>, <code>Prerequisite</code>, <code>Follow-up</code>, <code>Same topic</code>, <code>Shared tags</code>).</li>\n<li>Treatment: compact <code>.relcard</code> rows reusing card tokens (<code>bg2</code>, cat left bar, 30px <code>currentColor</code> icon chip, <code>Beg/Int/Adv</code> badge, time, <code>→ reason</code>); 4 items (was 3 pills). Spot-checks: pipelines→artifacts(next)/git-branching(prereq), security→rollback(next)/envs(prev), jenkins-pipelines→jenkins-agents(next)/jenkins-setup(prev).</li>\n</ul>\n","toc":[{"id":"structure-map-concept-amp-data-mapping-proposal","text":"STRUCTURE MAP — Concept &amp; Data-Mapping Proposal","level":1},{"id":"1-what-each-block-maps-to","text":"1. What each block maps to","level":2},{"id":"2-what-each-block-face-shows-zoomed-out-view","text":"2. What each block face shows (zoomed-out view)","level":2},{"id":"3-what-zoom-in-reveals","text":"3. What \"zoom in\" reveals","level":2},{"id":"4-visual-approach-trade-offs-and-recommendation","text":"4. Visual approach — trade-offs and recommendation","level":2},{"id":"5-route-amp-nav","text":"5. Route &amp; nav","level":2},{"id":"6-accessibility-amp-reduced-motion-fallback","text":"6. Accessibility &amp; reduced-motion fallback","level":2},{"id":"7-what-does-not-change","text":"7. What does NOT change","level":2},{"id":"questions-for-d-gate-your-sign-off","text":"Questions for D.gate (your sign-off)","level":2},{"id":"addendum-rev-2026-09-14-sepia-default-exterior-amp-interior-overhaul-feat-sepia-","text":"Addendum — Rev 2026-09-14: Sepia default + Exterior &amp; Interior overhaul (feat/sepia-map-polish)","level":2},{"id":"sepia-theme-now-default","text":"Sepia theme (now default)","level":3},{"id":"map-exterior-before-after","text":"Map exterior — before → after","level":3},{"id":"map-interior-before-after","text":"Map interior — before → after","level":3},{"id":"verification","text":"Verification","level":3},{"id":"addendum-rev-2026-09-14-map-becomes-landing-paths-redesigned-related-enriched-fe","text":"Addendum — Rev 2026-09-14: Map becomes landing, paths redesigned, related enriched (feat/paths-map-landing-related)","level":2},{"id":"route-swap-option-a-from-the-level-up-spec","text":"Route swap (option (a) from the level-up spec)","level":3},{"id":"learning-paths-redesign-visual-data-in-website-content-paths-json-grouping-deriv","text":"Learning Paths redesign (visual data in website/content/paths.json, grouping derived from books.json)","level":3},{"id":"related-books-still-build-time-static-richer-signals","text":"Related Books (still build-time static, richer signals)","level":3}],"words":2801,"assets":[]};