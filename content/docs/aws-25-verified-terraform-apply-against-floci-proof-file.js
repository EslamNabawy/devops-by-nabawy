window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["aws-25-verified-terraform-apply-against-floci-proof-file"]={"html":"<p>{/* MIGRATED from: D:\\NTI Courses Master Folder\\TerraformMaster Folder\\Terraform\\level1\\14-floci-verified-apply.md (2026-09-06)\nCourse metadata: course=terraform | level=1 | type=execution proof\nNote: source header claims COMPLETED 2026-09-06 via WSL2 workaround, while body sections still read NOT EXECUTED/PENDING. Both states preserved verbatim — verification status UNCONFIRMED during migration. Re-run steps below to confirm. */}</p>\n<h1 id=\"verified-terraform-apply-against-floci-proof-file\">Verified <code>terraform apply</code> against Floci — Proof File</h1>\n<p>Status: <strong>COMPLETED 2026-09-06 — executed inside WSL2 via <code>wsl -e bash -lc</code></strong>. Floci binary (<code>floci</code> CLI) still NOT installed (Windows <code>curl</code> SSL blocked; <code>brew</code> unavailable); Docker Desktop daemon off. Workaround: pulled <code>floci/floci:latest</code> inside WSL2 Docker (running) → container <code>floci-aws</code> (Floci v2.0.1 native) started healthy on 4566. <code>terraform init/plan/apply/destroy</code> executed (v1.16.1). <code>main.tf</code> typo (<code>flocil_verify</code> vs <code>floci_verify</code>) fixed via <code>sed</code>. Full details in source notes folder (completed copy preserved in original vault — archived, never deleted per owner rule).</p>\n<h2 id=\"what-this-file-proves-when-executed-fill-in-blanks-for-the-user-to-complete\">What this file proves when executed (fill-in blanks for the user to complete)</h2>\n<pre><code class=\"language-bash\"><span class=\"hljs-comment\"># 0. Start Floci (see Floci guide)</span>\nfloci start --detach\nfloci <span class=\"hljs-built_in\">wait</span> --<span class=\"hljs-built_in\">timeout</span> 60s\n<span class=\"hljs-built_in\">eval</span> <span class=\"hljs-string\">\"<span class=\"hljs-subst\">$(floci env)</span>\"</span>\n\n<span class=\"hljs-comment\"># 1. Init (with endpoint — must succeed only when Floci responds)</span>\n<span class=\"hljs-built_in\">cd</span> live-verify-floci\nterraform init\n<span class=\"hljs-comment\"># Expected: \"Terraform has been successfully initialized!\" + backend S3 (if configured) or local</span>\n\n<span class=\"hljs-comment\"># 2. Plan (the proof of correct endpoint setup)</span>\nterraform plan\n<span class=\"hljs-comment\"># Expected: 1 to add (aws_s3_bucket.flocil_verify), 0 change, 0 destroy</span>\n<span class=\"hljs-comment\"># If endpoint wrong / missing: hangs, times out, or attempts real AWS (check provider.tf)</span>\n\n<span class=\"hljs-comment\"># 3. Apply (the real resource creation in local emulator)</span>\nterraform apply -auto-approve\n<span class=\"hljs-comment\"># Expected: aws_s3_bucket.flocil_verify: Creating... → created. bucket_domain = ...</span>\n<span class=\"hljs-comment\"># Real: resource created inside Floci container (visible via `aws --endpoint-url ... s3 ls`)</span>\n\n<span class=\"hljs-comment\"># 4. Verify from CLI (the cross-check that the resource is real, not imaginary)</span>\naws --endpoint-url http://localhost:4566 s3 <span class=\"hljs-built_in\">ls</span>\n<span class=\"hljs-comment\"># Expected: list containing \"floci-verify-...\"</span>\n\n<span class=\"hljs-comment\"># 5. Confirm state was written (local or remote, per backend config)</span>\nterraform show\n<span class=\"hljs-comment\"># Expected: resource block with id = \"...\", bucket = \"...\"</span>\n\n<span class=\"hljs-comment\"># 6. Clean up (always)</span>\nterraform destroy -auto-approve\n<span class=\"hljs-comment\"># Expected: Destroy complete! 1 resource destroyed.</span>\n\nfloci stop --remove\n</code></pre>\n<h2 id=\"errors-you-ll-see-if-something-is-wrong-verified-shapes-from-workflow-provider-f\">Errors you'll see if something is wrong (verified shapes from workflow, provider, floci guides)</h2>\n<div class=\"table-scroll\"><table>\n<thead>\n<tr>\n<th>Command fails with...</th>\n<th>Cause</th>\n<th>Fix</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>init</code> → <code>AccessDenied</code> / hangs</td>\n<td>Floci not running / endpoint wrong</td>\n<td><code>floci start</code>; verify <a href=\"http://provider.tf\" target=\"_blank\" rel=\"noopener noreferrer\">provider.tf</a> endpoints</td>\n</tr>\n<tr>\n<td><code>plan</code> hangs forever</td>\n<td>endpoint points at non-responsive port / wrong endpoint line</td>\n<td>check <code>provider.tf</code> endpoints; <code>floci status</code></td>\n</tr>\n<tr>\n<td><code>apply</code> creates real AWS resources (unexpected bill)</td>\n<td>endpoint line missing / using real profile</td>\n<td>confirm endpoints + dummy creds; never mix real profile</td>\n</tr>\n<tr>\n<td><code>apply</code> succeeds but <code>aws s3 ls</code> empty</td>\n<td>created in Floci but list command hits wrong endpoint / wrong region</td>\n<td>match endpoint URL to <code>provider.tf</code>; use same <code>AWS_ACCESS_KEY_ID=test</code> env</td>\n</tr>\n<tr>\n<td><code>destroy</code> → <code>No changes</code> but bucket exists</td>\n<td>state lost / backend mis-configured / two different backend files</td>\n<td><code>terraform import</code> back; fix backend block</td>\n</tr>\n</tbody>\n</table></div>\n<h2 id=\"actual-verification-state-not-executed\">Actual verification state: NOT EXECUTED</h2>\n<ul>\n<li><code>floci</code> binary: <code>Not Found</code> (see install in <a href=\"/courses/terraform/level1/floci/\">Floci</a> CLI section).</li>\n<li>Port 4566: unreachable (<code>URLError</code>).</li>\n<li><code>live-verify-floci/</code> folder: exists with <code>provider.tf</code> + <code>main.tf</code>.</li>\n<li>Proof file (this file): created, tracking blanks.</li>\n<li>Action required: install Floci (<code>brew</code> / <code>curl ... | sh</code> / <code>iwr ...</code>), run steps 0–6 above, replace <code>NOT EXECUTED</code> with <code>COMPLETED</code>, and include the <code>plan</code> and <code>apply</code> output lines.</li>\n</ul>\n<h2 id=\"reference-commands-copy-paste-one-per-line-verified-syntax-from-core-provider-fl\">Reference commands (copy-paste, one per line, verified syntax from core, provider, floci, state guides)</h2>\n<pre><code class=\"language-bash\">floci start --detach\nfloci <span class=\"hljs-built_in\">wait</span> --<span class=\"hljs-built_in\">timeout</span> 60s\nea$(floci <span class=\"hljs-built_in\">env</span>)  <span class=\"hljs-comment\"># or: eval $(floci env) in bash</span>\nterraform init\nterraform plan\nterraform apply -auto-approve\naws --endpoint-url http://localhost:4566 s3 <span class=\"hljs-built_in\">ls</span>\nterraform show\nterraform destroy -auto-approve\nfloci stop --remove\n</code></pre>\n<hr />\n<p>Created: 2026-09-06 | Verified against: provider, vpc, floci, core/plugins, state guides | Execution: PENDING (requires Floci installation)</p>\n","toc":[{"id":"verified-terraform-apply-against-floci-proof-file","text":"Verified terraform apply against Floci — Proof File","level":1},{"id":"what-this-file-proves-when-executed-fill-in-blanks-for-the-user-to-complete","text":"What this file proves when executed (fill-in blanks for the user to complete)","level":2},{"id":"errors-you-ll-see-if-something-is-wrong-verified-shapes-from-workflow-provider-f","text":"Errors you'll see if something is wrong (verified shapes from workflow, provider, floci guides)","level":2},{"id":"actual-verification-state-not-executed","text":"Actual verification state: NOT EXECUTED","level":2},{"id":"reference-commands-copy-paste-one-per-line-verified-syntax-from-core-provider-fl","text":"Reference commands (copy-paste, one per line, verified syntax from core, provider, floci, state guides)","level":2}],"words":385,"assets":[]};