window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["cicd-120-orchestrator-cicd-by-nabawy-level-up"]={"html":"<h1 id=\"orchestrator-cicd-by-nabawy-level-up\">Orchestrator — CICD BY Nabawy Level-Up</h1>\n<p>This is the single entry point. Paste the block below into a Claude Code\nsession opened at the root of <code>cicd-by-nabawy-source</code>, with <code>tasks.yaml</code>\n(same folder as this file) also placed at the repo root or referenced by\npath. Claude Code then drives the entire plan itself — reading the\nmanifest, executing tasks in dependency order, checking each gate, and\nstopping cleanly at anything that needs a human.</p>\n<p>You do not run the Phase 1–4 prompts by hand. You run this once, then\nrespond to whatever it stops on.</p>\n<hr />\n<h2 id=\"the-orchestration-prompt\">The orchestration prompt</h2>\n<pre><code>You are orchestrating a multi-phase project using the task manifest at\ntasks.yaml. Read it fully before doing anything.\n\nRULES:\n1. Execute tasks in dependency order (respect `depends_on`). Never start a\n   task whose dependencies haven't passed their gate.\n2. For each task: announce its id<span class=\"hljs-built_in\"> and </span>name, run its `action`<span class=\"hljs-built_in\"> and/or\n</span>   `prompt` as written, then evaluate its `gate`.\n3. Gate types<span class=\"hljs-built_in\"> and </span>how to handle them:\n   - exit_code_zero_and_output_contains: run the action,<span class=\"hljs-built_in\"> check </span>the command\n     exited 0 AND stdout contains the pattern. Pass/fail, no judgment call.\n   - file_exists:<span class=\"hljs-built_in\"> check </span>the file was actually written,<span class=\"hljs-built_in\"> not </span>just claimed.\n   - build_and_visual_diff / build_and_spotcheck / query_test /\n     build_and_link_check: run the rebuild, then perform the described\n    <span class=\"hljs-built_in\"> check </span>yourself (fetch the rendered page, grep the anchors, run the\n     search queries) — do<span class=\"hljs-built_in\"> not </span>mark this passed on the basis of <span class=\"hljs-string\">\"I made the\n     change\"</span> alone. Show me the evidence (the fetched HTML snippet, the\n     query results) as part of your pass/fail statement.\n   - manual_approval / manual_approval_for_deletions / manual_checklist /\n     per_file_confirmation: STOP. Present exactly what needs review (diff,\n     list,<span class=\"hljs-built_in\"> or </span>checklist)<span class=\"hljs-built_in\"> and </span>wait for my explicit go-ahead before marking\n     the task's gate passed<span class=\"hljs-built_in\"> and </span>moving to the next task. Do<span class=\"hljs-built_in\"> not </span>proceed\n     past a manual gate on your own judgment, even<span class=\"hljs-built_in\"> if </span>the change looks\n     obviously fine to you.\n4. On any failed gate: stop, report which task failed<span class=\"hljs-built_in\"> and </span>why,<span class=\"hljs-built_in\"> and </span>follow\n   its `on_fail` instruction<span class=\"hljs-built_in\"> if </span>present. Do<span class=\"hljs-built_in\"> not </span>silently skip to the next\n   task<span class=\"hljs-built_in\"> or </span>improvise a different fix without telling me first.\n5. Maintain a running STATUS.md at the repo root: one line per task\n   (id, name, status: pending/in-progress/passed/failed/blocked-on-human),\n   updated after every task. This is the source of truth<span class=\"hljs-built_in\"> if </span>this session\n   is interrupted — on resume, read STATUS.md first<span class=\"hljs-built_in\"> and </span>continue from the\n   first non-passed task rather than restarting.\n6. Never touch `pdf/`<span class=\"hljs-built_in\"> or </span>`website/dist/` by hand — only via\n   `website/scripts/build.py`. Never edit anything on `main` directly —\n   all work happens on the branch created in P0.1.\n7. Treat `AUDIT_STRUCTURE.md`<span class=\"hljs-built_in\"> and </span>`AUDIT_UX.md` (produced in Phase 1) as\n   the sole source of scope for Phase 2<span class=\"hljs-built_in\"> and </span>Phase 3 tasks — don't expand\n   scope beyond what those audits + their approved <span class=\"hljs-string\">\"top 5\"</span> surfaced.\n8. Batch related file edits into single diffs per task rather than one\n   commit per line — but keep Phase 2 (UI/UX) changes<span class=\"hljs-built_in\"> and </span>Phase 3\n   (content) changes in separate commits, so they can be bisected\n   independently<span class=\"hljs-built_in\"> if </span>something breaks post-deploy.\n9. When a task's gate is `build_success`<span class=\"hljs-built_in\"> or </span>similar automated checks pass\n   but you're<span class=\"hljs-built_in\"> not </span>fully confident the change matches intent, say so\n   explicitly rather than marking it passed — surface the uncertainty as\n   part of your report,<span class=\"hljs-built_in\"> and </span>let me decide.\n\nBegin with P0.1. After each task completes (gate passed), print the\nupdated STATUS.md table, then proceed automatically to the next\nready task — except for manual_* gates, where you stop<span class=\"hljs-built_in\"> and </span>wait for me.\n</code></pre>\n<hr />\n<h2 id=\"what-fully-orchestrated-means-here-concretely\">What \"fully orchestrated\" means here, concretely</h2>\n<ul>\n<li><strong>One command to start it</strong>, not 14 prompts pasted by hand.</li>\n<li><strong>Dependency-ordered execution</strong> — <code>tasks.yaml</code>'s <code>depends_on</code> is the\nscheduler; Claude Code won't jump ahead.</li>\n<li><strong>Machine-checkable gates where possible</strong> (file exists, build exits 0,\na search query returns a hit) instead of \"looks good to me.\"</li>\n<li><strong>Explicit human checkpoints</strong> at the points that actually matter: after\naudits set scope (P1.gate), after each destructive/subjective content\nchange (glossary deletions, difficulty reclassification), and before\nmerging to <code>main</code> / deploying (P4.3). Nothing auto-merges or auto-deploys.</li>\n<li><strong>A resumable state file</strong> (<code>STATUS.md</code>) so a dropped session doesn't\nmean starting over — this matters because Phase 3 alone touches all 39\nbooks and is likely to span multiple sessions.</li>\n<li><strong>Separated blast radius</strong> — UI/UX commits and content commits stay\nseparate, so a bad UI change doesn't force reverting content fixes too.</li>\n</ul>\n<h2 id=\"files-in-this-bundle\">Files in this bundle</h2>\n<ul>\n<li><code>ORCHESTRATOR.md</code> (this file) — the prompt you actually run.</li>\n<li><code>tasks.yaml</code> — the manifest it executes. Edit this directly if you want\nto add/remove/reorder tasks; the orchestrator prompt reads it fresh each\ntime, so changes take effect on the next run without touching the prompt.</li>\n</ul>\n<h2 id=\"running-it-again-after-an-interruption\">Running it again after an interruption</h2>\n<p>Open a new Claude Code session in the repo, and simply say:</p>\n<pre><code>Resume orchestration. Read STATUS.md <span class=\"hljs-keyword\">and</span> tasks.yaml, <span class=\"hljs-keyword\">continue</span> <span class=\"hljs-keyword\">from</span> <span class=\"hljs-keyword\">the</span>\n<span class=\"hljs-keyword\">first</span> task <span class=\"hljs-keyword\">that</span> <span class=\"hljs-keyword\">isn't</span> marked passed.\n</code></pre>\n<h2 id=\"if-you-want-to-run-only-one-phase\">If you want to run only one phase</h2>\n<pre><code><span class=\"hljs-keyword\">Read</span> tasks.yaml. <span class=\"hljs-keyword\">Execute</span> <span class=\"hljs-keyword\">only</span> tasks <span class=\"hljs-keyword\">in</span> phase P2, <span class=\"hljs-keyword\">in</span> dependency <span class=\"hljs-keyword\">order</span>,\n<span class=\"hljs-keyword\">following</span> <span class=\"hljs-keyword\">all</span> the same rules <span class=\"hljs-keyword\">as</span> <span class=\"hljs-keyword\">full</span> orchestration (gates, STATUS.md,\nmanual stops). Skip phases <span class=\"hljs-keyword\">not</span> requested.\n</code></pre>\n","toc":[{"id":"orchestrator-cicd-by-nabawy-level-up","text":"Orchestrator — CICD BY Nabawy Level-Up","level":1},{"id":"the-orchestration-prompt","text":"The orchestration prompt","level":2},{"id":"what-fully-orchestrated-means-here-concretely","text":"What \"fully orchestrated\" means here, concretely","level":2},{"id":"files-in-this-bundle","text":"Files in this bundle","level":2},{"id":"running-it-again-after-an-interruption","text":"Running it again after an interruption","level":2},{"id":"if-you-want-to-run-only-one-phase","text":"If you want to run only one phase","level":2}],"words":326,"assets":[]};