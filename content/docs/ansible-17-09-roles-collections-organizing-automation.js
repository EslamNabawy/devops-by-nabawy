window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["ansible-17-09-roles-collections-organizing-automation"]={"html":"<p>{/* MIGRATED from: D:\\NTI Courses Master Folder\\ansible content\\content\\09-roles-and-collections.md\nCourse metadata: course=ansible | ansible-core 2.16+, FQCN, Python 3.11+ */}</p>\n<h1 id=\"09-roles-amp-collections-organizing-automation\">09 — Roles &amp; Collections: Organizing Automation</h1>\n<p>{/* icon: ansible | placement: chapter-opener <em>/}\n{/</em> asset: diagram-role-structure | placement: after-section-introduction | purpose: role directory layout <em>/}\n{/</em> icon: cloud | placement: callout | purpose: Galaxy distribution */}</p>\n<h2 id=\"what-reusable-units-and-their-distribution\">WHAT — Reusable units and their distribution</h2>\n<p>Role = reusable unit with conventional directories (tasks, handlers, templates, defaults, vars). Collection = versioned distribution of roles plus modules and plugins, addressed by fully qualified names (<code>namespace.collection</code>).</p>\n<pre><code>roles/web/\n  tasks/<span class=\"hljs-selector-tag\">main</span><span class=\"hljs-selector-class\">.yml</span>\n  handlers/<span class=\"hljs-selector-tag\">main</span><span class=\"hljs-selector-class\">.yml</span>\n  templates/nginx<span class=\"hljs-selector-class\">.conf</span><span class=\"hljs-selector-class\">.j2</span>\n  defaults/<span class=\"hljs-selector-tag\">main</span><span class=\"hljs-selector-class\">.yml</span>\n  vars/<span class=\"hljs-selector-tag\">main</span><span class=\"hljs-selector-class\">.yml</span>\n  meta/<span class=\"hljs-selector-tag\">main</span><span class=\"hljs-selector-class\">.yml</span>\n</code></pre>\n<pre><code class=\"language-yaml\"><span class=\"hljs-bullet\">-</span> <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">Configure</span> <span class=\"hljs-string\">web</span> <span class=\"hljs-string\">fleet</span>\n  <span class=\"hljs-attr\">hosts:</span> <span class=\"hljs-string\">web</span>\n  <span class=\"hljs-attr\">roles:</span>\n    <span class=\"hljs-bullet\">-</span> <span class=\"hljs-attr\">role:</span> <span class=\"hljs-string\">web</span>\n      <span class=\"hljs-attr\">vars:</span> { <span class=\"hljs-attr\">app_port:</span> <span class=\"hljs-number\">8080</span> }\n</code></pre>\n<p>Install: <code>ansible-galaxy collection install community.general</code>. FQCN in tasks: <code>community.general.ufw</code>, <code>ansible.builtin.template</code>.</p>\n<h2 id=\"why-playbooks-do-not-scale-flat\">WHY — Playbooks do not scale flat</h2>\n<p>One <code>site.yml</code> with 200 tasks is unreviewable: variables collide, handlers duplicate, copy-paste spreads. Roles give boundaries (defaults vs vars, tasks vs handlers); collections give versioning and sharing (pin <code>requirements.yml</code>, upgrade deliberately). Galaxy and private automation hub become the package registry for automation.</p>\n<h2 id=\"where-does-it-fit-organization-after-mechanics\">WHERE DOES IT FIT — Organization after mechanics</h2>\n<pre><code>Play → task → module <span class=\"hljs-punctuation\">(</span><span class=\"hljs-type\">Ch</span> <span class=\"hljs-number\">04</span><span class=\"hljs-punctuation\">)</span> + data <span class=\"hljs-punctuation\">(</span><span class=\"hljs-type\">Ch</span> <span class=\"hljs-number\">06</span><span class=\"hljs-punctuation\">)</span> + logic <span class=\"hljs-punctuation\">(</span><span class=\"hljs-type\">Ch</span> <span class=\"hljs-number\">07</span><span class=\"hljs-punctuation\">)</span> + files <span class=\"hljs-punctuation\">(</span><span class=\"hljs-type\">Ch</span> <span class=\"hljs-number\">08</span><span class=\"hljs-punctuation\">)</span>\n  → role bundles them → collection ships them → ansible.cfg/CLI locates them\n</code></pre>\n<p><code>defaults/main.yml</code> = lowest precedence (safe to override). <code>vars/main.yml</code> = high precedence (role asserts). <code>requirements.yml</code> pins collections per project — same discipline as <code>requirements.txt</code>.</p>\n<h2 id=\"how-it-works-resolution-and-inclusion\">HOW IT WORKS — Resolution and inclusion</h2>\n<p>Role search path: playbook <code>roles/</code> dir → <code>roles_path</code> in <code>ansible.cfg</code> → Galaxy default. Task inside role resolves <code>templates/</code> and <code>files/</code> relative to the role. <code>import_role</code> is static (parsed when playbook loads — <code>when</code> applies per task inside). <code>include_role</code> is dynamic (evaluated at execution — can loop over roles).</p>\n<pre><code class=\"language-yaml\"><span class=\"hljs-bullet\">-</span> <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">Static</span> <span class=\"hljs-string\">web</span> <span class=\"hljs-string\">role</span>\n  <span class=\"hljs-attr\">ansible.builtin.import_role:</span>\n    <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">web</span>\n\n<span class=\"hljs-bullet\">-</span> <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">Dynamic</span> <span class=\"hljs-string\">per-env</span> <span class=\"hljs-string\">roles</span>\n  <span class=\"hljs-attr\">ansible.builtin.include_role:</span>\n    <span class=\"hljs-attr\">name:</span> <span class=\"hljs-string\">\"<span class=\"hljs-template-variable\">{{ item }}</span>\"</span>\n  <span class=\"hljs-attr\">loop:</span> [<span class=\"hljs-string\">base</span>, <span class=\"hljs-string\">web</span>]\n</code></pre>\n<p>Collections: <code>ansible-galaxy collection install -r requirements.yml</code>; reference <code>namespace.collection.module</code>. Short names (<code>template</code>, <code>package</code>) still resolve today but are obsolete — lint flags them, content uses FQCN throughout.</p>\n<p><code>ansible.cfg</code> loading order: <code>ANSIBLE_CONFIG</code> env → <code>./ansible.cfg</code> → <code>~/.ansible.cfg</code> → <code>/etc/ansible/ansible.cfg</code>. Keep project-local <code>ansible.cfg</code> (<code>roles_path</code>, <code>inventory</code>, <code>forks</code>) committed beside <code>site.yml</code>.</p>\n<h2 id=\"practical-convert-play-to-role\">PRACTICAL — Convert play to role</h2>\n<pre><code>COMMAND\n────────────────────────\nansible-galaxy role init roles/web\nansible-playbook -<span class=\"hljs-selector-tag\">i</span> inventory<span class=\"hljs-selector-class\">.ini</span> site<span class=\"hljs-selector-class\">.yml</span> <span class=\"hljs-attr\">--check</span> <span class=\"hljs-attr\">--diff</span>\n</code></pre>\n<p>Meaning: scaffold conventional layout, then dry-run role-based play. Expected: same <code>changed/ok</code> counts as flat play — proves refactor preserved behavior.</p>\n<p>Why it matters: refactor without behavior change is the organization proof. Common failure: <code>couldn't resolve module/action 'my_module'</code> → collection not installed or FQCN missing → <code>ansible-galaxy collection install -r requirements.yml</code>.</p>\n<h2 id=\"what-can-fail\">WHAT CAN FAIL</h2>\n<div class=\"table-scroll\"><table>\n<thead>\n<tr>\n<th>Symptom</th>\n<th>Likely Cause</th>\n<th>Prevention</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>couldn't resolve module/action</code></td>\n<td>Collection missing or short name</td>\n<td>FQCN + <code>requirements.yml</code> + <code>ansible-galaxy install -r</code></td>\n</tr>\n<tr>\n<td>Role not found</td>\n<td><code>roles_path</code> wrong or role beside wrong playbook</td>\n<td><code>ansible-config dump | grep -i roles_path</code>; keep <code>roles/</code> beside <code>site.yml</code></td>\n</tr>\n<tr>\n<td><code>import_*</code> vs <code>include_*</code> surprise</td>\n<td>Static vs dynamic timing</td>\n<td><code>import</code> for fixed structure; <code>include</code> for loops/conditionals on the include itself</td>\n</tr>\n<tr>\n<td>Vars collision across roles</td>\n<td>Same var name in two roles</td>\n<td>Namespace vars (<code>web_app_port</code>), defaults for overridables</td>\n</tr>\n</tbody>\n</table></div>\n<pre><code>GOTCHA\n<span class=\"hljs-string\">`defaults/`</span> loses <span class=\"hljs-keyword\">to</span> almost everything; <span class=\"hljs-string\">`vars/`</span> beats almost everything.\nPut tunables <span class=\"hljs-keyword\">in</span> defaults, assertions <span class=\"hljs-keyword\">in</span> vars —\nreversing them makes overrides silently <span class=\"hljs-keyword\">fail</span>.\n</code></pre>\n<h2 id=\"real-world-connection\">REAL-WORLD CONNECTION</h2>\n<p>Platform team ships <code>acme.web</code> collection v2.4. App teams pin <code>acme.web: \"&gt;=2.0,&lt;3.0\"</code> in <code>requirements.yml</code>, override <code>acme_web_port</code> in <code>group_vars/</code>. Upgrade = bump pin, <code>--check --diff</code> in staging, promote. Roles become the API between platform and app teams.</p>\n<h2 id=\"lab-observe-execute-verify\">LAB — Observe / Execute / Verify</h2>\n<p>Objective: extract tasks into role without behavior change. Prereqs: working flat <code>site.yml</code> from Ch 04.</p>\n<ol>\n<li><code>ansible-galaxy role init roles/web</code> → Observe scaffold</li>\n<li>Move tasks/handlers/templates/vars into role dirs → point <code>site.yml</code> at <code>roles: [web]</code></li>\n<li><code>ansible-playbook -i inventory.ini site.yml --check --diff</code> → Verify identical planned changes. Explain: organization, not behavior, changed.</li>\n<li>Add <code>requirements.yml</code> with one collection → <code>ansible-galaxy collection install -r requirements.yml</code> → Verify FQCN task resolves.\nFailure drill: break <code>roles_path</code> in <code>ansible.cfg</code> → not-found error → <code>ansible-config dump</code> → fix path.</li>\n</ol>\n<h2 id=\"interview-check\">INTERVIEW CHECK</h2>\n<p><strong>Q:</strong> Role vs collection vs playbook? <code>import</code> vs <code>include</code>?</p>\n<p><strong>A:</strong> Playbook selects hosts and composes roles. Role bundles tasks/handlers/templates/vars for one concern. Collection versions and ships roles+modules+plugins. <code>import_*</code> resolves at parse (static), <code>include_*</code> at run (dynamic, loopable).</p>\n<h2 id=\"next-concept\">NEXT CONCEPT</h2>\n<p>Organized automation is ready to be secured. Next: privilege boundaries and secrets — become and Vault.</p>\n<h2 id=\"icon-markers\">Icon Markers</h2>\n<ul>\n<li>{/* icon: ansible */}</li>\n<li>{/* icon: cloud */}</li>\n<li>{/* icon: terminal */}</li>\n</ul>\n","toc":[{"id":"09-roles-amp-collections-organizing-automation","text":"09 — Roles &amp; Collections: Organizing Automation","level":1},{"id":"what-reusable-units-and-their-distribution","text":"WHAT — Reusable units and their distribution","level":2},{"id":"why-playbooks-do-not-scale-flat","text":"WHY — Playbooks do not scale flat","level":2},{"id":"where-does-it-fit-organization-after-mechanics","text":"WHERE DOES IT FIT — Organization after mechanics","level":2},{"id":"how-it-works-resolution-and-inclusion","text":"HOW IT WORKS — Resolution and inclusion","level":2},{"id":"practical-convert-play-to-role","text":"PRACTICAL — Convert play to role","level":2},{"id":"what-can-fail","text":"WHAT CAN FAIL","level":2},{"id":"real-world-connection","text":"REAL-WORLD CONNECTION","level":2},{"id":"lab-observe-execute-verify","text":"LAB — Observe / Execute / Verify","level":2},{"id":"interview-check","text":"INTERVIEW CHECK","level":2},{"id":"next-concept","text":"NEXT CONCEPT","level":2},{"id":"icon-markers","text":"Icon Markers","level":2}],"words":642,"assets":[]};