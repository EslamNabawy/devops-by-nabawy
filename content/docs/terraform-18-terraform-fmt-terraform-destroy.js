window.NTI=window.NTI||{};(window.NTI.docs=window.NTI.docs||{})["terraform-18-terraform-fmt-terraform-destroy"]={"html":"<p>{/* MIGRATED from: D:\\NTI Courses Master Folder\\TerraformMaster Folder\\Terraform\\level1\\23-fmt-and-destroy.md (2026-09-06)\nCourse metadata: course=terraform | level=1 | difficulty=Beginner */}</p>\n<h1 id=\"terraform-fmt-amp-terraform-destroy\">terraform fmt &amp; terraform destroy</h1>\n<p>Day 1 · Format code and clean up infrastructure</p>\n<p>Two commands at opposite ends of a resource's life: <code>fmt</code> keeps the code readable from day one, <code>destroy</code> tears everything down at the end. Run the first before every commit; run the second only when you mean it.</p>\n<h2 id=\"1-fmt-canonical-style-zero-decisions\">1. fmt — canonical style, zero decisions</h2>\n<pre><code class=\"language-hcl\"><span class=\"hljs-comment\"># BEFORE — works, but inconsistent</span><span class=\"hljs-built_in\">\nresource </span><span class=\"hljs-string\">\"aws_vpc\"</span> <span class=\"hljs-string\">\"main\"</span> {\n<span class=\"hljs-attribute\">cidr_block</span>=<span class=\"hljs-string\">\"10.0.0.0/16\"</span>\n    tags={<span class=\"hljs-attribute\">Name</span>=<span class=\"hljs-string\">\"study-vpc\"</span>}\n}\n</code></pre>\n<pre><code class=\"language-bash\">$ terraform <span class=\"hljs-built_in\">fmt</span>\nmain.tf\n</code></pre>\n<pre><code class=\"language-hcl\"><span class=\"hljs-comment\"># AFTER — aligned =, two-space indent, spacing normalized</span>\n<span class=\"hljs-attribute\">resource</span> <span class=\"hljs-string\">\"aws_vpc\"</span> <span class=\"hljs-string\">\"main\"</span> {\n  <span class=\"hljs-attribute\">cidr_block</span> = <span class=\"hljs-string\">\"10.0.0.0/16\"</span>\n  tags       = { <span class=\"hljs-attribute\">Name</span> = <span class=\"hljs-string\">\"study-vpc\"</span> }\n}\n</code></pre>\n<p><code>fmt</code> lists every file it rewrote. Check the habit into CI so style arguments never reach code review:</p>\n<pre><code class=\"language-bash\">$ terraform <span class=\"hljs-built_in\">fmt</span> -check\nmain.tf          <span class=\"hljs-comment\"># exit 1: this file needs formatting</span>\n</code></pre>\n<p><code>-check</code> fails (non-zero exit) when anything is unformatted — the pipeline gate. <code>-recursive</code> covers subfolders; <code>-diff</code> shows what would change without writing.</p>\n<h2 id=\"2-destroy-behavior-transcript-aftermath\">2. destroy — behavior, transcript, aftermath</h2>\n<pre><code class=\"language-bash\">$ terraform destroy\naws_instance.web[<span class=\"hljs-string\">\"web-a\"</span>]: Destroying... [<span class=\"hljs-built_in\">id</span>=i-012345]\naws_instance.web[<span class=\"hljs-string\">\"web-a\"</span>]: Destruction complete after 12s\naws_vpc.main: Destroying... [<span class=\"hljs-built_in\">id</span>=vpc-0ab123456]\naws_vpc.main: Destruction complete after 4s\nDestroy complete! Resources: 2 destroyed.\n</code></pre>\n<p>Order is reverse-dependency (instances before their VPC). Confirmation is prompted unless <code>-auto-approve</code>. Afterward, <code>terraform.tfstate</code> is updated to empty — the ledger matches the now-empty reality. Test-env cleanup is the legitimate daily use; <code>destroy -target=</code> removes ONE resource for debugging.</p>\n<h2 id=\"remember\">Remember</h2>\n<ul>\n<li>✓ <code>fmt</code> before every commit; <code>fmt -check</code> in CI. Formatting is a machine's job.</li>\n<li>✓ <code>destroy</code> prompts, deletes in reverse order, empties state. <code>-auto-approve</code> for pipelines only.</li>\n<li>⚠ PERMANENCE WARNING: <code>destroy</code> on prod state deletes real infrastructure with no undo — S3 versioning won't resurrect EC2s. Prod destroys belong behind manual approval gates, never on a laptop command line.</li>\n<li>🎓 Exam takeaway: \"which command normalizes style?\" → <code>fmt</code>. \"What does destroy update?\" → the state file (to empty).</li>\n</ul>\n","toc":[{"id":"terraform-fmt-amp-terraform-destroy","text":"terraform fmt &amp; terraform destroy","level":1},{"id":"1-fmt-canonical-style-zero-decisions","text":"1. fmt — canonical style, zero decisions","level":2},{"id":"2-destroy-behavior-transcript-aftermath","text":"2. destroy — behavior, transcript, aftermath","level":2},{"id":"remember","text":"Remember","level":2}],"words":246,"assets":[]};